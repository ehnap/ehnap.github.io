<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>跑断腿的租房过程杂谈</title>
    <url>/2019/03/23/it-life-tenement/</url>
    <content><![CDATA[<p>工作搞定之后，便开始了让人烦恼的看房租房的流程之中。这次比以往都要艰难，相隔一小时车程的两个地点，跑断腿这个形容词真的难以形容。整个过程历时三天，看了将近十套房子。过程中倒也积累了一些人生经验。</p>
<a id="more"></a>

<p>其实租房子，最难的是确定自己的需求，因为人的内心是非常善变的，很多人妄图靠感觉找到最合适自己的房子，我只能说无异于盲人摸象。只有真正对自己了如指掌的人，方能少走弯路，任何事情都是同理。</p>
<p>对于我而言，最重要的是私人空间，所以从一开始就将自如等合租方案排除了，最开始的时候我是有考虑带独卫的主卧，不过后来觉得做饭还是麻烦，所以就不考虑合租了。<strong>至此定下整租的基调</strong>。</p>
<p>另一个重点是公寓 or 小区房。二者在管理、户型、其他费用上有很大的不同。对于我来讲，还是喜欢小区房，毕竟住户不会太奇葩，而且水电物业的开销会小一些。</p>
<p>下一个重点是选址，每个人选址有不同的需求，而我比较看重的是与公司的距离，以及关于价格的参考。当在几个 APP 中简单看过之后，大概确定了价格范围，一公里内和三公里内两个能接受的范围，一公里以内比外边三公里以内租金贵五百块，这是我的实际情况，但考虑到，如果房源质量优秀，还是可以接受的，所以还是联系了一公里以内的一套房。是一个标准的商用公寓，一居室户型规规矩矩，但是没有阳台，只有飘窗，这样晾衣服着实捉急，加之价格不美丽，果断直接排除了一公里以内的所有房源。开始看另外一部分，幸运的是，在另外一个范围，一居室只有一个小区的一栋楼有，这极大的帮助我缩小了范围。开始了小区里的漫漫看房之路。</p>
<p>这里要说下我使用的 APP ：</p>
<ol>
<li>链家 &amp;&amp; 贝壳（因为他俩应该算是一家的 APP ）</li>
<li>zuber租房</li>
<li>自如</li>
<li>安居客</li>
<li>房天下</li>
<li>58</li>
</ol>
<p>使用频率最高的是前二的三个 APP 。zuber租房的图或者是视频质量也是极好的，但是房源太少，且联系了业主很多都不回话，留了微信不知道是不是微商套微信好友的。之所以非常高频的使用第一个，是因为房源是绝对真实的，图也是实拍，后三个的话，看看参考参考就好，很多真的是胡说八道。虽然我不考虑自如合租，但是还是下载了自如看整租，然后发现根本没几套，所以也就没怎么用。</p>
<p>锁定了小区之后，其实在 APP 中查询便是很容易的了。根据自己能接受的价格，找了几套，这里要注意的是，略高一点的价格也可以作为备选，因为很多房源是有议价空间的。在链家的 APP 选房，顺理成章地是选择链家中介的服务，但是我并没有在一棵树“吊死”，而是选择去小区楼下找附近中介实体店，让他们也帮忙带我看房。事实证明，这个选择是对的。一是有很多中介是有独家房源的，只在这一家中介挂，所以如果只选择一家中介，那必然看不全；二是当找了几个中介的时候，就会无形中让中介形成一种竞争关系，可以为以后杀价增加一些优势。<strong>但是尤其要注意，看房期间，可以让他们知道自己有找其他中介看房，但是不能让他们知道自己找了哪家中介看房以及看的哪套房</strong>。这是一种自己主动制造的信息不对称，是对自己有利的。</p>
<p>三天的时间里，看了将近十套房。主要关注<strong>价格</strong>、<strong>房屋位置</strong>、<strong>屋内配套</strong>、<strong>邻居</strong>、<strong>楼层楼号</strong>、<strong>房东信息</strong>。根据这几项，对十套房进行一个初步评分，筛选出三套，进入决赛，决赛很简单，就是看议价空间，这个时候反馈给中介，让他们去跟业主杀价，并且这个时候要透漏给中介，自己也有找其他中介在杀价，谁杀的低杀的快，就选谁。然后拿到房屋的低价，最后复看一遍决赛房屋，开始检查细节，包括柜子、屋子角落。一是根据细节调整评分，二是有时候会有意外收获（比方说翻到了之前的租房合同，就可以很清楚的知道自己拿到的是不是底价了，一般非首次出租的房屋，底价就是上次出租的价格）。</p>
<p>我在检查房子的时候，除了基本的配套设施是否齐全且能正常使用，另外要把柜子拉开，看看有没有味道，或者是发霉、或者是因为新柜子会有甲醛，另外顺带询问业主和上一个租客的信息，房子是不是首次出租等等。</p>
<p>就这样选出了想租的房子。这中间还有个小插曲：在看房的时候我发现房屋的东南向一两公里有陵园，其实我本人是不太介意的，但是我这人对合作的人会有一些判断影响我的合作意愿，所以我假装不知道，问中介那是什么地方，但是很遗憾，所有的中介都没有给我想要的答案，有的是不知道，有的是罔顾左右而言他。人性果然经不住考验，不过也可以理解，毕竟这个事一说，这单大概率泡汤。</p>
<p>其实根据这个套路选出的房子，不一定是性价比最高的，但一定是自己最喜欢的，因为租房的选择就有很强的主观意愿，而在自己打分的过程中，其实并不一定自己打分会很客观，而是这个分数可以在日后作为自己很强大的安慰，最终选出来的，在能想到的方面一定是自己满意的，即使不满意自己也有理由说服自己……</p>
<p>主观问题的正确答案，其实就是最能说服自己的答案而已。而且我相信天道酬勤，当自己付出了大量的精力在这方面，没理由会收获一个比较差的结果。</p>
]]></content>
      <categories>
        <category>IT-Life</category>
      </categories>
      <tags>
        <tag>随笔</tag>
        <tag>租房</tag>
        <tag>人生经验</tag>
      </tags>
  </entry>
  <entry>
    <title>【Qt源码笔记】简要说说 Qt5 中的 HighDPI 支持</title>
    <url>/2019/03/06/qt-source-highdpi/</url>
    <content><![CDATA[<p>想起之前在公司做的关于 HighDPI 的适配，在 Qt4 下可以说是比较繁琐，代码敲到手疼。早就听说 Qt5.6 开始支持了 <strong>HighDPI</strong> ，一直没机会看详细的代码。一直到开始做 <a href="https://github.com/ehnap/gal/releases" target="_blank" rel="noopener">Gal</a> ,才刚好在 Qt5 下需要 HighDPI 支持。用过之后，真的感叹，用起来太方便了。故看了一下详细实现。不过比较遗憾的是代码中有一个小瑕疵。</p>
<a id="more"></a>
<h2 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h2><p>其实想得到 Qt 给予的 HighDPI 支持，是非常之简单。只要在 <code>QApplication</code> 构造之前，开启 <code>Qt::AA_EnableHighDpiScaling</code>  这个属性。其实在代码中使用这个属性，等于环境中开启 <code>QT_AUTO_SCREEN_SCALE_FACTOR</code> 环境变量。还有另外的环境变量支持其他的 HighDPI 功能。这个参考文档即可</p>
<p>这里有一个小 <strong>tip</strong> ：HighDPI 只是是根据显示器的像素密度来调整大小。在 Qt 中，用过 <code>QFont</code> 的人都会知道。<code>QFont</code> 中有两个方法：<code>setPixelSize</code>、<code>setPointSize</code>很多人对此不是很明白，为什么要设置这两个方法。这里便可以找到答案。设置字体的<em>Pixel Size</em>，则会根据显示器的像素密度去改变字体大小；而设置字体的<em>Point Size</em>则不会更改，因为<em>Point Size</em>是基于显示器的物理单元。</p>
<p>关于 HighDPI ，一个比较良好的代码习惯，其实在 Qt 的 HighDPI 文档部分中有提到：</p>
<blockquote>
<ul>
<li>Always use the qreal versions of the QPainter drawing API.</li>
<li>Size windows and dialogs in relation to the screen size.</li>
<li>Replace hard-coded sizes in layouts and drawing code by values calculated from font metrics or screen size.</li>
</ul>
</blockquote>
<p>总而言之，使用的时候只要一个开关即可开启 HighDPI 支持，这一点让我还是十分好奇的。迫不及待地翻看了源码。</p>
<h2 id="代码实现"><a href="#代码实现" class="headerlink" title="代码实现"></a>代码实现</h2><p>其实关于 HighDPI 的代码，基本就在两部分中。</p>
<h3 id="一"><a href="#一" class="headerlink" title="一"></a>一</h3><p>其中一部分在 <strong>qtbase\src\gui\kernel</strong> 目录下 <code>qhighdpiscaling_p.h</code>、<code>qhighdpiscaling.cpp</code>这两个文件中的 <code>QHighDpiScaling</code> 类里。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Q_GUI_EXPORT</span> <span class="title">QHighDpiScaling</span> &#123;</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">public</span>:</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">initHighDpiScaling</span><span class="params">()</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">updateHighDpiScaling</span><span class="params">()</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">setGlobalFactor</span><span class="params">(qreal factor)</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">setScreenFactor</span><span class="params">(QScreen *window, qreal factor)</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">bool</span> <span class="title">isActive</span><span class="params">()</span> </span>&#123; <span class="keyword">return</span> m_active; &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">static</span> qreal <span class="title">factor</span><span class="params">(<span class="keyword">const</span> QWindow *window)</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">static</span> qreal <span class="title">factor</span><span class="params">(<span class="keyword">const</span> QScreen *screen)</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">static</span> qreal <span class="title">factor</span><span class="params">(<span class="keyword">const</span> QPlatformScreen *platformScreen)</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">static</span> QPoint <span class="title">origin</span><span class="params">(<span class="keyword">const</span> QScreen *screen)</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">static</span> QPoint <span class="title">origin</span><span class="params">(<span class="keyword">const</span> QPlatformScreen *platformScreen)</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">static</span> QPoint <span class="title">mapPositionFromNative</span><span class="params">(<span class="keyword">const</span> QPoint &amp;pos, <span class="keyword">const</span> QPlatformScreen *platformScreen)</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">static</span> QPoint <span class="title">mapPositionToNative</span><span class="params">(<span class="keyword">const</span> QPoint &amp;pos, <span class="keyword">const</span> QPlatformScreen *platformScreen)</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">static</span> QDpi <span class="title">logicalDpi</span><span class="params">()</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">private</span>:</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">static</span> qreal <span class="title">screenSubfactor</span><span class="params">(<span class="keyword">const</span> QPlatformScreen *screen)</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">static</span> qreal m_factor;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">static</span> <span class="keyword">bool</span> m_active;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">static</span> <span class="keyword">bool</span> m_usePixelDensity;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">static</span> <span class="keyword">bool</span> m_globalScalingActive;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">static</span> <span class="keyword">bool</span> m_pixelDensityScalingActive;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">static</span> <span class="keyword">bool</span> m_screenFactorSet;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">static</span> QDpi m_logicalDpi;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;;</span></pre></td></tr></table></figure>
<p>这个类最大的特色可以说就是纯静态的了。不过按照逻辑来说，也是合理的。其中 <code>initHighDpiScaling()</code>、<code>updateHighDpiScaling()</code>可以说是两个比较重要的方法了，这两个方法掌管着整个 HighDPI 支持的命脉。其实里边的内容只是一些方法的简单包装。只看堆栈调用的话：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&gt;   Qt5Guid.dll!QHighDpiScaling::initHighDpiScaling() 行 254	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line"> 	Qt5Guid.dll!QGuiApplicationPrivate::createPlatformIntegration() 行 1301	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line"> 	Qt5Guid.dll!QGuiApplicationPrivate::createEventDispatcher() 行 1403	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line"> 	Qt5Widgetsd.dll!QApplicationPrivate::createEventDispatcher() 行 187	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line"> 	Qt5Cored.dll!QCoreApplicationPrivate::init() 行 859	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line"> 	Qt5Guid.dll!QGuiApplicationPrivate::init() 行 1431	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line"> 	Qt5Widgetsd.dll!QApplicationPrivate::init() 行 569	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line"> 	Qt5Widgetsd.dll!QApplication::QApplication(int &amp; argc, char * * argv, int _internal) 行 556	C++</span></pre></td></tr></table></figure>
<p>可以看出，在 <code>QApplication</code> 构造的时候，会走入 HighDPI 的相关逻辑，这也是文档中要求要在构造之前开启开关是一致的，因为构造的时候就要检查这个属性的状态。</p>
<p>实际计算缩放因子的方法，应该是这个：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">qreal QHighDpiScaling::screenSubfactor(<span class="keyword">const</span> QPlatformScreen *screen)</span></pre></td></tr></table></figure>
<p>逻辑也是十分简单了。不用做过多解释。不过这里边有一个<code>pixelDensity()</code>的调用，内容还挺有意思的。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">qreal QWindowsScreen::pixelDensity() <span class="keyword">const</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="comment">// QTBUG-49195: Use logical DPI instead of physical DPI to calculate</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="comment">// the pixel density since it is reflects the Windows UI scaling.</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="comment">// High DPI auto scaling should be disabled when the user chooses</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="comment">// small fonts on a High DPI monitor, resulting in lower logical DPI.</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> qMax(<span class="number">1</span>, qRound(logicalDpi().first / <span class="number">96</span>));</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>
<p>这里边的逻辑可以明显地看到，当我们在 Windows 系统下使用类似 125% 的缩放比例的时候，这里边计算到的缩放比例还是 1。然后去 Qt BugReport 看了一下。<a href="https://bugreports.qt.io/browse/QTBUG-70721" target="_blank" rel="noopener">QTBUG-70721</a> 就是这个问题。</p>
<h3 id="二"><a href="#二" class="headerlink" title="二"></a>二</h3><p>上边说到，代码实现有两部分，另外一部分则是在 <strong>qtbase\src\widgets\styles</strong> 目录下的<code>qstylehelper_p.h</code>、<code>qstylehelper.cpp</code>中的<code>QStyleHelper</code>命名空间中。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">qreal <span class="title">dpiScaled</span><span class="params">(qreal value)</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span> Q_OS_MAC</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="comment">// On mac the DPI is always 72 so we should not scale it</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> value;</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">else</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">static</span> <span class="keyword">const</span> qreal scale = qreal(qt_defaultDpiX()) / <span class="number">96.0</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> value * scale;</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>
<p>如果在 Qt4 下有做过 HighDPI 的相关逻辑，想必对这个方法是不陌生的。至此基本上 Qt HighDPI 支持的代码逻辑基本找全。</p>
<h2 id="小瑕疵"><a href="#小瑕疵" class="headerlink" title="小瑕疵"></a>小瑕疵</h2><p>上边我提到过代码中的小瑕疵。就在上边那段代码上。不难看出这个<code>scale</code>是一个函数中的静态变量，后续对这个函数再次调用已经不改变<code>scale</code>的值了。</p>
<p>看到这里会觉得，大概是个隐患，然后再来看<code>qt_defaultDpiX()</code>这个方法：(这个方法在 <strong>qtbase\src\gui\text</strong> 目录的<code>qfont.cpp</code>文件中)</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">Q_GUI_EXPORT <span class="keyword">int</span> <span class="title">qt_defaultDpiX</span><span class="params">()</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (QCoreApplication::instance()-&gt;testAttribute(Qt::AA_Use96Dpi))</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span> <span class="number">96</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (!qt_is_gui_used)</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span> <span class="number">75</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (<span class="keyword">const</span> QScreen *screen = QGuiApplication::primaryScreen())</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span> qRound(screen-&gt;logicalDotsPerInchX());</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="comment">//PI has not been initialised, or it is being initialised. Give a default dpi</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> <span class="number">100</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>
<p>看到这里也就只有第三个 if 会导致这个方法的返回值不确定。</p>
<p>那很自然的就会想到，如果当 <code>dpiScaled</code> 调用的时候第三个 if 不起作用，那将是可怕的结果。所以紧接着探究这个 <code>screen</code> 。这部分过程略过，直接说结论。screen 能正常取到的前提是 <code>QGuiApplicationPrivate::screen_list</code> 这个列表是有内容的。而这个列表第一次被添加的时机堆栈：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&gt;	Qt5Guid.dll!QPlatformIntegration::screenAdded(QPlatformScreen * ps, bool isPrimary) 行 478	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line"> 	qwindowsd.dll!QWindowsIntegration::emitScreenAdded(QPlatformScreen * s, bool isPrimary) 行 110	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line"> 	qwindowsd.dll!QWindowsScreenManager::handleScreenChanges() 行 546	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line"> 	qwindowsd.dll!QWindowsIntegration::QWindowsIntegration(const QStringList &amp; paramList) 行 276	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line"> 	qwindowsd.dll!QWindowsGdiIntegration::QWindowsGdiIntegration(const QStringList &amp; paramList) 行 59	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line"> 	qwindowsd.dll!QWindowsIntegrationPlugin::create(const QString &amp; system, const QStringList &amp; paramList, int &amp; __formal, char * * __formal) 行 114	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line"> 	Qt5Guid.dll!qLoadPlugin&lt;QPlatformIntegration,QPlatformIntegrationPlugin,QStringList const &amp; __ptr64,int &amp; __ptr64,char * __ptr64 * __ptr64 &amp; __ptr64&gt;(const QFactoryLoader * loader, const QString &amp; key, const QStringList &amp; &lt;args_0&gt;, int &amp; &lt;args_1&gt;, char * * &amp; &lt;args_2&gt;) 行 108	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line"> 	Qt5Guid.dll!QPlatformIntegrationFactory::create(const QString &amp; platform, const QStringList &amp; paramList, int &amp; argc, char * * argv, const QString &amp; platformPluginPath) 行 72	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line"> 	Qt5Guid.dll!init_platform(const QString &amp; pluginNamesWithArguments, const QString &amp; platformPluginPath, const QString &amp; platformThemeName, int &amp; argc, char * * argv) 行 1179	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line"> 	Qt5Guid.dll!QGuiApplicationPrivate::createPlatformIntegration() 行 1383	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line"> 	Qt5Guid.dll!QGuiApplicationPrivate::createEventDispatcher() 行 1403	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line"> 	Qt5Widgetsd.dll!QApplicationPrivate::createEventDispatcher() 行 187	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line"> 	Qt5Cored.dll!QCoreApplicationPrivate::init() 行 859	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line"> 	Qt5Guid.dll!QGuiApplicationPrivate::init() 行 1431	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line"> 	Qt5Widgetsd.dll!QApplicationPrivate::init() 行 569	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line"> 	Qt5Widgetsd.dll!QApplication::QApplication(int &amp; argc, char * * argv, int _internal) 行 556	C++</span></pre></td></tr></table></figure>
<p>从这里可以看到，是在 <code>QApplication</code> 构造的时候。</p>
<p>所以可以得出一个结论，当在<code>QApplication</code>构造的之前调用<code>QStyleHelper::dpiScaled</code>得到的结果则可能不是准确的，也会导致，在以后得到结果都是错误的。没有经验的人也许会觉得在<code>QApplication</code>构造之前调用这个是没意义的，所以认为这个调用并不常见。此处我举一例以供参考。</p>
<p>很多人习惯提前定义一些比较固定的量，在某个 cpp 中，也许我们能看到这样一种代码，它有可能是直接写成，也有可能在实现 HighDPI 过程中更改而成</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">namespace</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    qreal testa_width = QStyleHelper::dpiScaled(<span class="number">1</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> qreal testb_width = QStyleHelper::dpiScaled(<span class="number">1</span>);</span></pre></td></tr></table></figure>
<p>如果在代码的上方出现了这种，则它们就属于是一种比较可怕的代码，可以影响全局调用<code>dpiScaled</code>得不到正确结果。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>不过即使有一点点小瑕疵，但是 Qt 对 HighDPI 的实现，以及调用设计还是有很多值得借鉴之处的。本文也只是对 Qt HighDPI 支持比较简要的分析，还有很多细节，限于篇幅，并没有展开来说……</p>
]]></content>
      <categories>
        <category>Programming</category>
      </categories>
      <tags>
        <tag>Qt</tag>
        <tag>源码分析</tag>
      </tags>
  </entry>
  <entry>
    <title>【Qt源码笔记】深谈 Qt 绘制</title>
    <url>/2018/12/19/qt-source-paint/</url>
    <content><![CDATA[<p>之前写了一篇 <a href="https://ehnap.me/2018/12/10/qt-source-paint-control/">浅谈Qt控件绘制</a> 。之所以叫浅谈是因为调用都是比较表层的调用。其实 Qt 的绘制，可以说用 Qt 的人都有用到，但是对于绘制底层，了解的人并不见得很多。我其实之前也是云山雾罩，从来没有深究过。所以想着知其然还是要知其所以然。</p>
<a id="more"></a>
<h2 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h2><p>在 Windows 平台 默认的 Qt 绘制，最终到底层，是直接调用<strong>指令集指令</strong>的，这有别于我最初的猜测，我以为是用 Windows API 。这着实让我吃了一惊。这让我对 Qt 的性能又放心了一些。</p>
<h2 id="探究过程"><a href="#探究过程" class="headerlink" title="探究过程"></a>探究过程</h2><p>其实研究这个，比其他的更好溯源。附上三段堆栈信息。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">	Qt5Guid.dll!BLEND_SOURCE_OVER_ARGB32_AVX2(unsigned int * dst, const unsigned int * src, const int length) 行 184	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line">	Qt5Guid.dll!qt_blend_argb32_on_argb32_avx2(unsigned char * destPixels, int dbpl, const unsigned char * srcPixels, int sbpl, int w, int h, int const_alpha) 行 253	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line">	Qt5Guid.dll!QRasterPaintEnginePrivate::drawImage(const QPointF &amp; pt, const QImage &amp; img, void(*)(unsigned char *, int, const unsigned char *, int, int, int, int) func, const QRect &amp; clip, int alpha, const QRect &amp; sr) 行 1057	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line">	Qt5Guid.dll!QRasterPaintEngine::drawImage(const QPointF &amp; p, const QImage &amp; img) 行 2250	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line">	Qt5Guid.dll!QRasterPaintEngine::drawPixmap(const QPointF &amp; pos, const QPixmap &amp; pixmap) 行 2128	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line">	Qt5Guid.dll!QPainter::drawPixmap(const QPointF &amp; p, const QPixmap &amp; pm) 行 5079	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line">	Qt5Guid.dll!QPainter::drawPixmap(const QPoint &amp; p, const QPixmap &amp; pm) 行 796	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">------------------</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">Qt5Guid.dll!alphargbblend_argb32(unsigned int * dst, unsigned int coverage, const QRgba64 &amp; srcLinear, unsigned int src, const QColorProfile * colorProfile) 行 5771	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line">	Qt5Guid.dll!qt_alphargbblit_argb32(QRasterBuffer * rasterBuffer, int x, int y, const QRgba64 &amp; color, const unsigned int * src, int mapWidth, int mapHeight, int srcStride, const QClipData * clip, bool useGammaCorrection) 行 5878	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line">	Qt5Guid.dll!QRasterPaintEngine::alphaPenBlt(const void * src, int bpl, int depth, int rx, int ry, int w, int h, bool useGammaCorrection) 行 2723	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line">	Qt5Guid.dll!QRasterPaintEngine::drawCachedGlyphs(int numGlyphs, const unsigned int * glyphs, const QFixedPoint * positions, QFontEngine * fontEngine) 行 2976	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line">	Qt5Guid.dll!QRasterPaintEngine::drawTextItem(const QPointF &amp; p, const QTextItem &amp; textItem) 行 3183	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line">	Qt5Guid.dll!QPainterPrivate::drawTextItem(const QPointF &amp; p, const QTextItem &amp; _ti, QTextEngine * textEngine) 行 6531	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line">	Qt5Guid.dll!QTextLine::draw(QPainter * p, const QPointF &amp; pos, const QTextLayout::FormatRange * selection) 行 2615	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line">	Qt5Guid.dll!qt_format_text(const QFont &amp; fnt, const QRectF &amp; _r, int tf, const QTextOption * option, const QString &amp; str, QRectF * brect, int tabstops, int * ta, int tabarraylen, QPainter * painter) 行 7702	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line">	Qt5Guid.dll!QPainter::drawText(const QRect &amp; r, int flags, const QString &amp; str, QRect * br) 行 5955	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">   -------------------</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">Qt5Guid.dll!qt_memfill32(unsigned int * dest, unsigned int value, int count) 行 262	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line">	Qt5Guid.dll!qt_memfill&lt;unsigned int&gt;(unsigned int * dest, unsigned int color, int count) 行 901	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line">	Qt5Guid.dll!blend_color_argb(int count, const QT_FT_Span_ * spans, void * userData) 行 4347	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line">	Qt5Guid.dll!qt_span_fill_clipRect(int count, const QT_FT_Span_ * spans, void * userData) 行 4229	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line">	Qt5Guid.dll!QSpanBuffer::flushSpans() 行 112	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line">	Qt5Guid.dll!QSpanBuffer::~QSpanBuffer() 行 87	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line">	Qt5Guid.dll!QRasterizer::rasterizeLine(const QPointF &amp; a, const QPointF &amp; b, double width, bool squareCap) 行 1191	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line">	Qt5Guid.dll!QRasterPaintEngine::fillRect(const QRectF &amp; r, QSpanData * data) 行 1858	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line">	Qt5Guid.dll!QRasterPaintEngine::fillRect(const QRectF &amp; r, const QBrush &amp; brush) 行 1882	C++</span></pre></td></tr><tr><td class="code"><pre><span class="line">	Qt5Guid.dll!QPainter::fillRect(const QRect &amp; r, const QBrush &amp; brush) 行 6971	C++</span></pre></td></tr></table></figure>
<p>只要从绘制代码，单步调试即可找到指定地点。不过最终使用的指令集却并不一样。有的用 <strong>avx2</strong> 、有的则是用 <strong>sse2</strong> 。如果想探究指令集部分的使用，需要到源码目录 <strong>qtbase\src\gui\painting</strong> ，根据目录下代码文件名即可知道是哪种指令集，一目了然。</p>
<p>回过头来再看上边的那些函数调用。其实不难发现，所有的绘制在中间都必然要经过<code>QPaintEngine</code>。<code>QRasterPaintEngine</code>只不过是它的一个派生，这个后边再说。而 <code>QPaintEngine</code> 根据所要绘制的内容，来区分绘制逻辑，比方说涂色采用填充 buffer 、统一刷新的方式；字体绘制要调用字体图元相关绘制逻辑等等。</p>
<p><em>所有的表层绘制都要经过绘制引擎来向下传递绘制信息。这是 Qt 作为一个高级框架的闪光点，在其他的 Qt 模块也有类似发现，比如控件的绘制上。这样看来 Qt 这个框架能给我们的，除了代码逻辑本身，还有设计。</em></p>
<h2 id="意外收获"><a href="#意外收获" class="headerlink" title="意外收获"></a>意外收获</h2><p>在整个代码探究的过程，我发现了这样一段代码，可以说是非常惊喜了。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (pd-&gt;devType() == QInternal::Pixmap)</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">static_cast</span>&lt;QPixmap *&gt;(pd)-&gt;<span class="built_in">detach</span>();</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">else</span> <span class="keyword">if</span> (pd-&gt;devType() == QInternal::Image)</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">static_cast</span>&lt;QImage *&gt;(pd)-&gt;<span class="built_in">detach</span>();</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">d-&gt;engine = pd-&gt;paintEngine();</span></pre></td></tr></table></figure>
<p>这段代码是<code>QPainter::begin()</code>中的代码。当时是在研究<code>QWidget</code>的绘制过程中，走到了这里。只看代码很难体验它的神奇之处。</p>
<p><code>pd</code> 在前边是 <code>QWidget</code> 的一个指针，当经过这个 <code>if</code> 语句之后，<code>pd</code> 就变成了一个 <code>QImage</code> 指针。这不可谓之不神奇。对于稍微对 Qt 源码有一些理解的同学对 <code>detach()</code> 并不陌生，它本是 Qt 中最常用的 <strong>Copy-on-Write</strong> 的实现。不过经常用于在类的成员方法中调用，今天看到它这种用法着实惊艳到了。至于为什么这种用法可行，这也是一个可研究的点，有时间，将其整理出来。这段代码算是研究绘制过程中的一个小礼物，这也解开了<code>QWidget</code>绘制的本质。至于<code>QWidget</code>的绘制，也是一个很有意思的东西了，以后有机会详细整理一下。</p>
<h2 id="附注"><a href="#附注" class="headerlink" title="附注"></a>附注</h2><p>之前我说<code>QRasterPaintEngine</code>只是<code>QPaintEngine</code>的派生类。我也说 Windows 平台下默认的 Qt 绘制是使用指令集的。原因就在于默认条件下，绝大部分的<code>QPaintDevice</code>是选择用<code>QRasterPaintEngine</code>的，这里我说绝大部分是因为，我没有完整的看过所有派生自<code>QPaintDevice</code>的类的代码。而选择用何种<code>QPaintEngine</code>具体逻辑可以以<code>QImage</code>为例：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">QPaintEngine *QImage::paintEngine() <span class="keyword">const</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (!d)</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (!d-&gt;paintEngine) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        QPaintDevice *paintDevice = <span class="keyword">const_cast</span>&lt;QImage *&gt;(<span class="keyword">this</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        QPaintEngine *paintEngine = <span class="number">0</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        QPlatformIntegration *platformIntegration = QGuiApplicationPrivate::platformIntegration();</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">if</span> (platformIntegration)</span></pre></td></tr><tr><td class="code"><pre><span class="line">            paintEngine = platformIntegration-&gt;createImagePaintEngine(paintDevice);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        d-&gt;paintEngine = paintEngine ? paintEngine : <span class="keyword">new</span> QRasterPaintEngine(paintDevice);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> d-&gt;paintEngine;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>
<p>简而言之就是取决于<code>QGuiApplicationPrivate::platformIntegration()</code>的返回值。至于这个调用相关的，那是有关 <strong>QPA</strong> 的范畴了，就不再这篇赘述了。有时间再整理 <strong>QPA</strong> 相关的内容出来。</p>
<h2 id="后记"><a href="#后记" class="headerlink" title="后记"></a>后记</h2><p>对于 Qt 绘制的深入探究，可以说是受益匪浅，这篇文章只是描述了冰山一角，其实整个流程比这个简要概括要高级的多。从研究 Qt 源码至今，对整个 Qt 项目的感受与评价，已和往日截然不同。而网上大部分人对 Qt 的评价，其实在我看来，无异于盲人摸象。只有对源码稍有了解的人，才知道 Qt 这个项目，对于客户端开发人员的价值。</p>
]]></content>
      <categories>
        <category>Programming</category>
      </categories>
      <tags>
        <tag>Qt</tag>
        <tag>源码分析</tag>
      </tags>
  </entry>
  <entry>
    <title>【Qt源码笔记】浅谈 Qt 中的控件绘制</title>
    <url>/2018/12/10/qt-source-paint-control/</url>
    <content><![CDATA[<p>在工作中经常要根据 UI 提供的稿子做自绘控件，而且在新项目中，我自己基于 Qt 做了一套项目自用控件库，还会涉及到换肤，所以对 Qt 的控件绘制，着重的研究了一下。看过代码之后，觉得 Qt 项目本身，确实是做 UI 发家，绘制流程清晰，可以说是做界面的范本了。将流程梳理记录一下。</p>
<a id="more"></a>
<h2 id="分类"><a href="#分类" class="headerlink" title="分类"></a>分类</h2><p>对于自绘控件的分类，我将其分为两大类：</p>
<ul>
<li>基于 Qt 控件类派生</li>
<li>基于 QWidget 派生</li>
</ul>
<p>除非是行为跟 Qt 本身的控件相差太多，或者是缺少需要的交互逻辑，否则的话尽可能从 Qt 现有的控件类派生，这是一条基本的原则。因为绘制上来说重写 <code>paintEvent</code> 方法，基本就可以达到想要的目的，而控件类本身会提供一些现成的控件逻辑，这样会大大减少开发难度。</p>
<h2 id="绘制流程"><a href="#绘制流程" class="headerlink" title="绘制流程"></a>绘制流程</h2><p>其实提起绘制流程来说，无非就是重写 <code>paintEvent</code> 方法。但是如果要做一整套 UI 库，没有结构，都在 <code>paintEvent</code> 里边写死，在后期加换肤，或者是在代码整洁度上都会大打折扣。 Qt 本身的控件绘制，就可以给我们很大的启示。以绘制事件方法代码最简单的 QPushbutton 为例，足见一斑：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> QPushButton::paintEvent(QPaintEvent *)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="function">QStylePainter <span class="title">p</span><span class="params">(<span class="keyword">this</span>)</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    QStyleOptionButton option;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    initStyleOption(&amp;option);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    p.drawControl(QStyle::CE_PushButton, option);</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>
<p>可以简单理解为两个流程：</p>
<ol>
<li>装配绘制必要上下文</li>
<li>绘制</li>
</ol>
<p>Qt 考虑到对于按钮样式，开发者有着自己的需求，而对于逻辑状态可能开发者可以用现成的。所以 <code>initStyleOption</code> 是一个保护方法，如果开发者没有对按钮状态的特殊要求，用这个方法，就可以把图标，按钮的点击状态什么的放进这个 <code>option</code> 中。在绘制的时候直接拿来用。</p>
<p>而下边的绘制，可以看到这里是用了 <code>QStylePainter</code> 而不是常规的 <code>QPainter</code>。不过 <code>QStylePainter</code> 和 <code>QPainter</code> 差别不大，前者只是在里边保存了当前 <code>QWidget::style()</code>的指针。所以这段代码改成：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">QPainter <span class="title">p</span><span class="params">(<span class="keyword">this</span>)</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">QStyleOptionButton option;</span></pre></td></tr><tr><td class="code"><pre><span class="line">initStyleOption(&amp;option);</span></pre></td></tr><tr><td class="code"><pre><span class="line">style()-&gt;drawControl(QStyle::CE_PushButton, option, &amp;p, <span class="keyword">this</span>);</span></pre></td></tr></table></figure>
<p>也是一样的。如果你翻看其他控件，比方说 <code>QMenu</code> 就是下边这种用法，可能是开发人员的不同习惯，但是他们逻辑是一致的。</p>
<p>Qt 的绘制精髓就在于此。对于绘制， Qt 控件是交给 style 来管理的。打开源码路径 <strong>qtbase\src\widgets\styles</strong> 这里边保存了 Qt 绘制的基本样式。为什么说是基本样式呢，因为还有一部分 <strong>qtbase\src\plugins\styles</strong> 在这个目录下，这所有的加一起就是全部样式了。话说回来， Qt 正是因为将绘制逻辑都保存在了 style 中，所以 Qt 才可以在不同平台都表现的像一个原生控件一样，“千平台千面”。</p>
<p>走到这里，就不得不一探 <code>QStyle</code> 的究竟了。<code>QStyle</code> 是一个抽象类，头文件因为包含了太多枚举，所以特别长，就不粘了。里边关于绘制的几个方法是纯虚的，在绘制方法中，通过传进去的枚举类型，来找到对应控件的绘制逻辑。除了绘制方法，还有一些是计算绘制区域大小的方法，以及 <em>hitTest</em> 获取子控件的方法。这就是整个 Qt 绘制的大本营了。而对于 Qt 控件的实际绘制逻辑，其实也对做自绘控件库也十分有帮助，不过既然是浅谈，也就不再罗嗦。</p>
]]></content>
      <categories>
        <category>Programming</category>
      </categories>
      <tags>
        <tag>Qt</tag>
        <tag>源码分析</tag>
      </tags>
  </entry>
  <entry>
    <title>【Qt源码笔记】 QImage 源码探究过程中的记忆碎片</title>
    <url>/2018/11/29/qt-source-qimage-fragment/</url>
    <content><![CDATA[<p>在公司曾经做有关图片的一个新项目。当时仔细研究了一下<code>QImage</code>的源码，将一些碎片化的东西记录在了某个平台上，如今翻出来了，感觉这些内容还可以再细化细化，有时间的话，详细整理，先将以前的碎片在此记录下来。</p>
<a id="more"></a>

<ol>
<li><p>所有的创建都由内部的<code>QImageData</code>类的<code>create</code>方法实现。即使传入<code>const char*</code> ，但是在创建时依然会被转为 <strong>非 const</strong> ，而在<code>create</code>中改为使用只读标志。</p>
</li>
<li><p><code>QImageData</code> 类中出现了一种叫位域的东西，可以指定这个东西占几位：<code>uint a:1;</code></p>
</li>
<li><p>在 <code>QImage</code> 拷贝构造时，如果 <code>QImage</code> 绑定了 <code>QPaintDevice</code> 或者是内部的 <code>QImageData</code> 标记了锁，则会复制一份全新的 <code>QImage</code> ；否则两个 <code>QImage</code> 共享一个<code>QImageData</code>。值得注意的是，Qt中用到的<code>swap</code>是标准库的。</p>
</li>
<li><p><code>QImage</code>中对于<code>set</code>或者是返回 <strong>非 const</strong> 的函数中使用<code>detach()</code>来实现写时拷贝</p>
</li>
<li><p><code>QImage</code>中的高质量抖动用的是 <strong>Floyd-SteinBerg</strong> 算法</p>
</li>
<li><p><code>QImage</code>中的<code>setText()</code>可以将字符串以 UTF-8 编码储存到图片里，但是并非所有的图片格式都支持，需要用<code>QImageWriter</code>去检查一下格式是否支持才可以使用。</p>
</li>
<li><p><code>QImage</code>中的<code>bitPlaneCount()</code>可以用来判断图片的有效位，因为类似 RGB32 这种，实际上它的有效位只有24位</p>
</li>
<li><p><code>QPixmap</code>中的<code>load()</code>是有缓存的，内部用<code>QPixmapCache</code>实现缓存。用下边的 key 做索引。自己也可以使用<code>QPixmapCache</code>根据实际需要做缓存。</p>
 <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">QString key &#x3D; QLatin1String(&quot;qt_pixmap&quot;)</span></pre></td></tr><tr><td class="code"><pre><span class="line">                % info.absoluteFilePath()</span></pre></td></tr><tr><td class="code"><pre><span class="line">                % HexString&lt;uint&gt;(info.lastModified().toTime_t())</span></pre></td></tr><tr><td class="code"><pre><span class="line">                % HexString&lt;quint64&gt;(info.size())</span></pre></td></tr><tr><td class="code"><pre><span class="line">                % HexString&lt;uint&gt;(data ? data-&gt;pixelType() : QPlatformPixmap::PixmapType);</span></pre></td></tr></table></figure>
</li>
<li><p><code>QPixmap</code>中<code>scaled()</code>用的就是<code>QImage</code>那一套缩放方法</p>
</li>
<li><p><code>QBitmap</code>实际上是<code>QPixmap</code>的二次封装</p>
</li>
<li><p><code>QImageRead</code>本质上是一个包装了接口的类。在读图片的时候会将Qt支持的图片格式全都尝试一遍，包括 imageformat 下的插件。优先寻找最可能匹配的格式( format 这个参数)。找到后拿到这个格式的 <em>handler</em> 。每一个格式本身都会实现作为 <em>handler</em> 的必要接口，以供<code>QImageReader</code>方法调用。</p>
</li>
</ol>
]]></content>
      <categories>
        <category>Programming</category>
      </categories>
      <tags>
        <tag>Qt</tag>
        <tag>源码分析</tag>
      </tags>
  </entry>
  <entry>
    <title>Effective Qt</title>
    <url>/2018/10/09/effective-qt-ing/</url>
    <content><![CDATA[<p>标题是非常的哗众取宠了。这里主要是用来记录一些使用 Qt 的小经验。之前在工作中，会有一些关于 Qt 的经验总结，加之平时自己写一些小东西也有使用 Qt ，所以使用起来会有一些经验教训，聚集起来，姑且叫做 Effective Qt 了。随时想到会随时更新内容。（<strong>注：这里会有一些代码设计的思路，不存在绝对的正误，只是一种习惯</strong>）</p>
<a id="more"></a>

<ol>
<li>避免在子控件中使用 <code>parentWidget()</code> 方法获取 <code>QWidget</code> 指针强制转换为父控件指针，调用父对象方法。<blockquote>
<p>在对子控件布局的时候，某些情况下实例化子控件时传递的父指针会被置空，例如对子控件设置一些特殊的窗口标志。</p>
</blockquote>
</li>
<li>在 <code>void paintEvent(QPaintEvent* e)</code> 方法中的逻辑要避免出现间接调用 <code>update()</code> 方法。<blockquote>
<p>由于 <code>update()</code> 方法与 <code>repaint()</code> 机制不同，所以当出现上述情况，并不会直接爆栈，迅速发现问题。而是在某些会导致频繁重绘的操作下，程序异常卡顿，但是不涉及重绘的操作，程序又看起来没问题。</p>
</blockquote>
</li>
<li>避免匿名空间内声明 Qt 类型、避免类外声明<code>static</code> Qt 类型。<blockquote>
<p>避免 Qt 类型的某些内容比 <code>QCoreApplication</code> 更早的初始化，导致一些数值异常。比如在更早的时机调用 <code>QStyle::dpiscaled()</code>，导致无法得到正确的系统DPI。</p>
</blockquote>
</li>
<li>获取 <code>connect()</code> 方法的返回值，可以在 debug 时快速的发现自己的槽和信号没有正确的连接。</li>
<li>如果槽失效了，99% 是以下几种情况，剩下 1% 我没遇到过<blockquote>
<ol>
<li>类没有继承自 QObject</li>
<li>没有写 Q_OBJECT 宏</li>
<li>槽(信号)的定义没有写在 slots(signals) 下边</li>
<li>connect 的时候写错了槽(信号)的名字或者参数</li>
</ol>
</blockquote>
</li>
<li><code>adjustSize()</code>、<code>updateGeometry()</code>以及<code>sizeHint()</code>是处理复杂界面布局变动的利器。<blockquote>
<p>简单来说 <code>adjustSize()</code> 是根据子控件调整自己的大小，<code>updateGeometry()</code> 是根据自己的大小和子控件的大小调整子控件的布局。在处理复杂布局时，还需要配合每一个控件本身的 <code>sizePolicy()</code> 以及布局的 <code>sizeConstraint()</code> 方可显示出Qt动态布局的威力</p>
</blockquote>
</li>
<li><code>updateGeometry()</code> 并不是每次调用都一定会生效，有时候你需要 <code>QLayout::invalidate()</code> 来辅助你更新布局。<blockquote>
<p>Qt只会在它觉得需要重新更新的时候才更新(具体的判断可以参考源码，大意是说调过了 <code>setXXXSize()</code> 这种方法以后，Qt才认为需要重新更新)。这个时候你可以选择拿到控件的 <code>QLayout</code> 然后循环对每个布局调用 <code>QLayout::invalidate()</code> ；在<strong>比较特殊</strong>的情况下你可以选择用 <code>setFixedSize(sizeHint())</code> 这种奇怪的调用代替循环，当然请在你非常熟悉你在做什么的时候才去用这种方式代替循环。</p>
</blockquote>
</li>
<li>在发现有一个需求需要用到 Qt 的某些东西而又调不到时，请再三确认， Qt 有没有提供一些虚方法可以重写，大部分的需求都是可以通过某些虚方法完成，只有极少数需要将 Qt 内部的东西移出来用。</li>
<li>尽量使用 <code>QLayout</code> 来控制控件的位置，而不是套用一个又一个的 <code>QWidget</code> ，因为永远不能预料到产品会把一个页面上的哪两个控件联系在一起，如果这些控件相隔了几个 <code>QWidget</code> ，拿到它们的指针将是一件非常蛋疼的事</li>
<li>槽的命名应该以反映他被调用的时机，而不是他的功能。方便后边的人想在某个信号发送的时候，快速的找到一个准确的槽位置，而不会同一个信号在同一个类里绑定了多个槽</li>
</ol>
<p><strong>未完待续……</strong></p>
]]></content>
      <categories>
        <category>Programming</category>
      </categories>
      <tags>
        <tag>Qt</tag>
      </tags>
  </entry>
  <entry>
    <title>狼人杀中的诈身份</title>
    <url>/2018/09/30/wolfkill-cheat-id/</url>
    <content><![CDATA[<p>狼人杀中，诈身份可谓是最常见的行为，无论是新手还是老手都喜欢使用这个伎俩。但是很遗憾并不是所有人都能用好这一技术手段。狼人杀这个游戏有方法、无定法，所以特将诈身份的一般套路整理出来，以备日后翻看。</p>
<a id="more"></a>
<h2 id="诈身份的手段"><a href="#诈身份的手段" class="headerlink" title="诈身份的手段"></a>诈身份的手段</h2><p>常用的诈身份手段：</p>
<ul>
<li>警上跳预言家发后置位查杀（金水）</li>
<li>警上跳女巫声称盲毒后置位</li>
<li>警上接到查杀跳猎人钢板</li>
</ul>
<h2 id="诈身份的目的"><a href="#诈身份的目的" class="headerlink" title="诈身份的目的"></a>诈身份的目的</h2><p>通常大部分人只记得上述行为可以诈身份，但是并没记住诈身份的目的是什么。</p>
<p>首先要明确的是，任何行为都要有收益，这是一条公认的事实，否则好人不为好人玩，这个游戏就会变得扑朔迷离。</p>
<p>而诈身份的<strong>收益</strong>或者说<strong>目的</strong>在于</p>
<blockquote>
<p>炸出来狼牌，可以帮助好人排出隐在好人堆里的一狼。</p>
</blockquote>
<h2 id="不同手段的使用方法"><a href="#不同手段的使用方法" class="headerlink" title="不同手段的使用方法"></a>不同手段的使用方法</h2><p>回头来看几种诈身份的手段</p>
<h3 id="跳预言家"><a href="#跳预言家" class="headerlink" title="跳预言家"></a>跳预言家</h3><p>有的人上警随便跳一个预言家给后置位发一个查杀，警徽流也没有。</p>
<p>有的人跳预言家刚在警上不退水，说是为预言家挡刀。</p>
<p>有的人觉得发警下查杀不构成诈身份退不了水。</p>
<p>有的人觉得发金水不构成诈身份退不了水。</p>
<p>这些说法或做法都有些绝对。因为没有抓住诈身份的目的。</p>
<h4 id="有收益的诈身份"><a href="#有收益的诈身份" class="headerlink" title="有收益的诈身份"></a>有收益的诈身份</h4><ul>
<li><p>发到真正的狼牌查杀，且状态像个预言家</p>
<blockquote>
<p>当你漫不经心的随便给后置位发一个查杀，发到好人身上，这次诈身份的收益为0。因为你并不能找到狼牌是哪个，狼人也不会因为你刚在警上不退水认你是真预言家，因为你发错查杀这是个铁逻辑，并不可能是预言家。另一方面好人会怼死你，碰巧是个神牌还会逼神牌起跳，收益为负，这是搅乱场上局势，不如不炸。</p>
</blockquote>
</li>
<li><p>发到真正的好人金水，且状态像个预言家</p>
<blockquote>
<p>首先说发警下金水不构成诈身份退不了水，这个观念只能是55开：确实不构成诈身份，但是退水还是可以退的。在白狼王的局里，好人通过发查杀挡刀远不如发金水更容易迷惑狼人，因为发到正确查杀的概率是36.4%，而发到正确金水的概率是63.6%。</p>
</blockquote>
</li>
</ul>
<h4 id="So-Sad"><a href="#So-Sad" class="headerlink" title="So Sad"></a>So Sad</h4><p>当没有把握做到这两点，茫茫多的人漫不经心的跳预言家诈身份，且刚在警上不退水，最终的后果是什么呢。</p>
<blockquote>
<p>即使你想脱衣服，但是也晚了，因为茫茫多的好人起跳预言家，导致好人根本记不住所有人的警徽流，当然包括真预言家的警徽流，后边即使所有人都脱衣服，狼人两爆，预言家第二个验人拿到警徽都说不出来，等于好人自己把警徽吞了</p>
</blockquote>
<h3 id="跳女巫"><a href="#跳女巫" class="headerlink" title="跳女巫"></a>跳女巫</h3><p>我最开始看到这个套路来自pandakill。属于南派狼人杀的一种打法（即状态流）。不经意抛出盲毒的信息，去看被毒人的反应。</p>
<p>要求抿卦相一流，但是因人而异。</p>
<h4 id="有收益的诈身份-1"><a href="#有收益的诈身份-1" class="headerlink" title="有收益的诈身份"></a>有收益的诈身份</h4><ul>
<li>要盲毒一个警上后置位容易抿卦相的人，抿完了抓紧退水，警下再给分析。<blockquote>
<p>盲毒老油条，当然什么都看不出来。抓紧退水的原因是，防止狼人爆，来不及脱衣服，被女巫在夜里直接解决了，虽然一般局，女巫撒毒不会撒的这么随意。但是难保有义愤填膺的。</p>
</blockquote>
</li>
</ul>
<h3 id="跳猎人"><a href="#跳猎人" class="headerlink" title="跳猎人"></a>跳猎人</h3><p>这个套路，其实是蛮推荐的一个套路，人畜无害。并且这也是反制悍跳预言家的一个策略。从狼人的心里角度出发，当狼人发你查杀，且给了充足时间让他退水，不退水，跳一张钢板认出，对狼的心理压力很大。</p>
<h4 id="有收益的诈身份-2"><a href="#有收益的诈身份-2" class="headerlink" title="有收益的诈身份"></a>有收益的诈身份</h4><blockquote>
<p>开始你的表演，给他充足的退水时间，然后勇敢的起跳，表示认出，总之演技决定一切</p>
<p>风险：第一天被刀的是猎人，解释不清楚，有可能就把你带走了。</p>
</blockquote>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>当然任何套路被用的稀烂的时候，都不是好套路，所以当我说出这些的时候，可能我自己都不会选择这几种方式了。</p>
<p>除了找出谁是真正的狼人、成功伪装自己之外，发挥自己的思路，开发让人耳目一新的打法，也狼人杀的乐趣所在。</p>
]]></content>
      <categories>
        <category>Game</category>
      </categories>
      <tags>
        <tag>狼人杀</tag>
        <tag>桌游</tag>
      </tags>
  </entry>
  <entry>
    <title>狼人杀无纸化打法</title>
    <url>/2018/09/20/wolfkill-no-paper/</url>
    <content><![CDATA[<p>在桌游吧跟专业玩家或者是在公司跟同事玩狼人杀都会遇上拿着纸笔或者是手机认认真真记录发言票型的玩家，对于狼人杀这个信息爆炸的游戏，确实记录是有必要的。而下边我要说的则是一种虽然记忆力不是很好，但抛开纸笔、手机的一种玩法。对于聚会娱乐这种场景的狼人杀也不失为一个可选方案。</p>
<a id="more"></a>
<h1 id="逻辑"><a href="#逻辑" class="headerlink" title="逻辑"></a>逻辑</h1><p>狼人杀是一个判断逻辑、考验发言的游戏，在游戏过程中会产生大量的信息，对于信息的处理至关重要，同时发言对游戏走势又有着至关重要的影响。</p>
<h2 id="逻辑归一化"><a href="#逻辑归一化" class="headerlink" title="逻辑归一化"></a>逻辑归一化</h2><p>很多人喜欢准备一张纸，记录一些信息，因为信息量爆炸，很多时候记不住，这里提供一种无纸化狼人杀的打法。核心叫做逻辑归一化。</p>
<p>所谓的逻辑归一化，就是通过对信息的整理和判断，将每一轮的信息，最后汇总筛选，只记录最有用的信息。</p>
<p>这个过程需要两种方式来处理。剪枝和去重。</p>
<h3 id="剪枝"><a href="#剪枝" class="headerlink" title="剪枝"></a>剪枝</h3><p>剪枝指的是屏蔽掉无用信息。</p>
<p>例如对于一个认狼出局的发言，他的信息是具有迷惑性的，对于点出他的狼同伴。他可以打一个正逻辑，说出一些假的狼同伴；也可以打一个反逻辑，说出所有的真的狼同伴。而对于这个信息，是没有逻辑支点(逻辑支点的概念下边有说)的，所以这个信息是不具有参考价值的。</p>
<p>例如票型，对于每一轮的票型，全部都能记住是最好的，但是在没有纸张的情况下，则要选择最关心的几个人的票型，每一轮的发现都会有几个焦点人物出现，而焦点人物的票型就至关重要。</p>
<h3 id="去重"><a href="#去重" class="headerlink" title="去重"></a>去重</h3><p>去重指的是屏蔽跟风式发言，和重复性发言。</p>
<p>当第二个人重复一个信息的时候，其实对于第二个人来说，相当于没有发言。划水式发言等于直接说过，也没有必要记录。</p>
<h2 id="寻找逻辑支点"><a href="#寻找逻辑支点" class="headerlink" title="寻找逻辑支点"></a>寻找逻辑支点</h2><p>逻辑支点指的是支撑整套逻辑的事实依据。所谓的铁逻辑指的便是有一个强有力的逻辑支点制成的逻辑。</p>
<p>可以做成逻辑支点的有：</p>
<ul>
<li>票型</li>
<li>明显的个人习惯</li>
<li>事实矛盾</li>
</ul>
<p>(一言以蔽之，就是极大概率事件)</p>
<h2 id="逻辑与概率"><a href="#逻辑与概率" class="headerlink" title="逻辑与概率"></a>逻辑与概率</h2><p>狼人杀是一个软逻辑游戏，与之对应的是抵抗组织这种硬逻辑游戏，抵抗组织的好人必须出执行成功，而狼人杀对任何人都没有限制。软逻辑游戏，在很多情况下都没有什么必然。久而久之会把自己陷入一种困境，在正反逻辑盘开以后，发现怎么说都有理，对于现状分析不清。其实则是自己没有找到强有力的逻辑支点，知道游戏结束，找不到一个铁逻辑。</p>
<blockquote>
<p>就我个人举例：</p>
<p>第二天白天6号、7号上警跳预言家，两人发言都没有毛病，我12号因为在7号警徽流里，所以上票7号，但7号只吃到我一票，6号当选警长。</p>
<p>警下发言，我是站边7号，理由是，在两个预言家发言都很不错的情况下，狼人是没必要压手的，给队友上票总能找到充分的理由。</p>
<p>但是第二天我听了6号的发言，觉得6号发言的状态很好，跟狼人一起把7号真预言家投了出去</p>
<p>这里就犯了一个错误，以发言状态代替了票型这个逻辑支点，而发言状态是不能作为逻辑支点，支撑整个逻辑体系的，所以才会导致做了狼助。</p>
</blockquote>
<p>狼人杀是个概率游戏，但是人的性格是很难改变的，再加上狼人杀是一个多人游戏，所以搏大概率事件做逻辑支点，是保持胜利的比较容易的方式。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>结合逻辑归一化去处理信息，总能把茫茫多的信息汇总到一点或者几点，最终将这一点跟推出的逻辑支点相结合，确定场上的局面。</p>
<p>这个打法并不是十全十美。</p>
<p>因为剪枝和去重是一个局部性的处理，并没有考虑全局最优，这样就会有一定的局限性，如果恰好某个重要信息被屏蔽掉了，也再难以重新回头了。</p>
<p>因为将所有的信息绑定到一个逻辑支点，假如选错了逻辑支点，就会一愚到底，难以回头。</p>
<p>但是在无纸的情况下，不失为一种可选择的打法。</p>
]]></content>
      <categories>
        <category>Game</category>
      </categories>
      <tags>
        <tag>狼人杀</tag>
        <tag>桌游</tag>
      </tags>
  </entry>
  <entry>
    <title>狼人杀分享总结</title>
    <url>/2018/09/12/wolfkill-share/</url>
    <content><![CDATA[<p>在公司的时候，作为狼人杀协会的会长，兼鱼塘塘主，给萌新做过一次狼人杀分享会，同时也是对自己玩狼人杀的思路的梳理，记录下来，便于日后翻阅。</p>
<a id="more"></a>
<h1 id="狼人夜间手势"><a href="#狼人夜间手势" class="headerlink" title="狼人夜间手势"></a>狼人夜间手势</h1><ul>
<li>角色：<blockquote>
<p>预言家、女巫、白痴、猎人、守卫、丘比特、狼人、恶魔</p>
</blockquote>
</li>
<li>行动：<blockquote>
<p>悍跳、煽动、隐、倒钩、发金水、发查杀、上警、警下、自刀、下一夜、(恶魔)查验、自爆、推(砸)</p>
</blockquote>
<h1 id="狼人分工"><a href="#狼人分工" class="headerlink" title="狼人分工"></a>狼人分工</h1></li>
<li>悍跳狼(上警)：</li>
</ul>
<blockquote>
<p>对于发金水还是发查杀，要有清晰的认识。一般基于以下几点：</p>
<ul>
<li>目标是一张摇摆不定的牌，有机会骗到他，发金水拉票</li>
<li>目标已经明确被抿出是一张民牌，且发言一般般，发查杀做扛推</li>
<li>前置位对跳的真预言家博得了很多好人的信任，这个时候要考虑的是发到哪一张牌的验人力度是最大的，才能拉到票。</li>
</ul>
</blockquote>
<ul>
<li>冲锋狼、煽动狼(大概率上警)：</li>
</ul>
<blockquote>
<p>是要为悍跳狼号票的一张牌，可以说两个人是一条绳上的蚂蚱，任何一个人发言的失误，俩人都要炸。</p>
</blockquote>
<blockquote>
<p>如果是在警上，除了煽动随时都要准备起跳，也就是我们常说的那张补位狼牌。前置位悍跳狼为了博力度去发警上后置位金水恰好发到真预言家，这个时候如果没有队友救，就是白送一狼。作为补位狼牌要做的就是起跳预言家，给悍跳狼机会退水</p>
</blockquote>
<ul>
<li>隐狼(一般不上警)：</li>
</ul>
<blockquote>
<p>很多人觉得隐狼就是倒钩狼，这是非常错误的。隐狼的自由度是很大的，他要根据场上形势自己判断是倒钩还是冲锋。他的目的只有一个，别把自己打成焦点，一直狗到狼人获胜，好人先于他死光了，他的目的就达到了。</p>
</blockquote>
<ul>
<li>倒钩狼(上不上警看自己心情)：</li>
</ul>
<blockquote>
<p>倒钩狼就是传说中的卧底。倒钩狼的行动是倒钩，但是目的不是倒钩。他的目的是要让狼人获胜。对于场上的局势判断至少要分两部分：</p>
</blockquote>
<blockquote>
<ul>
<li>队友悍跳成功，博得了一部分好人的信任，可以跟真预言家55开，这个时候，倒钩狼牌就是想办法不动声色的(太爆炸自己就被好人干出局了)做低真预言家面，或者是在末置位，以一种巧妙地方式分了好人的票，帮助狼人冲票成功(所谓的阴阳倒钩狼)</li>
<li>队友悍跳失败，基本上大家已经准确站边，这个时候优先保证的是做好自己身份，博得好人的信任，甚至可以第一个点出队友的狼面，用巧妙地语言改掉预言家落在自己身上的警徽流。</li>
<li>在判断局势的同时，既然自己倒钩了真预言家，那狼坑就一定少一个，要准确的找到场上的晕民，把他们打进狼坑，做后边的扛推。</li>
</ul>
</blockquote>
<h1 id="逻辑推论常识与经验"><a href="#逻辑推论常识与经验" class="headerlink" title="逻辑推论常识与经验"></a>逻辑推论常识与经验</h1><ul>
<li>常识</li>
</ul>
<blockquote>
<ul>
<li>金水反水立警，身份最高</li>
<li>两个<strong>对立阵营</strong>打成的公共狼大概率是一张狼牌(倒钩狼和狼队友发出的公共狼可能是一个好人)；两个<strong>对立阵营</strong>公认的公共好人牌非常大概率是一张好人牌</li>
<li><strong>真预言家和悍跳狼</strong>发到的双查杀一定是一张狼人牌；<strong>真预言家和悍跳狼</strong>发到的双金水一定是一张金水牌。</li>
<li>预言家的两夜警徽流：1.验到一好人一狼人，警徽飞好人；2.两狼人警徽外置位飞；3.两好人警徽撕掉</li>
</ul>
</blockquote>
<ul>
<li>经验<blockquote>
<ul>
<li>村民:发言阳光，不被扛推是第一要义。在这个基础上最好能晚点再让狼人抿出闭眼民的身份；警上后置位接了金水之后可以反水立警刚一会看看对方的态度，诈一诈；一定要站边，因为站边之后你的尸体都会说话，不站边，死也是白死。只有在你明确的知道你压手是为了什么才压手，永远不要在你分不清要投给谁的时候压手。</li>
<li>女巫:1、管理好你的银水，如果你自己都定不了他是自刀狼还是真银水，不要指望其他人可以判断清楚；2、所以女巫开药的对象一定得是你能摸得准的，也就是说，救起来他你去听发言可以定他身份的。3、在解药用掉之后的晚上你摸不准守卫会不会守你，那一定要撒毒。如果你死了，药没撒出来，且没有遗言，保不齐后边狼人就会在末置位要被出的情况下跳一张女巫，然后冲一张好人牌，跟你这瓶药撒到好人身上没区别。</li>
<li>狼人：在夜里去打一层逻辑，白天发言去盘二层逻辑，或者反之。</li>
<li>如果你盘对跳的两个预言家盘狼坑都盘不齐，或者找出五六匹狼，那大概率有狼倒钩了</li>
</ul>
</blockquote>
</li>
</ul>
<h1 id="团队收益论"><a href="#团队收益论" class="headerlink" title="团队收益论"></a>团队收益论</h1><ol>
<li>团队最大</li>
<li>你的行为一定要为团队带来收益</li>
<li>是否算是为团队带来收益要从队友出发考虑(即你的做法会不会被队友认可)</li>
</ol>
]]></content>
      <categories>
        <category>Game</category>
      </categories>
      <tags>
        <tag>狼人杀</tag>
        <tag>桌游</tag>
      </tags>
  </entry>
  <entry>
    <title>【读书笔记】大教堂与集市-黑客圈简史</title>
    <url>/2018/09/05/book-djt-js-1/</url>
    <content><![CDATA[<p>研究计算机的开源文化，就不得不说起早期的极客(geek)，比起其他行业来说，计算机行业很幸运，一群牛逼的人开了一个好头，他们身上的特质，直接影响了整个行业，以及后来的计算机人。向他们致敬。</p>
<a id="more"></a>
<h1 id="黑客圈简史"><a href="#黑客圈简史" class="headerlink" title="黑客圈简史"></a>黑客圈简史</h1><ol>
<li><p>真程序员</p>
</li>
<li><p>早期的黑客</p>
<blockquote>
<p>计算机问世之后迅速涌现了一批编程爱好者，他们编写软件只是出于乐趣，慢慢形成了一些技术文化。这批人被称之为真程序员(Real Programmer)。他们通常具备了工程学和物理学的背景，并常常是业余无线电爱好者。在现在的眼光看来，应该叫做极客(geek)。在这些真程序员的带动下，形成了一种所谓的黑客(Hack)文化。初期的黑客文化主要是集中在大学的圈子里，不流俗，不走寻常路，极具创新精神。</p>
</blockquote>
</li>
<li><p>UNIX的兴起</p>
<blockquote>
<p>1969年，APRAnet(第一个高速互联网)、UNIX和C语言都诞生在这一年。UNIX和C的组合凭借着(KISS)哲学(Keep It Simple, Stupid)迅速风靡，马上就形成了一股新生力量，可以把这一力量叫做<strong>Unix/Usenet文化</strong>，与此相对应的就是之前的黑客文化，可以简要叫做<strong>ITS(不兼容分时系统)文化</strong>。1975年第一台个人电脑进入市场，1977年苹果公司成立，马上又有一拨人出现，他们用BASIC，可以把这批人称为<strong>没有组织的微机爱好者</strong>，不过这股力量遭到上面两种力量的共同鄙视。</p>
</blockquote>
</li>
<li><p>远古时代的终结</p>
<blockquote>
<p>三种文化其实代表了三个不同的技术体系。1980年，力量最强的是PDP-10文化，但是却也逐渐显得疲软，主要是人才在不断向其他文化转移，技术也略显过时。1983年ITS文化的依赖ITS被宣布不再继续研发，因为不可移植的东西是没有未来的。显然微型计算机是大势所趋，只有能跑在所有机器上的技术，才是王道。</p>
<p>就在这时，Richard M. Stallman(Emacs的发明者)坚决反对将实验室研究成果商业化。他离开了实验室，创建了自由软件基金会(FSF)。Steven Levy(《黑客》作者)称他为“最后一个真正的黑客”。</p>
<p>1982年RMS开始用C重新构建UNIX的克隆，并且免费发布，这就是大名鼎鼎的GNU(Gnu is Not Unix)这是一个递归式的缩写。此后GNU成为了所有黑客文化的焦点，ITS文化和Unix文化汇入了一起，准确的说是Unix文化将ITS精神融入了自己的圈子，此后的十多年，自由软件基金会统治了黑客文化的意识形态，RMS本人也便是黑客文化的最高精神领袖</p>
</blockquote>
</li>
<li><p>“专有UNIX”时代</p>
<blockquote>
<p>1984年，黑客文化的圈子分为了两个阵营，一部分是围绕着Internet和Usenet的网络派，还有一部分是不联网的微机爱好者。也正是这一阶段，出现了一些关于计算机的攻击事件，媒体将黑客这一词用来描述这些做坏事的人，一直沿用至今。<br>这个阶段Sun和其他公司生产的工作站级别的电脑出现，大家的关注点便放在了让其进行更高效的图形处理和更快捷的网络传输上。</p>
<p>在网络传输方面，Berkeley UNIX提供了对APRAnet协议的内置支持，解决了UUCP点到点连接慢带来的网络问题，促进了互联网的进一步发展;</p>
<p>而在图形处理方面，则出现了<strong>X Window</strong>。在若干尝试中，X Window脱颖而出，正是由于吸纳了十多家公司数百名员工的贡献，而关键在于开发者愿意遵守黑客盗的免费提供源码，而且通过互联网发布。，在几年间<strong>X Window</strong>战胜<strong>专有图像处理系统</strong>这件事深深地影响了整个UNIX</p>
<p>随着1990年最后一台ITS机器关机，ITS文化正式消失了，这部分力量放下立场，融入了UNIX文化。<br>紧接着就是UNIX的内战，最重要的两个便是Berkeley UNIX与AT&amp;T UNIX，代表了免费与商业的斗争。</p>
<p>20世纪90年代基于Intel 386芯片的PC出现了，MS-DOS和Mac的用户如雨后春笋般，然而并没有发展出什么带有自我意识的文化。与此同时商业的UNIX贵的一批且没有可移植性，而自由软件基金会也没能给出易用的可移植的UNIX。以至于在UNIX青黄不接之时，微软凭借Windows操作系统，抢占了市场份额。当然自此便经常可以看到媒体发声，“UNIX要玩完”的言论，直到现在依旧如此</p>
</blockquote>
</li>
<li><p>早期的自由UNIX</p>
<blockquote>
<p>1994年Berkeley UNIX研究组彻底失去了经费支持，看似UNIX是彻底玩完了。但是其实在人们关注之外，自由软件基金会一直未完成的可移植内核让一个大学生起了兴趣，这个人就是Linus Torvalds。</p>
<p>1991年，他开始使用FSF提供的套件为386开发自由Unix内核，他成功了，并且还吸引了很多黑客跟他一起做这件事情。这个产物就是Linux，全功能的Unix，免费而且代码完全公开，可以再发布。</p>
<p>Linux的开发成功，让人们得到了不一样的思考。以前的人们一直觉得对于如此复杂的系统必须要一个小而美的团队来做，团队要紧密互动，在Linux之前几乎所有的项目都是如此。而Linux从一开始就走了一条截然不同的路线，没有约束，各位合作者随意发挥，只有一个简单的约定，每周发布，得到用户反馈，并改之，让人惊讶的是这却工作的很好。一个很好的比喻便是<strong>物竞天择</strong>。</p>
<p>1993年底，Linux的稳定性已经和商业Unix不相上下了，加之商业Unix贵，很多人已不再使用商业Unix，导致很多商业Unix关门大吉。一些商业软件也已经开始考虑一直到Linux，不再支持Unix。至此黑客们对外界唱衰Unix便不再理会，开始用自己的观点重塑商业软件世界.</p>
</blockquote>
</li>
<li><p>WEB大爆发</p>
<blockquote>
<p>20世纪90年代，也就是Linux的早期，互联网也开始大行其道，变得便利，而黑客们的工作也就变成开发Linux，宣传互联网。Linux也借到了互联网便利的东风，迅速流传开来。当互联网成为主流，黑客群体便受到了尊重。也出现了一些政治影响。</p>
<ol>
<li>黑客们通过抗议阻止了一个强加密算法置于美国政府的控制下</li>
<li>黑客们通过发动同盟阻止了“通信合宜法(CDA)”。CDA指的是当时的美国为了避免色情内容流传于互联网影响青少年，提出监管网络内容。而这一做法遭到了美国公民自由联盟的起诉，理由是侵犯了公民言论自由的权利，CDA最终在1997年被废除。</li>
</ol>
</blockquote>
</li>
</ol>
<p><strong>至此黑客文化终于在这个世界上有了一席之地</strong></p>
]]></content>
      <categories>
        <category>Book</category>
      </categories>
      <tags>
        <tag>开源</tag>
        <tag>读书笔记</tag>
      </tags>
  </entry>
  <entry>
    <title>【Windows Programming】/MT、/MD 的区别</title>
    <url>/2018/08/26/windows-mt-md-diff/</url>
    <content><![CDATA[<p>Windows 编程中无法绕开的编译开关 <strong>/MT</strong>、<strong>/MD</strong> 其实还有一个 <strong>/LD</strong>，不过后者很少见。之前对这些一直没有一个清晰的概念，所以就查阅了相关资料。算是对这块有了一个粗浅的认识。</p>
<p>其实最直观的资料还属 <a href="https://docs.microsoft.com/en-us/cpp/build/reference/md-mt-ld-use-run-time-library?view=vs-2017" target="_blank" rel="noopener">MSDN</a></p>
<p><strong>/MT</strong>、<strong>/MD</strong> 其实一言以蔽之，就是运行时的库是静态链接还是动态链接。<strong>/MTd</strong>、<strong>/MDd</strong> 是他们的 <em>debug</em> 版本。所以他们的选择最直接的就是影响生成文件的大小。静态链接显然会大，动态链接则要求目标机器有需要的运行时库文件。其实我见过的大部分应用还是选择 <strong>/MD</strong> 比较多，因为在它们的安装目录下会看到 <em>MSVCRTXXX.dll</em> 。显然是带了运行时库的。</p>
<p><strong>/LD</strong> 是创建一个 DLL。如果以 <em>/LD</em> 编译，代码中没有写 DLL 入口点，编译器会自动插入一个入口点。值得一提的是这个编译开关默认是 <strong>/MT</strong>。 除非显式定义 <strong>/MD</strong>。</p>
]]></content>
      <categories>
        <category>Programming</category>
      </categories>
      <tags>
        <tag>Windows编程</tag>
        <tag>Runtime</tag>
      </tags>
  </entry>
  <entry>
    <title>【Windows Programming】浅谈 Thread</title>
    <url>/2018/08/21/windows-shallow-thread/</url>
    <content><![CDATA[<p>线程对于 Windows 编程人员来说，并不陌生，但是一直以来，我对它的了解也只是基本的使用层面。对于很多细节，也并不是很了解。这作为一个 Windows 客户端开发人员，可以说是非常尴尬了。所以，抽了一点时间，仔细梳理了一下线程相关的内容。顺便记录下来。</p>
<a id="more"></a>
<h2 id="一些常识"><a href="#一些常识" class="headerlink" title="一些常识"></a>一些常识</h2><ul>
<li>基本状态：就绪，执行，阻塞</li>
<li>堆公有、栈私有</li>
<li>创建和结束所需要的系统开销：小</li>
<li>没有自己的地址空间<h2 id="创建线程"><a href="#创建线程" class="headerlink" title="创建线程"></a>创建线程</h2>在 Windows 下创建一个线程，很自然的会想到<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">CreateThread(</span></pre></td></tr><tr><td class="code"><pre><span class="line">    _In_opt_ LPSECURITY_ATTRIBUTES lpThreadAttributes,</span></pre></td></tr><tr><td class="code"><pre><span class="line">    _In_ SIZE_T dwStackSize,</span></pre></td></tr><tr><td class="code"><pre><span class="line">    _In_ LPTHREAD_START_ROUTINE lpStartAddress,</span></pre></td></tr><tr><td class="code"><pre><span class="line">    _In_opt_ __drv_aliasesMem LPVOID lpParameter,</span></pre></td></tr><tr><td class="code"><pre><span class="line">    _In_ DWORD dwCreationFlags,</span></pre></td></tr><tr><td class="code"><pre><span class="line">    _Out_opt_ LPDWORD lpThreadId</span></pre></td></tr><tr><td class="code"><pre><span class="line">    );</span></pre></td></tr></table></figure>
这个方法可以说对 Windows 应用开发人员并不陌生。当使用这个方法的时候，在平时使用的时候，比较多关注的就是<code>lpStartAddress</code>、<code>lpParameter</code>。这是线程函数的入口以及参数。创建一个新线程之后，将会从这里开始执行。</li>
</ul>
<p>但是对于 C++ 来说，其实有另一个方法</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">_ACRTIMP <span class="keyword">uintptr_t</span> __cdecl _beginthreadex(</span></pre></td></tr><tr><td class="code"><pre><span class="line">    _In_opt_  <span class="keyword">void</span>*                    _Security,</span></pre></td></tr><tr><td class="code"><pre><span class="line">    _In_      <span class="keyword">unsigned</span>                 _StackSize,</span></pre></td></tr><tr><td class="code"><pre><span class="line">    _In_      _beginthreadex_proc_type _StartAddress,</span></pre></td></tr><tr><td class="code"><pre><span class="line">    _In_opt_  <span class="keyword">void</span>*                    _ArgList,</span></pre></td></tr><tr><td class="code"><pre><span class="line">    _In_      <span class="keyword">unsigned</span>                 _InitFlag,</span></pre></td></tr><tr><td class="code"><pre><span class="line">    _Out_opt_ <span class="keyword">unsigned</span>*                _ThrdAddr</span></pre></td></tr><tr><td class="code"><pre><span class="line">    );</span></pre></td></tr></table></figure>
<p>在这里，<code>_StartAddress</code>、<code>_ArgList</code>则跟上述那两个参数是类似的作用。<br>然而在这两个方法的选择中，《Windows 核心编程》早有公断。</p>
<p>根据作者的说法是选择<code>_beginthreadex</code>替代<code>CreateThread</code>。而原因则要从<code>_beginthreadex</code>的实现上说起。</p>
<p><code>_beginthreadex</code>在 Windows 下的实现也是调用了<code>CreateThread</code>，毕竟在 Windows 系统中，只认这一种创建线程的方式。但是在这之前，它还会做一些额外工作。创建一个线程数据块( tiddata )，然后将入口和参数都保存到数据块中，最后还要把数据块保存在 TLS 中。之后还要初始化一个 SEH 帧，用来处理运行时产生的错误。然后在线程结束之前，释放掉 tiddata 。那这样看，确实要比<code>CreateThread</code>多做一些事情。</p>
<p>话说回来，如果不做这些事情，当然就会有问题。比较直接的问题就是内存泄漏。原因是，如果使用<code>CreateThread</code>创建线程，当调用一些运行库函数的时候，会检查这个 tiddata 。如果发现没有，则会自己搞出一个，而这个在线程结束的时候，就不会被正确释放，就出现了内存泄漏。</p>
<p>类似<code>errno</code>这种运行库函数，需要反应正确的错误信息，如果不记录线程相关信息，则会在多线程的时候出现错误，所以一个 tiddata 是必要的，这也说明了为什么这个 tiddata 无论什么情况都会存在。</p>
<p>所以综上所述，在创建线程是，应该选择<code>_beginthreadex</code>。</p>
<p>关于更详细的<code>_beginthreadex</code>内容，参考 <a href="https://docs.microsoft.com/en-us/cpp/c-runtime-library/reference/beginthread-beginthreadex?view=vs-2017" target="_blank" rel="noopener">_beginthread, _beginthreadex</a> 这篇文章是最好了</p>
<h2 id="TLS"><a href="#TLS" class="headerlink" title="TLS"></a>TLS</h2><p>上边说的 TLS。可谓是线程中不可缺少的东西。因为线程之间是共享地址空间的，所以当有一些每个线程自己所需要的数据的时候，就不那么方便。而 TLS 就是用来解决这个问题。存储在 TLS 中的数据，对于每个线程之间，是互相隔离的。</p>
<h2 id="结束线程"><a href="#结束线程" class="headerlink" title="结束线程"></a>结束线程</h2><p>尽可能的让线程执行完自然结束。不到万不得已的时候，都不要使用<code>ExitThread</code>或者是<code>_endthreadex</code>。因为会使主调线程不正常返回，导致构造的 C++ 对象都不会析构；如果使用<code>ExitThread</code>还会造成 tiddata 不会被释放。</p>
<h2 id="后记"><a href="#后记" class="headerlink" title="后记"></a>后记</h2><p>关于多线程编程其实坑不算少，唯有对 Thread 多一些了解，才能写出更高质量的代码。</p>
]]></content>
      <categories>
        <category>Programming</category>
      </categories>
      <tags>
        <tag>Windows编程</tag>
        <tag>Thread</tag>
      </tags>
  </entry>
  <entry>
    <title>【Windows Programming】堆</title>
    <url>/2018/08/20/windows-heap/</url>
    <content><![CDATA[<p>提起堆，大部分人都不陌生，但是其实很多人也不见得就很了解。我见过的大部分人，对堆的理解其实还停留在，全局的一种内存，速度没有栈快，不会自动销毁，需要开发人员自己管理。这其实不怪 Windows，怪就怪面试人员水平参差不齐，五百年了，问堆还是，堆栈究竟有什么区别。然后在中国这个应试教育横行的地方，也必然是各种针对性的突击，问八百个人都是上边的答案。然而，对于 Windows 的堆，作为一个开发人员，这些了解显然是不够的。</p>
<a id="more"></a>
<p>其实想深入了解 Windows 中的堆，仅需要两篇文章，日常开发就够用了。</p>
<ul>
<li>Heap: Pleasures and Pains<blockquote>
<p><a href="https://docs.microsoft.com/en-us/previous-versions/ms810466(v=msdn.10)" target="_blank" rel="noopener">https://docs.microsoft.com/en-us/previous-versions/ms810466(v=msdn.10)</a></p>
</blockquote>
</li>
<li>Managing Heap Memory<blockquote>
<p><a href="https://docs.microsoft.com/en-us/previous-versions/ms810603(v=msdn.10)" target="_blank" rel="noopener">https://docs.microsoft.com/en-us/previous-versions/ms810603(v=msdn.10)</a></p>
</blockquote>
</li>
</ul>
<p>这两篇文章，说的还算详尽，至少基本的开发会清晰很多。</p>
<h2 id="堆的使用条件"><a href="#堆的使用条件" class="headerlink" title="堆的使用条件"></a>堆的使用条件</h2><ul>
<li>当程序需要的对象不能提前知晓的时候，也就是说需要在运行时动态分配的对象，要在堆上</li>
<li>栈上放不下的对象，要在堆上</li>
</ul>
<h2 id="堆的种类"><a href="#堆的种类" class="headerlink" title="堆的种类"></a>堆的种类</h2><p>这里要引用一段 MSDN 原文：</p>
<blockquote>
<p><strong>GlobalAlloc/GlobalFree</strong>: Heap calls that talk directly to the per-process default heap.</p>
<p><strong>LocalAlloc/LocalFree</strong>: Heap calls that talk directly to the per-process default heap.</p>
<p><strong>COM’s IMalloc allocator (or CoTaskMemAlloc / CoTaskMemFree)</strong>: Functions use the default per-process heap. Automation uses the Component Object Model (COM)’s allocator, and the requests use the per-process heap.</p>
<p><strong>C/C++ Run-time (CRT) allocator</strong>: Provides malloc() and free() as well as new and delete operators. Languages like Microsoft Visual Basic® and Java also offer new operators and use garbage collection instead of heaps. CRT creates its own private heap, which resides on top of the Windows heap.</p>
</blockquote>
<blockquote>
<p>Traditionally, the operating system and run-time libraries come with an implementation of the heap. At the beginning of a process, the OS creates a default heap called Process heap. ** The Process heap is used for allocating blocks if no other heap is used. Language run times also can create separate heaps within a process. (For example, C run time creates a heap of its own.) Besides these dedicated heaps, the application program or one of the many loaded dynamic-link libraries (DLLs) may create and use separate heaps. Windows offers a rich set API for creating and using private heaps.</p>
</blockquote>
<p>从这段描述上看：</p>
<ul>
<li>每个进程会有一个默认堆</li>
<li>C/C++ 运行时会有自己的私有堆。</li>
<li>进程中用到的模块，允许创建自己的私有堆。</li>
</ul>
<p>这就非常清晰了。这也就是传说中的<strong>一个模块一个堆</strong>。而关于堆的种类的认知是非常必要的，因为对于堆上的内存，要本着<strong>谁申请谁释放</strong>的原则，如果在模块的私有堆中申请的内存，拿到模块外由别人释放，就会引发崩溃，因为别人释放的时候会去自己的堆中找那部分内容，找不到就GG了。</p>
<p>而其实在 Windows 中关于堆分配器，其实是有前后端之分的。前端分配器维护一个固定大小的块列表，一个内存分配过来以后先在列表中找未被使用的块，如果找不到才会到后端分配器，新分配出一个块，并且后端分配器还会把这个操作提交到虚拟内存。因为有前后端分配器之分，所以性能问题肯定也会在这中间产生。一个显而易见的就是如果用到后端分配器的操作，必然会比只用前端分配器慢，所以解决这种性能问题还是尽量避免后端分配器操作。</p>
<h2 id="堆的性能问题"><a href="#堆的性能问题" class="headerlink" title="堆的性能问题"></a>堆的性能问题</h2><ul>
<li>内存分配<blockquote>
<p>内存分配导致的慢主要还是在于当前端分配器找不到可用块时，调用后端分配器，创建新块，以及跟虚拟内存的交互会有性能损耗</p>
</blockquote>
</li>
<li>内存释放<blockquote>
<p>内存释放导致的慢是由于释放内存会有一个块合并的操作，将空闲块合并到一起重组成一个大的空闲块，但是这中间会引发对内存的无序访问，导致缓冲命中失败和性能下降。</p>
</blockquote>
</li>
<li>堆竞争<blockquote>
<p>在多线程的情况，出现多个线程访问一个堆，需要有一个等待过程。而加锁，会引起线程的上下文切换也是性能下降的原因之一。</p>
</blockquote>
</li>
<li>堆破坏<blockquote>
<p>程序没有正确使用堆导致对破坏</p>
</blockquote>
</li>
<li>频繁的 alloc 和 realloc<blockquote>
<p>脚本语言容易发生，不过现在的脚本语言解释器都比较机智了，都会分配一块很大的内存自己用，来避免这个。</p>
</blockquote>
</li>
</ul>
<h2 id="提升堆性能的一般操作"><a href="#提升堆性能的一般操作" class="headerlink" title="提升堆性能的一般操作"></a>提升堆性能的一般操作</h2><ul>
<li>避免使用指针关联两个数据结构<blockquote>
<p>使用指针关联两个数据结构会导致对象的分配和释放被分离，产生额外开销。</p>
</blockquote>
</li>
<li>把孩子对象嵌入父亲对象。<blockquote>
<p>减少额外分配内存的次数。</p>
</blockquote>
</li>
<li>合并小对象组成一个大对象(聚合)<blockquote>
<p>可以减少被分配的块的数量来提升性能，关键是要找好聚合边界</p>
</blockquote>
</li>
<li>用 Buffer 满足 80% 的需求(二八原则)<blockquote>
<p>用内存 Buffer 存储字符串或者二进制数据，开一个能满足 80% 需求的大小的 Buffer 即可。剩下 20% 可以开一个新的 Buffer，然后持有指针即可。这样可以减少内存分配和释放，也可以减少数据空间，会提升性能</p>
</blockquote>
</li>
<li>成块分配内存对象<blockquote>
<p>小声BB(我个人理解就是指内存池)</p>
</blockquote>
</li>
<li>使用<code>_amblksiz</code><blockquote>
<p>C语言运行时(CRT)特有的前端分配器，可以用它跟后端分配器申请分配一个比较大的块，从而减少对后端分配器的请求。</p>
</blockquote>
</li>
</ul>
<h2 id="提升堆性能的进阶操作"><a href="#提升堆性能的进阶操作" class="headerlink" title="提升堆性能的进阶操作"></a>提升堆性能的进阶操作</h2><ul>
<li>使用 Windows Heap</li>
<li>使用内存池</li>
<li>使用 MP Heap。(一个多进程友好的包)</li>
<li>重新思考算法与数据结构</li>
</ul>
<h2 id="改善堆性能之前需要做的"><a href="#改善堆性能之前需要做的" class="headerlink" title="改善堆性能之前需要做的"></a>改善堆性能之前需要做的</h2><ol>
<li>评估代码中堆的使用方法</li>
<li>梳理代码，减少关于堆的调用，修复错误并调整数据结构</li>
<li>要对堆的性能消耗做具体评估</li>
</ol>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>很多人会认为这些过于底层，对一般开发用处不大，但其实对堆的深入了解，除了可以在程序性能上有一些更大的提升，对于一般开发则可以写出质量更高的代码。只有对操作系统的了解足够的深入，才能写出跟操作系统有着完美配合的代码，这看似是一种玄学，其实是一种科学。是基于 Windows 平台开发应用的开发人员与操作系统的开发人员的一种默契。</p>
]]></content>
      <categories>
        <category>Programming</category>
      </categories>
      <tags>
        <tag>Windows编程</tag>
        <tag>Heap</tag>
      </tags>
  </entry>
  <entry>
    <title>Qt5 中的 Json 模块与 JsonCpp 的对比</title>
    <url>/2018/08/12/qt5json-jsoncpp-diff/</url>
    <content><![CDATA[<p>工作中有幸做过关于 Qt5 Json 模块向 Qt4 的移植。做过有关 <em>Qt Json</em> 与 <em>JsonCpp</em> 的对比，并做过相关的兼容工作。所以着重研究了一下有关 Json 解析器的相关内容。注：大家常说的 <strong>QJson</strong> 其实并不是 Qt 中的模块，而是在 Qt4 没有 Json 模块的年代，一个非官方的第三方模块。对于现在 Qt 中的 Json 模块，官方称之为 <strong>Qt Json</strong>。</p>
<a id="more"></a>

<p>其实 Qt5 中的 Qt Json 模块的代码，写的可以说是严格按照 ECMA-404 协议的解析范本，没有任何自己画蛇添足的逻辑。而相比之下，JsonCpp 就相当随意了，作为一个非常有个性的解析器，是当之无愧了。以至于从 JsonCpp 换到 Qt Json 着实要做些兼容工作。</p>
<h2 id="二者的细节对比："><a href="#二者的细节对比：" class="headerlink" title="二者的细节对比："></a>二者的细节对比：</h2><h3 id="Qt-Json"><a href="#Qt-Json" class="headerlink" title="Qt Json"></a>Qt Json</h3><ol>
<li>不支持C/C++风格注释，解析失败</li>
<li>不支持0123456这种数值解析</li>
<li>不支持QJsonValue、QJsonDocument的直接比较大小(支持判断相等和不等)</li>
<li>允许设置默认值，不抛异常。没有设置默认值的，无法转换成功返回空值。</li>
<li>错误信息不可定位到具体行列</li>
<li>Json文本最大不能超过128MB(超过会爆DocumentTooLargeError)</li>
<li>最大不能超过1024层嵌套(超过会爆DeepNestingError)<br>(以上两个限制都是写死在代码的，可以改掉，但是改掉限制以后。是否有其他问题暂不清楚)</li>
<li>qDebug下支持直接输出 Qt Json 的相关对象</li>
</ol>
<h3 id="JsonCpp"><a href="#JsonCpp" class="headerlink" title="JsonCpp"></a>JsonCpp</h3><ol>
<li>支持C/C++风格注释，可以选择是否解析注释，可以允许设置和获取注释(然而在FastWriter的情况下不会输出注释)</li>
<li>支持0123456这种数值的解析</li>
<li>支持JsonValue的直接比较</li>
<li>不允许设置默认值，对于无法转换成功的，直接抛异常</li>
<li>错误信息可以定位到具体行列</li>
</ol>
<p>对于二者的效率，只用了 JsonCpp 项目中的测试用例跑过，结果证明 Qt Json 性能会好大概3-5倍。而且对于 Qt 项目来说， JsonCpp 那种解析错误就抛异常的，显然有点激进了，利用 Qt 的信号和槽就优雅了很多。但是由于 Qt 开发人员的懒惰，使用递归来进行解析，导致对 Json 数据的嵌套层数有要求，算是一个遗憾。不过客户端应用也够用了。</p>
<p>工作中对 JsonCpp 的兼容也只是兼容了 JsonCpp 的第1、2条。对 Qt Json 模块只要改 parse 部分。而 Qt Json 的 parse 是一个状态机，代码也通俗易懂，改起来还是不难的。另外一部分内容就是 Qt5 Json 向 Qt4 移植，这块的话，还是要稍微看一下源码，改动的话需要调整 Qt4 中的 QString 以及 QAtomicInt 中的一些原子操作，主要是一些琐碎细节，没啥特色，也就不表了。</p>
<p>很遗憾的是这两个库都只支持 DOM 解析</p>
<p>论专业性，还是 RapidJson 好一点，还支持 SAX 解析，而且在性能上会比较拔群。</p>
]]></content>
      <categories>
        <category>Programming</category>
      </categories>
      <tags>
        <tag>Qt</tag>
        <tag>源码分析</tag>
      </tags>
  </entry>
  <entry>
    <title>开发工具备忘</title>
    <url>/2018/08/12/develop-tool-archive/</url>
    <content><![CDATA[<p>工作时候用到的工具繁多，进行一波归档备忘，方便以后使用。</p>
<a id="more"></a>

<h1 id="效率工具"><a href="#效率工具" class="headerlink" title="效率工具"></a>效率工具</h1><ul>
<li><a href="http://www.voidtools.com/" target="_blank" rel="noopener">Everything</a> 搜索文件利器</li>
<li><a href="http://www.listary.com/" target="_blank" rel="noopener">Listary</a> 文件管理器与程序打开窗口联动</li>
<li><a href="http://cmder.net/" target="_blank" rel="noopener">Cmder</a> Windows 下最好用的命令行</li>
<li><a href="https://notepad-plus-plus.org/" target="_blank" rel="noopener">Notepad++</a> 文本文件编辑利器(x86版本插件比较多)</li>
<li><a href="http://www.funduc.com/search_replace.htm" target="_blank" rel="noopener">Search and Replace</a> 文件内容查找利器，查找速度秒杀一切 </li>
<li><a href="https://github.com/ehnap/gal" target="_blank" rel="noopener">Gal</a> 我自己写的快速启动工具</li>
<li><a href="http://www.ghisler.com/" target="_blank" rel="noopener">Total Commander</a> 处理文件比文件管理器好用</li>
<li><a href="https://www.scootersoftware.com/download.php" target="_blank" rel="noopener">Beyond Compare</a> 目录、文件内容对比工具，比 <em>git diff</em> 更专业</li>
<li><a href="https://windirstat.net/download.html" target="_blank" rel="noopener">WinDirStat</a> 文件夹大小扫描工具，速度飞快</li>
</ul>
<h1 id="开发工具"><a href="#开发工具" class="headerlink" title="开发工具"></a>开发工具</h1><ul>
<li><a href="https://www.ieinspector.com/" target="_blank" rel="noopener">HTTPAnalyzer</a> Windows下简单好用的抓包工具</li>
<li><a href="https://docs.microsoft.com/zh-cn/sysinternals/downloads/process-explorer" target="_blank" rel="noopener">Process Explorer</a> 进程观察利器</li>
<li><a href="https://docs.microsoft.com/zh-cn/sysinternals/downloads/procmon" target="_blank" rel="noopener">Process Monitor</a> 进程监视利器</li>
<li><a href="https://docs.microsoft.com/zh-cn/sysinternals/downloads/" target="_blank" rel="noopener">SysinternalsSuite</a> 微软出品的系统工具合集</li>
<li><a href="http://www.torchsoft.com/en/rw_information.html" target="_blank" rel="noopener">Registry Workshop</a> 加强版注册表，历史记录跳转功能超级好用</li>
<li><a href="https://www.apimonitor.com/" target="_blank" rel="noopener">API Monitor</a> 软件调用 Win32 API 捕捉利器</li>
<li><a href="https://oldj.github.io/SwitchHosts/" target="_blank" rel="noopener">SwitchHosts</a> Host 管理、切换工具</li>
<li><a href="https://git-scm.com/" target="_blank" rel="noopener">Git</a> 源代码管理工具</li>
<li><a href="https://www.sourcetreeapp.com/" target="_blank" rel="noopener">SourceTree</a> Git 的可视化客户端，观察文件变化比较方便</li>
<li><a href="http://www.tortoisegit.org/" target="_blank" rel="noopener">TortoiseGit</a> Git 的另一个可视化客户端，合分支比较方便</li>
<li><a href="http://www.hostbuf.com/" target="_blank" rel="noopener">FinalShell</a> SSH 工具，非常不错</li>
<li><a href="https://www.tightvnc.com/" target="_blank" rel="noopener">TightVNC</a> 免费的 VNC 软件</li>
</ul>
]]></content>
      <categories>
        <category>Programming</category>
      </categories>
      <tags>
        <tag>开发工具</tag>
      </tags>
  </entry>
  <entry>
    <title>【Qt源码笔记】关于 QTimer 在 Windows 下实现的杂谈</title>
    <url>/2018/08/05/qt-source-qtimer-achieve/</url>
    <content><![CDATA[<p>关于 QTimer 的具体实现，翻看源码源于一次面试经历。被问到 QTimer 的问题，我随口说了一句：Windows 平台下是用 Windows API 实现的，然后便引起了怀疑，不过我据理力争，便也作罢。所以回来之后，就又确认了一下。</p>
<a id="more"></a>
<p>要找这个佐证就必然要从<code>start()</code>方法顺藤摸瓜。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> QTimer::start()</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (id != INV_TIMER)    <span class="comment">// stop running timer</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="built_in">stop</span>();</span></pre></td></tr><tr><td class="code"><pre><span class="line">    nulltimer = (!inter &amp;&amp; single);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    id = QObject::startTimer(inter, Qt::TimerType(type));</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>
<p>事实证明还是走到了<code>QObject</code>中。继续顺藤摸瓜。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> QObject::startTimer(<span class="keyword">int</span> interval, Qt::TimerType timerType)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    Q_D(QObject);</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (Q_UNLIKELY(interval &lt; <span class="number">0</span>)) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        qWarning(<span class="string">"QObject::startTimer: Timers cannot have negative intervals"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (Q_UNLIKELY(!d-&gt;threadData-&gt;hasEventDispatcher())) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        qWarning(<span class="string">"QObject::startTimer: Timers can only be used with threads started with QThread"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (Q_UNLIKELY(thread() != QThread::currentThread())) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        qWarning(<span class="string">"QObject::startTimer: Timers cannot be started from another thread"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">int</span> timerId = d-&gt;threadData-&gt;eventDispatcher.load()-&gt;registerTimer(interval, timerType, <span class="keyword">this</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (!d-&gt;extraData)</span></pre></td></tr><tr><td class="code"><pre><span class="line">        d-&gt;extraData = <span class="keyword">new</span> QObjectPrivate::ExtraData;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    d-&gt;extraData-&gt;runningTimers.append(timerId);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> timerId;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>
<p>这里可以很清楚的看到<code>registerTimer()</code>方法。不过到这里就可以很快找到方法的位置。<code>eventDispatcher</code>让我直接找到 <strong>qeventdispatcher_win.cpp</strong>。果然在这中间，发现了想要找的东西。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> QEventDispatcherWin32Private::registerTimer(WinTimerInfo *t)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    Q_ASSERT(internalHwnd);</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    Q_Q(QEventDispatcherWin32);</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">bool</span> ok = <span class="literal">false</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    calculateNextTimeout(t, qt_msectime());</span></pre></td></tr><tr><td class="code"><pre><span class="line">    uint interval = t-&gt;interval;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (interval == <span class="number">0u</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="comment">// optimization for single-shot-zero-timer</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">        QCoreApplication::postEvent(q, <span class="keyword">new</span> QZeroTimerEvent(t-&gt;timerId));</span></pre></td></tr><tr><td class="code"><pre><span class="line">        ok = <span class="literal">true</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (interval &lt; <span class="number">20u</span> || t-&gt;timerType == Qt::PreciseTimer) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="comment">// 3/2016: Although MSDN states timeSetEvent() is deprecated, the function</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="comment">// is still deemed to be the most reliable precision timer.</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">        t-&gt;fastTimerId = timeSetEvent(interval, <span class="number">1</span>, qt_fast_timer_proc, DWORD_PTR(t),</span></pre></td></tr><tr><td class="code"><pre><span class="line">                                      TIME_CALLBACK_FUNCTION | TIME_PERIODIC | TIME_KILL_SYNCHRONOUS);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        ok = t-&gt;fastTimerId;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (!ok) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="comment">// user normal timers for (Very)CoarseTimers, or if no more multimedia timers available</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">        ok = SetTimer(internalHwnd, t-&gt;timerId, interval, <span class="number">0</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (!ok)</span></pre></td></tr><tr><td class="code"><pre><span class="line">        qErrnoWarning(<span class="string">"QEventDispatcherWin32::registerTimer: Failed to create a timer"</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>
<p>熟悉的 Windows API 就被发现了。如果当初我能记得 <code>QObject</code> 里边的调用细节，或许应该就在面试的让对方少些疑问了。</p>
<p>其实看到这里的逻辑，会发现一些 Qt 的小操作。</p>
<ul>
<li>一个间隔为 <strong>0</strong> 的定时器，Qt 只会发一个事件放到事件队列，不涉及系统 API 调用。</li>
<li>间隔 <strong>20ms</strong> 以下，会调用<code>timeSetEvent</code>。但是这里有两个隐患：1. <code>timeSetEvent</code>是一个已经废弃的 API；2. <code>timeSetEvent</code>虽然精度高，但是同一个进程开 <strong>16</strong> 个之后就会失败，这是一个致命伤，想避开这个问题，就要用最新的 <code>CreateTimerQueueTimer</code>，由此可见这里是偷懒了。</li>
<li>除此之外的计时器会调用 <code>SetTimer</code>。</li>
</ul>
<p>在这里，会很自然的想到，Timer 的设置是成对出现的，也就是<code>KillTimer</code> 、 <code>timeKillEvent</code>这种调用。</p>
<p>通过对 <code>QTimer</code> 机制的理解，不难想到目标代码应该从 <code>timeEvent</code> 查起。按图索骥，<code>stop()</code>。最终发现这个：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> QEventDispatcherWin32Private::unregisterTimer(WinTimerInfo *t)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (t-&gt;interval == <span class="number">0</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        QCoreApplicationPrivate::removePostedTimerEvent(t-&gt;dispatcher, t-&gt;timerId);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (t-&gt;fastTimerId != <span class="number">0</span>) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        timeKillEvent(t-&gt;fastTimerId);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        QCoreApplicationPrivate::removePostedTimerEvent(t-&gt;dispatcher, t-&gt;timerId);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (internalHwnd) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        KillTimer(internalHwnd, t-&gt;timerId);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    t-&gt;timerId = <span class="number">-1</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (!t-&gt;inTimerEvent)</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">delete</span> t;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>
<p>Qt 对 Windows API 的基本调用还是可信赖的，但是就是开发人员懒了一点。</p>
]]></content>
      <categories>
        <category>Programming</category>
      </categories>
      <tags>
        <tag>Qt</tag>
        <tag>源码分析</tag>
      </tags>
  </entry>
  <entry>
    <title>【Qt源码笔记】万般皆是int main</title>
    <url>/2018/07/28/qt-source-qtmain/</url>
    <content><![CDATA[<p>经常写 Qt 的程序，就会发现，不管是写控制台程序还是带窗体的应用程序，在 Qt 中的入口都是<code>int main()</code>。但实际上抛开其他平台不说，就是在 Windows 平台上，二者的入口就是有区别的。之前只是略知一点，今天翻看了一下代码，算是了解了一下。</p>
<a id="more"></a>

<p>其实这个探究过程倒也并不费劲。命令行程序暂且不表。就拿带窗体的应用程序来说，已知它的入口只能是<code>WinMain</code>、<code>wWinMain</code>、<code>_tWinMain</code>。不难按图索骥找到 <strong>qtmain_win.cpp</strong> 这个文件。事实上， 另一个关于 winrt 的入口定义也在同级目录下( <strong>qtbase\src\winmain</strong> )。 <strong>qtmain_win.cpp</strong> 文件内容如下:</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">/************************************************************************</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">#include "qt_windows.h"</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">#include "qbytearray.h"</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">#include "qstring.h"</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">#include "qvector.h"</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment"></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">#include &lt;shlobj.h&gt;</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment"></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">  This file contains the code in the qtmain library for Windows.</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">  qtmain contains the Windows startup code and is required for</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">  linking to the Qt DLL.</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment"></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">  When a Windows application starts, the WinMain function is</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">  invoked.</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">*/</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">QT_USE_NAMESPACE</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">if</span> defined(QT_NEEDS_QMAIN)</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">qMain</span><span class="params">(<span class="keyword">int</span>, <span class="keyword">char</span> **)</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> main qMain</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">else</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">extern</span> <span class="string">"C"</span> <span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span>, <span class="keyword">char</span> **)</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">  WinMain() - Initializes Windows and calls user's startup function main().</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">  <span class="doctag">NOTE:</span> WinMain() won't be called if the application was linked as a "console"</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">  application.</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">*/</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">// Convert a wchar_t to char string, equivalent to QString::toLocal8Bit()</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">// when passed CP_ACP.</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">inline</span> <span class="keyword">char</span> *<span class="title">wideToMulti</span><span class="params">(<span class="keyword">int</span> codePage, <span class="keyword">const</span> <span class="keyword">wchar_t</span> *aw)</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">const</span> <span class="keyword">int</span> required = WideCharToMultiByte(codePage, <span class="number">0</span>, aw, <span class="number">-1</span>, <span class="literal">NULL</span>, <span class="number">0</span>, <span class="literal">NULL</span>, <span class="literal">NULL</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">char</span> *result = <span class="keyword">new</span> <span class="keyword">char</span>[required];</span></pre></td></tr><tr><td class="code"><pre><span class="line">    WideCharToMultiByte(codePage, <span class="number">0</span>, aw, <span class="number">-1</span>, result, required, <span class="literal">NULL</span>, <span class="literal">NULL</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> result;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">extern</span> <span class="string">"C"</span> <span class="function"><span class="keyword">int</span> APIENTRY <span class="title">WinMain</span><span class="params">(HINSTANCE, HINSTANCE, LPSTR <span class="comment">/*cmdParamarg*/</span>, <span class="keyword">int</span> <span class="comment">/* cmdShow */</span>)</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">int</span> argc;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">wchar_t</span> **argvW = CommandLineToArgvW(GetCommandLineW(), &amp;argc);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (!argvW)</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">char</span> **argv = <span class="keyword">new</span> <span class="keyword">char</span> *[argc + <span class="number">1</span>];</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; argc; ++i)</span></pre></td></tr><tr><td class="code"><pre><span class="line">        argv[i] = wideToMulti(CP_ACP, argvW[i]);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    argv[argc] = <span class="literal">nullptr</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    LocalFree(argvW);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">const</span> <span class="keyword">int</span> exitCode = main(argc, argv);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; argc &amp;&amp; argv[i]; ++i)</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">delete</span> [] argv[i];</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">delete</span> [] argv;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> exitCode;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>这个文件中不难看出，我在自己的工程中使用的 <code>int main()</code> 其实就是 <code>const int exitCode = main(argc, argv);</code> 这一行中的 <code>main</code> 了。 找到了案发现场，转而想到了一个问题，这个文件是如何应用在我的工程中的。</p>
<p>翻看目录时候 <strong>winmain.pro</strong> 引起了我的注意，根据它的内容不难发现，这个目录在 Windows 下编译会生成 <strong>qtmain.lib</strong> 。机智的我直接去找项目工程文件( .vcxproj )。查看他的内容，一切都真相大白。在 link 部分，会发现 <strong>qtmain.lib</strong> 文件会被链接到 exe 中。而这一步的操作，应该就是 VS 中 Qt 插件的功劳了。</p>
<p>所以由此可推断，如果用 VS 裸写 Qt 的程序，在链接的时候除了链接必要的 Qt 库文件，还要自己手动把这个 <strong>qtmain.lib</strong> 链接进去。</p>
]]></content>
      <categories>
        <category>Programming</category>
      </categories>
      <tags>
        <tag>Qt</tag>
        <tag>源码分析</tag>
      </tags>
  </entry>
  <entry>
    <title>【Windows Programming】字符编码</title>
    <url>/2018/07/09/windows-character-encoding/</url>
    <content><![CDATA[<p>经常在写代码的时候需要处理宽字符，ASCII 字符，在代码中看到 wchar、char 等等。一般都是处理一个方法的时候发现需要的是某字符串，然后这边有什么字符串，之后查一个转换方法。还有对于 Unicode 、ANSI 这些不太分得清，所以花了一点时间看了一看。做个小结。</p>
<a id="more"></a>
<h1 id="编码介绍"><a href="#编码介绍" class="headerlink" title="编码介绍"></a>编码介绍</h1><h2 id="ANSI"><a href="#ANSI" class="headerlink" title="ANSI"></a>ANSI</h2><p>ANSI(American National Standards Institute) 其实并不算是一种固定编码，可以理解为在不同国家，有着不同的解释。例如在中国大陆，ANSI 编码指的就是 GBK 编码，在台湾地区指的是 BIG5 编码。所以一个场景下这种编码是有问题的，比方说一个保存为 ANSI 编码的文件，在不同区域的系统下，用记事本打开就会有问题，因为对文本的解释是不同的。例如在中国的系统上保存，其实就是 GBK 编码，然后在美国的系统上打开，会被当做 ASCII 编码来解释，就会出现问题。看不到想要的内容。(注：所以《 Windows 核心编程(第五版)》(下称《核心编程》)2.1节作者说到：<code>调用 strlen 会返回“以 0 结尾的一个 ANSI 单字节字符数组”中的字符数</code>，这个表述是不准确的，之所以这么说是因为作者所在的国家显然是 ASCII 编码，但是拿到中文这里说就不恰当，可以说是作者的锅也可以说是译者的锅。所以此书第二章所有讲到 ANSI，都可以理解为 ASCII 编码)</p>
<h2 id="Unicode"><a href="#Unicode" class="headerlink" title="Unicode"></a>Unicode</h2><p>Unicode 标准(使用多字符编码)解决了 ASCII 编码这种单字符编码无法表示一些包含特别多字符的问题。官方的一段解释<code>The Unicode Standard provides a unique number for every character, no matter what platform, device, application or language.</code>，其实就是把每个字符作为一个具体数字 。对于 Unicode 标准，存在多种编码，例如：<code>UTF-8</code> 编码，<code>UTF-16</code> 编码等等。UTF(Unicode Transformation Format)，指的是 Unicode 转换格式。</p>
<h3 id="UTF-8"><a href="#UTF-8" class="headerlink" title="UTF-8"></a>UTF-8</h3><p>以下引用《核心编程》原文：</p>
<blockquote>
<p>UTF-8 将一些字符编码为 1 个字节(可以说就是那些 ASCII 字符)，一些字符编码为 2 个字节，一些字符编码为 3 个字节，一些字符编码为 4 个字节。根据 Unicode 的数字不同来区分应该编码为几个字节，属于<code>变长</code>字节编码。这样的好处是显而易见的，就是节省空间，坏处也是显而易见的，处理一些字符编码比较复杂的文本，显然效率会差，至少要不断判断是几个字节，计算长度就比较麻烦。</p>
</blockquote>
<h3 id="UTF-16"><a href="#UTF-16" class="headerlink" title="UTF-16"></a>UTF-16</h3><p>UTF-16 就比较鸡贼了，如果细说就要扯到辅助平面和基本文字平面了，感觉意义都不是很大。简单理解就是一般字符(文字基本都是这个范畴)编码为 2 个字节，不一般的编码为 4 个字节(也就是 2 个 2 字节)。关于 UTF-16 连《核心编程》都没说，可见作者也是非常鸡贼了。</p>
<h3 id="UTF-32"><a href="#UTF-32" class="headerlink" title="UTF-32"></a>UTF-32</h3><p>UTF-32 这个算是最省事了，把 Unicode 值用 32 位无符号整数表示就得到了 UTF-32 的编码了。缺点也是显而易见的，贼占地方。</p>
<h3 id="BOM头"><a href="#BOM头" class="headerlink" title="BOM头"></a>BOM头</h3><p>经常在 Code Page 中看到带 BOM 头和不带 BOM 头。这个跟编码的大小端有关。对于这种多个字节的编码存在一个大小端的问题。如何来区分编码的大小端。Unicode 标准推荐使用一个 BOM(Byte Order Mark)来做区分。BOM 的字符编码是0xFEFF，这个叫做零宽无中断字符，这也解释了为什么你在文件里边去掉和添加 BOM 头都不会影响排版。所以 BOM 头的存在可以帮助判断文本的编码的大小端，如果没有 BOM 头的文本，在跨系统使用的时候，编辑器的实现可以做出两种做法：1. 会根据系统是大小端强行解释，这样的问题是一旦两个系统不一致，看到的内容也就完全不对了；2. 根据里边的数据，做一个判断，因为当大端被解释成小端有可能会出现 Unicode 中不存在的字符(如 BOM 头这个字符，0xFEFF存在，0xFFEF不存在)。在我看来显然应该是第一种做法。具体理由按下不表了。</p>
<h1 id="数据类型"><a href="#数据类型" class="headerlink" title="数据类型"></a>数据类型</h1><h2 id="char"><a href="#char" class="headerlink" title="char"></a>char</h2><p>1 个字节(8 bit)。用来表示 ASCII 编码。</p>
<h2 id="wchar-t"><a href="#wchar-t" class="headerlink" title="wchar_t"></a>wchar_t</h2><p>2 个字节(16 bit)。用来表示 Unicode 字符(UTF-16)。当写出<code>wchar_t c = L&#39;A&#39;;</code>这行代码的时候，编译器会把<code>L</code>后边的东西用 UTF-16 来编码。值得一提的是<code>wchar_t</code>早期的 Microsoft 编译器并不支持。在那个上古时期有这样一个定义<code>typedef unsigned short wchar_t</code>。后来支持以后，编译器搞了一个编译开关<code>/Zc:wchar_t</code>，有这个的才在编译器定义这个数据类型，现在新建项目的时候会默认开启了。</p>
<h2 id="CHAR、WCHAR"><a href="#CHAR、WCHAR" class="headerlink" title="CHAR、WCHAR"></a>CHAR、WCHAR</h2><p>按照《核心编程》的说法：</p>
<blockquote>
<p>为了与 C 语言稍微有一些区分，Windows 开发团队希望定义自己的数据类型。</p>
</blockquote>
<ul>
<li>CHAR：<code>typedef char CHAR</code></li>
<li>WCHAR: <code>typedef wchar_t WCHAR</code></li>
<li>指针：<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Pointer to 8-bit character(s)</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> CHAR *PCHAR;</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> CHAR *PSTR;</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> CONST CHAR *PCSTR</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">// Pointer to 16-bit character(s)</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> WCHAR *PWCHAR;</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> WCHAR *PWSTR;</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> CONST WCHAR *PCWSTR</span></pre></td></tr></table></figure>

</li>
</ul>
<h2 id="TCHAR"><a href="#TCHAR" class="headerlink" title="TCHAR"></a>TCHAR</h2><p><code>TCHAR c = TEXT(&#39;A&#39;)</code>。这个可以理解为万能类型，之所以这么说，可以看一下它的定义</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span> UNICODE</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> WCHAR TCHAR, *PTCHAR, PTSTR;</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> CONST WCHAR *PCTSTR;</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> __TEXT(quote) L##quote</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">else</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> CHAR TCHAR, *PTCHAR, PTSTR;</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> CONST CHAR *PCTSTR;</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> __TEXT(quote) quote</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> TEXT(quote) __TEXT(quote)</span></span></pre></td></tr></table></figure>
<p>所以看你的项目是否定义了 UNICODE 宏来决定 TCHAR 的类型，当然这个 UNICODE 宏还会影响 Windows API 调用函数版本的选择，后边细说。所以会看到大批文章告诉你解决什么编不过的问题都直接让你用 TCHAR 和 TEXT()。但我觉得并没有太大意义，至少我暂时想不到需要这两个版本都支持的场景。项目使用哪种数据类型明确一点会比较好，会影响到效率，后边细说。</p>
<h1 id="函数"><a href="#函数" class="headerlink" title="函数"></a>函数</h1><p>对于 Windows API 微软都会提供两个版本的例如 <code>CreateWindowExW</code>、<code>CreateWindowExA</code>，一个是宽字符版本，一个是单字符版本。当然如果你用<code>CreateWindowEx</code>，你会发现再配合 TCHAR 这套，显然也可以正常使用。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span> UNICODE</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CreateWindowEx CreateWindowExW</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="meta">#eles</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CreateWindowEx CreateWindowExA</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span></pre></td></tr></table></figure>
<p>就是因为这个缘故。所以上边我说会影响到函数版本的选择。而效率问题，在 Windows Vista 上(当然可以理解为之后的版本也都如此) A 版本的函数其实只是一个转换层，将传入的 ASCII 字符转换成 Unicode 字符，然后调用 W 版本。所以这中间会有一个分配内存的过程，显然会有一个效率上的问题。所以其实现在写代码，非常推荐统一使用宽字符版本。</p>
<p>另外除了 Windows API 之外，C 运行库，也有类似的操作。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span> _UNICODE</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> _tcslen wcslen</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="meta">#eles</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> _tcslen strlen</span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span></pre></td></tr></table></figure>
<p>只不过使用的是 _UNICODE 宏。所以不想让工程出现编码的混乱，显然 UNICODE、_UNICODE 是要成对出现的。事实上，现在用 Visual Studio 新建工程的时候，默认这两个都会定义上的。</p>
<h1 id="跨平台的坑"><a href="#跨平台的坑" class="headerlink" title="跨平台的坑"></a>跨平台的坑</h1><p>对于 wchar_t 在 Windows 平台是 UTF-16 编码，是 2 个字节的长度。而在 Linux 上是 4 个字节的长度，GCC 编译的时候会用 UTF-32 编码。这里边就会有一个不一致。要考虑编码转换问题。</p>
<h1 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h1><p>至此编程中需要的编码，大致了解清楚了。Windows 编程中，除非有特殊需要，否则一律使用宽字符是最好的选择。编码则选择 UTF-16 编码。</p>
]]></content>
      <categories>
        <category>Programming</category>
      </categories>
      <tags>
        <tag>字符编码</tag>
        <tag>Windows编程</tag>
      </tags>
  </entry>
  <entry>
    <title>面试问题整理</title>
    <url>/2018/06/08/job-interview-summary/</url>
    <content><![CDATA[<p>最近一段时间整理了一些我遇到的面试题，各个方面都有，写在这里，做个记录。大厂偏好算法和数据结构，小厂偏好项目经验。这算是一个比较鲜明的特点了。</p>
<a id="more"></a>
<h2 id="C"><a href="#C" class="headerlink" title="C++"></a>C++</h2><ul>
<li>static 的作用<blockquote>
<ol>
<li>static的最主要功能是隐藏，</li>
<li>其次因为static变量存放在静态存储区，所以它具备持久性和默认值0. </li>
<li>用于类成员声明，所有实例共享。</li>
</ol>
<p><a href="https://www.cnblogs.com/songdanzju/p/7422380.html" target="_blank" rel="noopener">C++中static关键字作用总结</a></p>
</blockquote>
</li>
<li>extern “C” 的作用<blockquote>
<p>extern “C”的主要作用就是为了能够正确实现C++代码调用其他C语言代码。加上extern “C”后，会指示编译器这部分代码按C语言的进行编译，而不是C++的。由于C++支持函数重载，因此编译器编译函数的过程中会将函数的参数类型也加到编译后的代码中，而不仅仅是函数名；而C语言并不支持函数重载，因此编译C语言代码的函数时不会带上函数的参数类型，一般之包括函数名。</p>
<p><a href="https://blog.csdn.net/jiqiren007/article/details/5933599" target="_blank" rel="noopener">extern “C”作用详解</a></p>
</blockquote>
</li>
<li>sizeof(char)、sizeof(char*)、sizeof(int)、sizeof(int*)<blockquote>
<p>1、4 or 8、4、4 or 8</p>
<p><a href="https://blog.csdn.net/xunfeng13/article/details/51011509" target="_blank" rel="noopener">C/C++中sizeof()的用法</a></p>
</blockquote>
</li>
<li>指针是多少字节由什么决定<blockquote>
<p>程序位数决定</p>
</blockquote>
</li>
</ul>
<h2 id="STL"><a href="#STL" class="headerlink" title="STL"></a>STL</h2><ul>
<li>shared_ptr 如何解决循环引用<blockquote>
<p>使用weak_ptr</p>
<p><a href="https://www.cnblogs.com/diegodu/p/6370736.html" target="_blank" rel="noopener">std::shared_ptr 和 std::weak_ptr的用法以及引用计数的循环引用问题</a></p>
</blockquote>
</li>
<li>Vector、List、Queue分别在什么情况下用<blockquote>
<ul>
<li>查找操作使用较多，使用Vector</li>
<li>增删操作使用较多，使用List</li>
<li>先进先出的使用场景，使用Queue</li>
</ul>
</blockquote>
</li>
</ul>
<h2 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h2><ul>
<li>完全二叉树是什么<blockquote>
<p>完全二叉树：倒数第二层是满二叉树，最后一层靠左对齐</p>
</blockquote>
</li>
<li>满二叉树是什么<blockquote>
<p>满二叉树：结点要么是叶子结点，要么它有两个孩子结点。</p>
</blockquote>
</li>
</ul>
<h2 id="操作系统"><a href="#操作系统" class="headerlink" title="操作系统"></a>操作系统</h2><ul>
<li>进程与线程分别都是什么<blockquote>
<p>进程：程序在执行过程中分配和管理资源的基本单位。每一个进程都有一个自己的地址空间，即进程空间或（虚空间）。(资源独立，创建销毁代价大)<br>线程：CPU独立运行和独立调度的基本单位。(堆公有，栈私有，创建销毁代价小)</p>
</blockquote>
</li>
<li>进程与线程能否多对多<blockquote>
<p>可以多对多。1. 地址空间映射；2. 进程间通讯。 做好这两部分。(云计算比较有用)</p>
</blockquote>
</li>
<li>线程切换上下文需要保存什么信息<blockquote>
<ul>
<li>CONTEXT结构中保存着特定于处理器寄存器的数据。系统使用CONTEXT结构执行各种内部操作。参考WinNT.h</li>
<li>见《Windows核心编程》P174</li>
</ul>
</blockquote>
</li>
<li>线程安全与可重入分别都是什么<blockquote>
<ul>
<li>线程安全：某个方法，在多线程下调用，跟顺序执行一致，即安全的，就叫做线程安全方法</li>
<li>可重入：某个方法，执行过程中，中断，然后继续执行，结果不受影响，即方法可重入</li>
<li>二者没什么概念上的必然联系。一个方法是可重入的，不一定就是线程安全的；一个方法是线程安全的，不一定是可重入的</li>
</ul>
</blockquote>
</li>
<li>给两把锁、两个线程如何构造死锁<blockquote>
<p>线程T1、T2；锁L1、L2。T1获取L1，等待L2，获取L2，释放L2，释放L1；T2获取L2，等待L1，获取L1，释放L1，释放L2。</p>
<p><a href="https://blog.csdn.net/u010915032/article/details/52060232" target="_blank" rel="noopener">多线程死锁例子</a></p>
</blockquote>
</li>
<li>同步异步和阻塞非阻塞的区别</li>
<li>A线程while循环，B线程打印，先后开启A、B线程，能否输出内容</li>
</ul>
<h2 id="计算机网络"><a href="#计算机网络" class="headerlink" title="计算机网络"></a>计算机网络</h2><ul>
<li>TCP 三次握手<blockquote>
<ul>
<li>A -&gt; B  SYN</li>
<li>B -&gt; A  SYN+ACK</li>
<li>A -&gt; B  ACK</li>
</ul>
</blockquote>
</li>
</ul>
<h2 id="数据库"><a href="#数据库" class="headerlink" title="数据库"></a>数据库</h2><ul>
<li>什么是主键和外键<blockquote>
<ul>
<li>主键：数据库表中对储存数据对象予以唯一和完整标识的数据列或属性的组合。一个数据列只能有一个主键，且主键的取值不能缺失，即不能为空值（Null）。</li>
<li>外键：在一个表中存在的另一个表的主键称此表的外键。</li>
</ul>
</blockquote>
</li>
<li>DROP、DELETE、TRUNCATE的用途<blockquote>
<ul>
<li>DELETE是DML，可以回滚，需要显式提交，删除单条数据。</li>
<li>DROP是DDL，隐式提交，不能回滚，不触发触发器。删除表结构。</li>
<li>TRUNCATE是DDL， 隐式提交，不能回滚，不触发触发器。清空表内容。</li>
</ul>
<p><a href="https://www.cnblogs.com/zhizhao/p/7825469.html" target="_blank" rel="noopener">drop、truncate和delete的区别</a></p>
</blockquote>
</li>
</ul>
]]></content>
      <tags>
        <tag>随笔</tag>
      </tags>
  </entry>
  <entry>
    <title>阿里云(CentOS7.4、专有网络)配置shadowsocks-server</title>
    <url>/2017/11/12/centos7-shadowsocks/</url>
    <content><![CDATA[<p>双十一在阿里云剁了一个服务器，随便搞搞，找了一些教程，搭了个梯子。</p>
<a id="more"></a>

<h3 id="安装Python、pip"><a href="#安装Python、pip" class="headerlink" title="安装Python、pip"></a>安装Python、pip</h3><p>这一步在阿里云的预装系统上，不需要，系统带了</p>
<h3 id="安装配置shadowsocks"><a href="#安装配置shadowsocks" class="headerlink" title="安装配置shadowsocks"></a>安装配置shadowsocks</h3><p>安装 shadowsocks：</p>
<blockquote>
<p>$ pip install shadowsocks</p>
</blockquote>
<p>安装完成后，创建配置文件，路径可以自选，自己能找到就行</p>
<p><code>/etc/shadowsocks/config.json</code></p>
<blockquote>
<p>{<br>​    “server”:”0.0.0.0”,<br>​    “local_address”:”阿里云公网ip”,<br>​    “local_port”:”15645”，<br>​    “port_password”:{<br>​         “8838”:”password”,<br>​         “8839”:”passworda”<br>​     }<br>​     “timeout”:300,<br>​     “method”:”aes-256-cfb”,<br>​     “fast_open”:false<br>}</p>
</blockquote>
<p><code>port_password</code>配置客户端连接的时候访问端口，和需要的密码</p>
<p><code>method</code>配置加密方法</p>
<p><code>server</code>不要写阿里云的公网ip，这样启动的时候会报连接不上加载不起来</p>
<p><code>local</code>的两项目测配置server不是必须的</p>
<h3 id="配置自启动"><a href="#配置自启动" class="headerlink" title="配置自启动"></a>配置自启动</h3><p>创建启动脚本<code>/etc/systemd/system/shadowsocks.service</code>:</p>
<blockquote>
<p>[Unit]</p>
<p>Description=Shadowsocks</p>
<p>[Service]<br>TimeoutStartSec=0<br>ExecStart=/usr/bin/ssserver -c /etc/shadowsocks/config.json<br>ExecReload=/bin/kill -HUP $MAINPID<br>ExecStop=/bin/kill -s QUIT $MAINPID<br>PrivateTmp=true<br>KillMode=process<br>Restart=on-failure<br>RestartSec=5s</p>
<p>[Install]<br>WantedBy=multi-user.target</p>
</blockquote>
<p>这个时候要让防火墙对刚才配置的端口放行</p>
<blockquote>
<p>查看状态：$ systemctl status firewalld<br>启动：$ systemctl start firewalld</p>
</blockquote>
<blockquote>
<p>$ firewall-cmd –zone=public –add-port=8838/tcp –permanent<br>$ firewall-cmd –zone=public –add-port=8839/tcp –permanent</p>
</blockquote>
<p>将<code>8838</code>、<code>8839</code>两个端口放行。阿里云还有个安全组，在阿里云的控制台可以找到，然后添加规则将上边两个端口放行(授权对象写0.0.0.0/0就好)</p>
<p>执行以下命令启动 shadowsocks 服务：</p>
<blockquote>
<p>$ systemctl enable shadowsocks.service<br>$ systemctl start shadowsocks.service</p>
</blockquote>
<p>查看启动状态：</p>
<blockquote>
<p>$ systemctl status shadowsocks.service -l</p>
</blockquote>
<p>如果发现失败了，这里的报错信息是不全的</p>
<p>可以手动启动ss，看一下是什么错误</p>
<p>以指定配置启动ss ：</p>
<blockquote>
<p>$ ssserver -c /etc/shadowsocks/config.json</p>
</blockquote>
]]></content>
      <categories>
        <category>Programming</category>
      </categories>
      <tags>
        <tag>ss</tag>
      </tags>
  </entry>
  <entry>
    <title>AVL树(自平衡二叉树)</title>
    <url>/2017/11/06/avl-tree/</url>
    <content><![CDATA[<h2 id="特点"><a href="#特点" class="headerlink" title="特点"></a>特点</h2><ul>
<li>二叉树</li>
<li>同节点左右子树高度差不超过1<a id="more"></a>
<h2 id="复杂度"><a href="#复杂度" class="headerlink" title="复杂度"></a>复杂度</h2></li>
<li>插入、查找、删除均为O(logN)</li>
</ul>
<h2 id="节点数"><a href="#节点数" class="headerlink" title="节点数"></a>节点数</h2><ul>
<li>最多(满二叉树) <code>2^h-1</code></li>
<li>最少 <code>2^(h-1)</code></li>
</ul>
<h2 id="规则"><a href="#规则" class="headerlink" title="规则"></a>规则</h2><h3 id="旋转："><a href="#旋转：" class="headerlink" title="旋转："></a>旋转：</h3><ul>
<li>左旋：节点的左旋，节点的右孩子指针指向节点右孩子的左孩子，节点的右孩子的左孩子指针指向节点。</li>
<li>右旋：节点的右旋，节点的左孩子指针指向节点左孩子的右孩子，节点的左孩子的右孩子指针指向节点。</li>
</ul>
<h3 id="平衡因子："><a href="#平衡因子：" class="headerlink" title="平衡因子："></a>平衡因子：</h3><ul>
<li>平衡因子=左子树高度-右子树高度</li>
</ul>
<p><strong>导致AVL Tree不平衡的几种类型及调整方法:</strong></p>
<h3 id="插入："><a href="#插入：" class="headerlink" title="插入："></a>插入：</h3><ul>
<li>LL：节点的左(L)子树的左(L)子树因为存在非空子节点，导致与节点的右子树高度差超过1 <strong>(右旋)</strong></li>
<li>LR：节点的左(L)子树的右(R)子树因为存在非空子节点，导致与节点的右子树高度差超过1 <strong>(先左旋再右旋)</strong></li>
<li>RL：节点的右(R)子树的左(L)子树因为存在非空子节点，导致与节点的左子树高度差超过1 <strong>(先右旋再左旋)</strong></li>
<li>RR：节点的右(R)子树的右(R)子树因为存在非空子节点，导致与节点的左子树高度差超过1 <strong>(左旋)</strong></li>
</ul>
<h3 id="删除："><a href="#删除：" class="headerlink" title="删除："></a>删除：</h3><ul>
<li>删除叶子结点，删除之后判断一下是否平衡</li>
<li>删除只带有左子树或右子树的节点，直接把子树节点接上就行了</li>
<li>删除带有左右子树的节点，找其左子树的最大节点或者是其右子树的最小节点交换值，然后删除被交换节点(规则跟删除叶子结点一样了)。选择左子树的最大节点还是右子树最小节点可以根据左右子树高度选择，优先选高的子树，这样更快趋于平衡。</li>
</ul>
]]></content>
      <categories>
        <category>Programming</category>
      </categories>
      <tags>
        <tag>数据结构</tag>
      </tags>
  </entry>
  <entry>
    <title>【Qt源码笔记】Qt事件与Windows消息循环的联系</title>
    <url>/2016/07/31/qt-source-win-qt-event/</url>
    <content><![CDATA[<p>上次研究了一下Qt是如何对Win32初始化程序进行包装的。这次研究下Qt的事件循环和Windows消息循环之间的联系。</p>
<p>上次说到<code>QApplication</code>注册了一个<code>qt_internal_proc</code>方法来处理消息循环，但是在这个方法中并没有看到一些关于Qt事件的蛛丝马迹。例如<em>鼠标事件</em>、<em>键盘事件</em>等。</p>
<a id="more"></a>

<p>其实在<code>qt_internal_proc</code>方法中有个调用值得注意:<code>sendPostedEvents()</code>。如果在我们自己Demo的鼠标事件中打个断点，不难发现，就是从这个调用来到我们的<code>mousePressEvent()</code>的。但是如果我们查看堆栈信息，按图索骥，会发现：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">bool QWindowSystemInterface::sendWindowSystemEvents(QEventLoop::ProcessEventsFlags flags)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    int nevents &#x3D; 0;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    while (QWindowSystemInterfacePrivate::windowSystemEventsQueued()) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        QWindowSystemInterfacePrivate::WindowSystemEvent *event &#x3D;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            (flags &amp; QEventLoop::ExcludeUserInputEvents) ?</span></pre></td></tr><tr><td class="code"><pre><span class="line">                QWindowSystemInterfacePrivate::getNonUserInputWindowSystemEvent() :</span></pre></td></tr><tr><td class="code"><pre><span class="line">                QWindowSystemInterfacePrivate::getWindowSystemEvent();</span></pre></td></tr><tr><td class="code"><pre><span class="line">        if (!event)</span></pre></td></tr><tr><td class="code"><pre><span class="line">            break;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">        if (QWindowSystemInterfacePrivate::eventHandler) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            if (QWindowSystemInterfacePrivate::eventHandler-&gt;sendEvent(event))</span></pre></td></tr><tr><td class="code"><pre><span class="line">                nevents++;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125; else &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            nevents++;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            QGuiApplicationPrivate::processWindowSystemEvent(event);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#x2F;&#x2F; Record the accepted state for the processed event</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#x2F;&#x2F; (excluding flush events). This state can then be</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#x2F;&#x2F; returned by flushWindowSystemEvents().</span></pre></td></tr><tr><td class="code"><pre><span class="line">        if (event-&gt;type !&#x3D; QWindowSystemInterfacePrivate::FlushEvents)</span></pre></td></tr><tr><td class="code"><pre><span class="line">            QWindowSystemInterfacePrivate::eventAccepted.store(event-&gt;eventAccepted);</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">        delete event;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    return (nevents &gt; 0);</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>
<p>在上边可以看到，这个最原始的事件就是从<code>getXXXXXEvent()</code>方法中得到的，而这个方法是从一个事件队列中取事件。</p>
<p><code>getWindowSystemEvent()</code>方法中的内容是这样的：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">QWindowSystemInterfacePrivate::WindowSystemEvent * QWindowSystemInterfacePrivate::getWindowSystemEvent()</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    return windowSystemEventQueue.takeFirstOrReturnNull();</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>
<p>可以说这个事件队列就是我们要关注的焦点。那事件是如何被添加到这个队列里的，这里暂时按下不表，先记住他的名字<code>windowSystemEventQueue</code>。</p>
<p>###从QWidget谈起<br>回过头来想，鼠标键盘事件其实都是依托于窗口的，但其实<code>QApplication</code>本身并不属于窗体，我们如果想在程序中加入一些可视的窗口，就要自己做个<code>QWidget</code>或者是<code>QMainWindow</code>等等。所以可以得出一个大概的结论，这些事件的接收处理必然和<code>QWidget</code>有着千丝万缕的联系。另外关于Win32消息的处理，我们必然要关注的一个，那就是回调函数。</p>
<p>拿着这两个线索，花了一点时间，简单梳理一下，不难发现这里边的调用。<strong>以下调用非必要的会省略掉参数</strong></p>
<ol>
<li>初始化<code>QWidget</code>会初始化<code>QWidgetPrivate</code>，在<code>QWidgetPrivate</code>的<code>init()</code>中会调用<code>QWidget::create()</code>;</li>
<li>接着在<code>QWidget::create()</code>中调用<code>QWidgetPrivate::create_sys()</code>，在这个方法中，会创建一个<code>QWindow</code>，在创建之后如果<code>QWidget</code>是显示的，会调用<code>QWindow::setVisible(true)</code>; </li>
<li>在<code>QWindow::setVisible(true)</code>中调用<code>QWindow::create()</code>，这个方法中没有别的只是转调<code>QWindowPrivate::create()</code>。 <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">void QWindowPrivate::create(bool recursive)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    Q_Q(QWindow);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    if (platformWindow)</span></pre></td></tr><tr><td class="code"><pre><span class="line">        return;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    if (q-&gt;parent())</span></pre></td></tr><tr><td class="code"><pre><span class="line">        q-&gt;parent()-&gt;create();</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    platformWindow &#x3D; QGuiApplicationPrivate::platformIntegration()-&gt;createPlatformWindow(q);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    Q_ASSERT(platformWindow);</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    if (!platformWindow) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        qWarning() &lt;&lt; &quot;Failed to create platform window for&quot; &lt;&lt; q &lt;&lt; &quot;with flags&quot; &lt;&lt; q-&gt;flags();</span></pre></td></tr><tr><td class="code"><pre><span class="line">        return;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    QObjectList childObjects &#x3D; q-&gt;children();</span></pre></td></tr><tr><td class="code"><pre><span class="line">    for (int i &#x3D; 0; i &lt; childObjects.size(); i ++) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        QObject *object &#x3D; childObjects.at(i);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        if (!object-&gt;isWindowType())</span></pre></td></tr><tr><td class="code"><pre><span class="line">            continue;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">        QWindow *childWindow &#x3D; static_cast&lt;QWindow *&gt;(object);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        if (recursive)</span></pre></td></tr><tr><td class="code"><pre><span class="line">            childWindow-&gt;d_func()-&gt;create(recursive);</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#x2F;&#x2F; The child may have had deferred creation due to this window not being created</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#x2F;&#x2F; at the time setVisible was called, so we re-apply the visible state, which</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#x2F;&#x2F; may result in creating the child, and emitting the appropriate signals.</span></pre></td></tr><tr><td class="code"><pre><span class="line">        if (childWindow-&gt;isVisible())</span></pre></td></tr><tr><td class="code"><pre><span class="line">            childWindow-&gt;setVisible(true);</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">        if (QPlatformWindow *childPlatformWindow &#x3D; childWindow-&gt;d_func()-&gt;platformWindow)</span></pre></td></tr><tr><td class="code"><pre><span class="line">            childPlatformWindow-&gt;setParent(this-&gt;platformWindow);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    QPlatformSurfaceEvent e(QPlatformSurfaceEvent::SurfaceCreated);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    QGuiApplication::sendEvent(q, &amp;e);</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure></li>
<li>在这个方法中，可以看到<code>createPlatformWindow()</code>，顾名思义，会创建一个平台相关的<em>Window</em>。这里的实际调用是<code>QWindowsIntegration::createPlatformWindow()</code>。<br> 而在这个方法中，我们会看到这个语句<code>QWindowsWindowData::create(window, requested, window-&gt;title());</code>这里的<code>create()</code>是一个静态方法。</li>
<li>在<code>create()</code>中会搞出一个<code>WindowCreationData</code>，这个结构体在<strong>qwindowswindow.cpp</strong>中，可以看到在定义上边的注释，没错，<code>create()</code>中会调用<code>WindowCreationData::create()</code>来创建一个<em>system handle</em>。 <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">    &#x2F;*!</span></pre></td></tr><tr><td class="code"><pre><span class="line">    \class WindowCreationData</span></pre></td></tr><tr><td class="code"><pre><span class="line">    \brief Window creation code.</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    This struct gathers all information required to create a window.</span></pre></td></tr><tr><td class="code"><pre><span class="line">    Window creation is split in 3 steps:</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    \list</span></pre></td></tr><tr><td class="code"><pre><span class="line">    \li fromWindow() Gather all required information</span></pre></td></tr><tr><td class="code"><pre><span class="line">    \li create() Create the system handle.</span></pre></td></tr><tr><td class="code"><pre><span class="line">    \li initialize() Post creation initialization steps.</span></pre></td></tr><tr><td class="code"><pre><span class="line">    \endlist</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    The reason for this split is to also enable changing the QWindowFlags</span></pre></td></tr><tr><td class="code"><pre><span class="line">    by calling:</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    \list</span></pre></td></tr><tr><td class="code"><pre><span class="line">    \li fromWindow() Gather information and determine new system styles</span></pre></td></tr><tr><td class="code"><pre><span class="line">    \li applyWindowFlags() to apply the new window system styles.</span></pre></td></tr><tr><td class="code"><pre><span class="line">    \li initialize() Post creation initialization steps.</span></pre></td></tr><tr><td class="code"><pre><span class="line">    \endlist</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    Contains the window creation code formerly in qwidget_win.cpp.</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    \sa QWindowCreationContext</span></pre></td></tr><tr><td class="code"><pre><span class="line">    \internal</span></pre></td></tr><tr><td class="code"><pre><span class="line">    \ingroup qt-lighthouse-win</span></pre></td></tr><tr><td class="code"><pre><span class="line">*&#x2F;</span></pre></td></tr></table></figure></li>
<li>在<code>WindowCreationData::create()</code>中会发现一个非常熟悉的一段代码<br><code>const QString windowClassName = QWindowsContext::instance()-&gt;registerWindowClass(w);</code></li>
<li>这段代码是得到一个<code>QWindowsContext</code>实例，调用它的<code>registerWindowClass()</code>方法。而在<code>QWindowsContext::registerWindowClass()</code>中，我们会看到这段代码<br><code>return registerWindowClass(cname, qWindowsWndProc, style, GetSysColorBrush(COLOR_WINDOW), icon);</code>，在这里我们就会看到<code>qWindowsWndProc</code>，其实这个就是最终跟每个<code>QWidget</code>的事件相关的回调方法，这里暂时按下不表，先观察这个重载方法的内容：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">QString QWindowsContext::registerWindowClass(QString cname,</span></pre></td></tr><tr><td class="code"><pre><span class="line">                                             WNDPROC proc,</span></pre></td></tr><tr><td class="code"><pre><span class="line">                                             unsigned style,</span></pre></td></tr><tr><td class="code"><pre><span class="line">                                             HBRUSH brush,</span></pre></td></tr><tr><td class="code"><pre><span class="line">                                             bool icon)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#x2F;&#x2F; since multiple Qt versions can be used in one process</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#x2F;&#x2F; each one has to have window class names with a unique name</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#x2F;&#x2F; The first instance gets the unmodified name; if the class</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#x2F;&#x2F; has already been registered by another instance of Qt then</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#x2F;&#x2F; add an instance-specific ID, the address of the window proc.</span></pre></td></tr><tr><td class="code"><pre><span class="line">    static int classExists &#x3D; -1;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    const HINSTANCE appInstance &#x3D; static_cast&lt;HINSTANCE&gt;(GetModuleHandle(0));</span></pre></td></tr><tr><td class="code"><pre><span class="line">    if (classExists &#x3D;&#x3D; -1) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        WNDCLASS wcinfo;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        classExists &#x3D; GetClassInfo(appInstance, reinterpret_cast&lt;LPCWSTR&gt;(cname.utf16()), &amp;wcinfo);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        classExists &#x3D; classExists &amp;&amp; wcinfo.lpfnWndProc !&#x3D; proc;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    if (classExists)</span></pre></td></tr><tr><td class="code"><pre><span class="line">        cname +&#x3D; QString::number(reinterpret_cast&lt;quintptr&gt;(proc));</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    if (d-&gt;m_registeredWindowClassNames.contains(cname))        &#x2F;&#x2F; already registered in our list</span></pre></td></tr><tr><td class="code"><pre><span class="line">        return cname;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">#ifndef Q_OS_WINCE</span></pre></td></tr><tr><td class="code"><pre><span class="line">    WNDCLASSEX wc;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    wc.cbSize       &#x3D; sizeof(WNDCLASSEX);</span></pre></td></tr><tr><td class="code"><pre><span class="line">#else</span></pre></td></tr><tr><td class="code"><pre><span class="line">    WNDCLASS wc;</span></pre></td></tr><tr><td class="code"><pre><span class="line">#endif</span></pre></td></tr><tr><td class="code"><pre><span class="line">    wc.style        &#x3D; style;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    wc.lpfnWndProc  &#x3D; proc;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    wc.cbClsExtra   &#x3D; 0;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    wc.cbWndExtra   &#x3D; 0;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    wc.hInstance    &#x3D; appInstance;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    wc.hCursor      &#x3D; 0;</span></pre></td></tr><tr><td class="code"><pre><span class="line">#ifndef Q_OS_WINCE</span></pre></td></tr><tr><td class="code"><pre><span class="line">    wc.hbrBackground &#x3D; brush;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    if (icon) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        wc.hIcon &#x3D; static_cast&lt;HICON&gt;(LoadImage(appInstance, L&quot;IDI_ICON1&quot;, IMAGE_ICON, 0, 0, LR_DEFAULTSIZE));</span></pre></td></tr><tr><td class="code"><pre><span class="line">        if (wc.hIcon) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            int sw &#x3D; GetSystemMetrics(SM_CXSMICON);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            int sh &#x3D; GetSystemMetrics(SM_CYSMICON);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            wc.hIconSm &#x3D; static_cast&lt;HICON&gt;(LoadImage(appInstance, L&quot;IDI_ICON1&quot;, IMAGE_ICON, sw, sh, 0));</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125; else &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            wc.hIcon &#x3D; static_cast&lt;HICON&gt;(LoadImage(0, IDI_APPLICATION, IMAGE_ICON, 0, 0, LR_DEFAULTSIZE | LR_SHARED));</span></pre></td></tr><tr><td class="code"><pre><span class="line">            wc.hIconSm &#x3D; 0;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125; else &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        wc.hIcon    &#x3D; 0;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        wc.hIconSm  &#x3D; 0;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">#else</span></pre></td></tr><tr><td class="code"><pre><span class="line">    if (icon) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        wc.hIcon &#x3D; (HICON)LoadImage(appInstance, L&quot;IDI_ICON1&quot;, IMAGE_ICON, 0, 0, LR_DEFAULTSIZE);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125; else &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        wc.hIcon    &#x3D; 0;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">#endif</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    wc.lpszMenuName  &#x3D; 0;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    wc.lpszClassName &#x3D; reinterpret_cast&lt;LPCWSTR&gt;(cname.utf16());</span></pre></td></tr><tr><td class="code"><pre><span class="line">#ifndef Q_OS_WINCE</span></pre></td></tr><tr><td class="code"><pre><span class="line">    ATOM atom &#x3D; RegisterClassEx(&amp;wc);</span></pre></td></tr><tr><td class="code"><pre><span class="line">#else</span></pre></td></tr><tr><td class="code"><pre><span class="line">    ATOM atom &#x3D; RegisterClass(&amp;wc);</span></pre></td></tr><tr><td class="code"><pre><span class="line">#endif</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    if (!atom)</span></pre></td></tr><tr><td class="code"><pre><span class="line">        qErrnoWarning(&quot;QApplication::regClass: Registering window class &#39;%s&#39; failed.&quot;,</span></pre></td></tr><tr><td class="code"><pre><span class="line">                      qPrintable(cname));</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    d-&gt;m_registeredWindowClassNames.insert(cname);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    qCDebug(lcQpaWindows).nospace() &lt;&lt; __FUNCTION__ &lt;&lt; &#39; &#39; &lt;&lt; cname</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &lt;&lt; &quot; style&#x3D;0x&quot; &lt;&lt; hex &lt;&lt; style &lt;&lt; dec</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &lt;&lt; &quot; brush&#x3D;&quot; &lt;&lt; brush &lt;&lt; &quot; icon&#x3D;&quot; &lt;&lt; icon &lt;&lt; &quot; atom&#x3D;&quot; &lt;&lt; atom;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    return cname;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

</li>
</ol>
<p>到这里，就看到了注册窗口的基本套路<code>RegisterClass()</code>，就算是彻底把跟Qt事件相关的消息循环回调找到了。</p>
<p>现在再来看一下刚才说的<code>qWindowsWndProc</code>，这里边的内容，其实比较简短：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">extern &quot;C&quot; LRESULT QT_WIN_CALLBACK qWindowsWndProc(HWND hwnd, UINT message, WPARAM wParam, LPARAM lParam)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    LRESULT result;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    const QtWindows::WindowsEventType et &#x3D; windowsEventType(message, wParam, lParam);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    const bool handled &#x3D; QWindowsContext::instance()-&gt;windowsProc(hwnd, message, et, wParam, lParam, &amp;result);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    if (QWindowsContext::verbose &gt; 1 &amp;&amp; lcQpaEvents().isDebugEnabled()) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        if (const char *eventName &#x3D; QWindowsGuiEventDispatcher::windowsMessageName(message)) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            qCDebug(lcQpaEvents) &lt;&lt; &quot;EVENT: hwd&#x3D;&quot; &lt;&lt; hwnd &lt;&lt; eventName &lt;&lt; hex &lt;&lt; &quot;msg&#x3D;0x&quot;  &lt;&lt; message</span></pre></td></tr><tr><td class="code"><pre><span class="line">                &lt;&lt; &quot;et&#x3D;0x&quot; &lt;&lt; et &lt;&lt; dec &lt;&lt; &quot;wp&#x3D;&quot; &lt;&lt; int(wParam) &lt;&lt; &quot;at&quot;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                &lt;&lt; GET_X_LPARAM(lParam) &lt;&lt; GET_Y_LPARAM(lParam) &lt;&lt; &quot;handled&#x3D;&quot; &lt;&lt; handled;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    if (!handled)</span></pre></td></tr><tr><td class="code"><pre><span class="line">        result &#x3D; DefWindowProc(hwnd, message, wParam, lParam);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    return result;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>
<p>在这里主要做了一些微小的工作，对消息分类把消息处理成<code>QtWindow::WindowEventType</code>类型，便于后续处理，具体逻辑在<code>windowsEventType()</code>方法中，主要是做Win32消息和Qt事件的映射。然后就是调用<code>QWindowsContext::windowsProc()</code>处理消息。特定情况下输出debug信息。在处理消息的时候会得到处理结果，对于没有处理的调用<code>DefWindowProc()</code>做默认处理。</p>
<p><strong>如果想看Win32消息和Qt事件对应的关系映射，在上边说到的windowEventType()方法中是最快的，基本涵盖了大部分，但是要注意有一些名字对不上，因为到这里其实分类还不是QEvent，而是一个中间类型</strong></p>
<p>现在来重点关注一下<code>windowProc()</code>方法。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">bool QWindowsContext::windowsProc(HWND hwnd, UINT message,</span></pre></td></tr><tr><td class="code"><pre><span class="line">                                  QtWindows::WindowsEventType et,</span></pre></td></tr><tr><td class="code"><pre><span class="line">                                  WPARAM wParam, LPARAM lParam, LRESULT *result)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    *result &#x3D; 0;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    MSG msg;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    msg.hwnd &#x3D; hwnd;         &#x2F;&#x2F; re-create MSG structure</span></pre></td></tr><tr><td class="code"><pre><span class="line">    msg.message &#x3D; message;   &#x2F;&#x2F; time and pt fields ignored</span></pre></td></tr><tr><td class="code"><pre><span class="line">    msg.wParam &#x3D; wParam;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    msg.lParam &#x3D; lParam;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    msg.pt.x &#x3D; msg.pt.y &#x3D; 0;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    if (et !&#x3D; QtWindows::CursorEvent &amp;&amp; (et &amp; (QtWindows::MouseEventFlag | QtWindows::NonClientEventFlag))) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        msg.pt.x &#x3D; GET_X_LPARAM(lParam);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        msg.pt.y &#x3D; GET_Y_LPARAM(lParam);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#x2F;&#x2F; For non-client-area messages, these are screen coordinates (as expected</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#x2F;&#x2F; in the MSG structure), otherwise they are client coordinates.</span></pre></td></tr><tr><td class="code"><pre><span class="line">        if (!(et &amp; QtWindows::NonClientEventFlag)) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            ClientToScreen(msg.hwnd, &amp;msg.pt);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125; else &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">#ifndef Q_OS_WINCE</span></pre></td></tr><tr><td class="code"><pre><span class="line">        GetCursorPos(&amp;msg.pt);</span></pre></td></tr><tr><td class="code"><pre><span class="line">#endif</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#x2F;&#x2F; Run the native event filters.</span></pre></td></tr><tr><td class="code"><pre><span class="line">    long filterResult &#x3D; 0;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    QAbstractEventDispatcher* dispatcher &#x3D; QAbstractEventDispatcher::instance();</span></pre></td></tr><tr><td class="code"><pre><span class="line">    if (dispatcher &amp;&amp; dispatcher-&gt;filterNativeEvent(d-&gt;m_eventType, &amp;msg, &amp;filterResult)) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        *result &#x3D; LRESULT(filterResult);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        return true;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    QWindowsWindow *platformWindow &#x3D; findPlatformWindow(hwnd);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    if (platformWindow) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        filterResult &#x3D; 0;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        if (QWindowSystemInterface::handleNativeEvent(platformWindow-&gt;window(), d-&gt;m_eventType, &amp;msg, &amp;filterResult)) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            *result &#x3D; LRESULT(filterResult);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            return true;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    if (et &amp; QtWindows::InputMethodEventFlag) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        QWindowsInputContext *windowsInputContext &#x3D; ::windowsInputContext();</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#x2F;&#x2F; Disable IME assuming this is a special implementation hooking into keyboard input.</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#x2F;&#x2F; &quot;Real&quot; IME implementations should use a native event filter intercepting IME events.</span></pre></td></tr><tr><td class="code"><pre><span class="line">        if (!windowsInputContext) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            QWindowsInputContext::setWindowsImeEnabled(platformWindow, false);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            return false;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        switch (et) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        case QtWindows::InputMethodStartCompositionEvent:</span></pre></td></tr><tr><td class="code"><pre><span class="line">            return windowsInputContext-&gt;startComposition(hwnd);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        case QtWindows::InputMethodCompositionEvent:</span></pre></td></tr><tr><td class="code"><pre><span class="line">            return windowsInputContext-&gt;composition(hwnd, lParam);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        case QtWindows::InputMethodEndCompositionEvent:</span></pre></td></tr><tr><td class="code"><pre><span class="line">            return windowsInputContext-&gt;endComposition(hwnd);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        case QtWindows::InputMethodRequest:</span></pre></td></tr><tr><td class="code"><pre><span class="line">            return windowsInputContext-&gt;handleIME_Request(wParam, lParam, result);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        default:</span></pre></td></tr><tr><td class="code"><pre><span class="line">            break;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125; &#x2F;&#x2F; InputMethodEventFlag</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#x2F;&#x2F;...</span></pre></td></tr><tr><td class="code"><pre><span class="line">    if (platformWindow) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#x2F;&#x2F; Suppress events sent during DestroyWindow() for native children.</span></pre></td></tr><tr><td class="code"><pre><span class="line">        if (platformWindow-&gt;testFlag(QWindowsWindow::WithinDestroy))</span></pre></td></tr><tr><td class="code"><pre><span class="line">            return false;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        if (QWindowsContext::verbose &gt; 1)</span></pre></td></tr><tr><td class="code"><pre><span class="line">            qCDebug(lcQpaEvents) &lt;&lt; &quot;Event window: &quot; &lt;&lt; platformWindow-&gt;window();</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125; else &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        qWarning(&quot;%s: No Qt Window found for event 0x%x (%s), hwnd&#x3D;0x%p.&quot;,</span></pre></td></tr><tr><td class="code"><pre><span class="line">                 __FUNCTION__, message,</span></pre></td></tr><tr><td class="code"><pre><span class="line">                 QWindowsGuiEventDispatcher::windowsMessageName(message), hwnd);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        return false;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    switch (et) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    case QtWindows::KeyboardLayoutChangeEvent:</span></pre></td></tr><tr><td class="code"><pre><span class="line">        if (QWindowsInputContext *wic &#x3D; windowsInputContext())</span></pre></td></tr><tr><td class="code"><pre><span class="line">            wic-&gt;handleInputLanguageChanged(wParam, lParam); &#x2F;&#x2F; fallthrough intended.</span></pre></td></tr><tr><td class="code"><pre><span class="line">    case QtWindows::KeyDownEvent:</span></pre></td></tr><tr><td class="code"><pre><span class="line">    case QtWindows::KeyEvent:</span></pre></td></tr><tr><td class="code"><pre><span class="line">    case QtWindows::InputMethodKeyEvent:</span></pre></td></tr><tr><td class="code"><pre><span class="line">    case QtWindows::InputMethodKeyDownEvent:</span></pre></td></tr><tr><td class="code"><pre><span class="line">    case QtWindows::AppCommandEvent:</span></pre></td></tr><tr><td class="code"><pre><span class="line">#if !defined(Q_OS_WINCE) &amp;&amp; !defined(QT_NO_SESSIONMANAGER)</span></pre></td></tr><tr><td class="code"><pre><span class="line">        return platformSessionManager()-&gt;isInteractionBlocked() ? true : d-&gt;m_keyMapper.translateKeyEvent(platformWindow-&gt;window(), hwnd, msg, result);</span></pre></td></tr><tr><td class="code"><pre><span class="line">#else</span></pre></td></tr><tr><td class="code"><pre><span class="line">        return d-&gt;m_keyMapper.translateKeyEvent(platformWindow-&gt;window(), hwnd, msg, result);</span></pre></td></tr><tr><td class="code"><pre><span class="line">#endif</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#x2F;&#x2F;...</span></pre></td></tr><tr><td class="code"><pre><span class="line">    case QtWindows::MouseWheelEvent:</span></pre></td></tr><tr><td class="code"><pre><span class="line">    case QtWindows::MouseEvent:</span></pre></td></tr><tr><td class="code"><pre><span class="line">    case QtWindows::LeaveEvent:</span></pre></td></tr><tr><td class="code"><pre><span class="line">#if !defined(Q_OS_WINCE) &amp;&amp; !defined(QT_NO_SESSIONMANAGER)</span></pre></td></tr><tr><td class="code"><pre><span class="line">        return platformSessionManager()-&gt;isInteractionBlocked() ? true : d-&gt;m_mouseHandler.translateMouseEvent(platformWindow-&gt;window(), hwnd, et, msg, result);</span></pre></td></tr><tr><td class="code"><pre><span class="line">#else</span></pre></td></tr><tr><td class="code"><pre><span class="line">        return d-&gt;m_mouseHandler.translateMouseEvent(platformWindow-&gt;window(), hwnd, et, msg, result);</span></pre></td></tr><tr><td class="code"><pre><span class="line">#endif</span></pre></td></tr><tr><td class="code"><pre><span class="line">    case QtWindows::TouchEvent:</span></pre></td></tr><tr><td class="code"><pre><span class="line">#if !defined(Q_OS_WINCE) &amp;&amp; !defined(QT_NO_SESSIONMANAGER)</span></pre></td></tr><tr><td class="code"><pre><span class="line">        return platformSessionManager()-&gt;isInteractionBlocked() ? true : d-&gt;m_mouseHandler.translateTouchEvent(platformWindow-&gt;window(), hwnd, et, msg, result);</span></pre></td></tr><tr><td class="code"><pre><span class="line">#else</span></pre></td></tr><tr><td class="code"><pre><span class="line">        return d-&gt;m_mouseHandler.translateTouchEvent(platformWindow-&gt;window(), hwnd, et, msg, result);</span></pre></td></tr><tr><td class="code"><pre><span class="line">#endif</span></pre></td></tr><tr><td class="code"><pre><span class="line">    case QtWindows::FocusInEvent: &#x2F;&#x2F; see QWindowsWindow::requestActivateWindow().</span></pre></td></tr><tr><td class="code"><pre><span class="line">    case QtWindows::FocusOutEvent:</span></pre></td></tr><tr><td class="code"><pre><span class="line">        handleFocusEvent(et, platformWindow);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        return true;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    case QtWindows::ShowEventOnParentRestoring: &#x2F;&#x2F; QTBUG-40696, prevent Windows from re-showing hidden transient children (dialogs).</span></pre></td></tr><tr><td class="code"><pre><span class="line">        if (!platformWindow-&gt;window()-&gt;isVisible()) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            *result &#x3D; 0;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            return true;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        break;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#x2F;&#x2F;...</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">   &#x2F;&#x2F;...</span></pre></td></tr><tr><td class="code"><pre><span class="line">    return false;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>
<p>本着太长不看的原则，我把一些相似的都省略掉了。这里就能看到在这里会根据消息类型来进行分类处理。处理的方式也是统一的，调用<code>handleXXXXEvent()</code>或是<code>tranlateXXXXEvent()</code>。需要二次加工的就要走到<code>tranlateXXXXEvent()</code>二次加工。最终其实都是走到<code>handleXXXXEvent()</code>。而<code>handleXXXXEvent()</code>方法中会将事件包装成一个新的类型，再统一调用<code>QWindowSystemInterfacePrivate::handleWindowSystemEvent(e)</code><strong>PS:这是个静态方法</strong>，这个静态方法中需要关注<code>postWindowSystemEvent()</code>。</p>
<p>现在来看<code>postWindowSystemEvent()</code>：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">void QWindowSystemInterfacePrivate::postWindowSystemEvent(WindowSystemEvent *ev)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    windowSystemEventQueue.append(ev);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    QAbstractEventDispatcher *dispatcher &#x3D; QGuiApplicationPrivate::qt_qpa_core_dispatcher();</span></pre></td></tr><tr><td class="code"><pre><span class="line">    if (dispatcher)</span></pre></td></tr><tr><td class="code"><pre><span class="line">        dispatcher-&gt;wakeUp();</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>看到了非常熟悉的一个队列<code>windowSystemEventQueue</code>，就是在这里将事件加入队列，至此整个Qt事件和Windows消息循环彻底联系起来……</p>
<p>其实这只是一个添加事件、获取事件的简单流程，仅仅为了研究Qt事件和Windows消息循环的联系。<br>在这中间省略的很多其他细节，包括注册窗口，反注册，具体的事件处理规则，还有一些防止事件错误发送的保护机制，都是很好的研究内容……</p>
]]></content>
      <categories>
        <category>Programming</category>
      </categories>
      <tags>
        <tag>Qt</tag>
        <tag>源码分析</tag>
      </tags>
  </entry>
  <entry>
    <title>【Qt源码笔记】从WinMain说起</title>
    <url>/2016/07/24/qt-source-begin_with_WinMain/</url>
    <content><![CDATA[<p>Qt在各个平台下都是对平台API进行了一些包装。Windows下是对Win32API的封装。如果是Windows平台的GUI Application就一定是从<code>WinMain</code>开始。</p>
<p>不难发现<code>WinMain</code>就在<strong>qtmain_win.cpp</strong>中。</p>
<a id="more"></a>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">extern</span> <span class="string">"C"</span> <span class="function"><span class="keyword">int</span> APIENTRY <span class="title">WinMain</span><span class="params">(HINSTANCE, HINSTANCE, LPSTR <span class="comment">/*cmdParamarg*/</span>, <span class="keyword">int</span> <span class="comment">/* cmdShow */</span>)</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">int</span> argc;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">wchar_t</span> **argvW = CommandLineToArgvW(GetCommandLineW(), &amp;argc);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (!argvW)</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">char</span> **argv = <span class="keyword">new</span> <span class="keyword">char</span> *[argc + <span class="number">1</span>];</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; argc; ++i)</span></pre></td></tr><tr><td class="code"><pre><span class="line">        argv[i] = wideToMulti(CP_ACP, argvW[i]);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    argv[argc] = Q_NULLPTR;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    LocalFree(argvW);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">const</span> <span class="keyword">int</span> exitCode = main(argc, argv);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; argc &amp;&amp; argv[i]; ++i)</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">delete</span> [] argv[i];</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">delete</span> [] argv;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> exitCode;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>
<p>在这里的<code>WinMain</code>仅仅充当一个入口，所有对命令行参数 这些 都交由<code>main</code>来处理。而这个<code>main</code>就是我们在自己的主程序中写的<code>main</code>。</p>
<p>入口找到以后，在Windows下的程序还有一个很重要的东西，那就是消息循环。Win32中经典的<code>PeekMessage()</code>、<code>DispatchMessage()</code>和<code>TranslateMessage()</code>。这些东西在程序中注册的回调函数中被调用，用来处理和解析消息。Qt本身也要依赖这些，只不过在上边进行了一些封装。调到我们自己的程序里看到的就是<code>winEvent()</code>或者是一些<code>QEvent</code>了。</p>
<p>我们写Qt程序的时候，一个很常见的套路是：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">int main(int argv, char **args)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    QApplication app(argv, args);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#x2F;&#x2F;todo...</span></pre></td></tr><tr><td class="code"><pre><span class="line">    return app.exec();</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>
<p>这个回调函数就是在<code>app.exec()</code>中被注册(准确的说回调函数是由在这个方法中调用的其他方法注册)。不难找到一个叫做<strong>qeventdispatcher_win.cpp</strong>文件，名字已经很明确了，就是处理Qt事件的。我们会找到一个类<code>QEventDispatcherWin32</code>。可以发现一个<code>processEvents()</code>方法。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">bool</span> QEventDispatcherWin32::processEvents(QEventLoop::ProcessEventsFlags flags)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    Q_D(QEventDispatcherWin32);</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (!d-&gt;internalHwnd) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        createInternalHwnd();</span></pre></td></tr><tr><td class="code"><pre><span class="line">        wakeUp(); <span class="comment">// trigger a call to sendPostedEvents()</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    </span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="comment">//...</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">do</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        DWORD waitRet = <span class="number">0</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        HANDLE pHandles[MAXIMUM_WAIT_OBJECTS - <span class="number">1</span>];</span></pre></td></tr><tr><td class="code"><pre><span class="line">        QVarLengthArray&lt;MSG&gt; processedTimers;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">while</span> (!d-&gt;interrupt) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            DWORD nCount = d-&gt;winEventNotifierList.count();</span></pre></td></tr><tr><td class="code"><pre><span class="line">            Q_ASSERT(nCount &lt; MAXIMUM_WAIT_OBJECTS - <span class="number">1</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">            MSG msg;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">bool</span> haveMessage;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">if</span> (!(flags &amp; QEventLoop::ExcludeUserInputEvents) &amp;&amp; !d-&gt;queuedUserInputEvents.isEmpty()) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                <span class="comment">// process queued user input events</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">                haveMessage = <span class="literal">true</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                msg = d-&gt;queuedUserInputEvents.takeFirst();</span></pre></td></tr><tr><td class="code"><pre><span class="line">            &#125; <span class="keyword">else</span> <span class="keyword">if</span>(!(flags &amp; QEventLoop::ExcludeSocketNotifiers) &amp;&amp; !d-&gt;queuedSocketEvents.isEmpty()) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                <span class="comment">// process queued socket events</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">                haveMessage = <span class="literal">true</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                msg = d-&gt;queuedSocketEvents.takeFirst();</span></pre></td></tr><tr><td class="code"><pre><span class="line">            &#125; <span class="keyword">else</span> &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                haveMessage = PeekMessage(&amp;msg, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, PM_REMOVE);</span></pre></td></tr><tr><td class="code"><pre><span class="line">                <span class="keyword">if</span> (haveMessage) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                    <span class="keyword">if</span> ((flags &amp; QEventLoop::ExcludeUserInputEvents)</span></pre></td></tr><tr><td class="code"><pre><span class="line">                        &amp;&amp; ((msg.message &gt;= WM_KEYFIRST</span></pre></td></tr><tr><td class="code"><pre><span class="line">                             &amp;&amp; msg.message &lt;= WM_KEYLAST)</span></pre></td></tr><tr><td class="code"><pre><span class="line">                            || (msg.message &gt;= WM_MOUSEFIRST</span></pre></td></tr><tr><td class="code"><pre><span class="line">                                &amp;&amp; msg.message &lt;= WM_MOUSELAST)</span></pre></td></tr><tr><td class="code"><pre><span class="line">                            || msg.message == WM_MOUSEWHEEL</span></pre></td></tr><tr><td class="code"><pre><span class="line">                            || msg.message == WM_MOUSEHWHEEL</span></pre></td></tr><tr><td class="code"><pre><span class="line">                            || msg.message == WM_TOUCH</span></pre></td></tr><tr><td class="code"><pre><span class="line">#ifndef QT_NO_GESTURES</span></pre></td></tr><tr><td class="code"><pre><span class="line">                            || msg.message == WM_GESTURE</span></pre></td></tr><tr><td class="code"><pre><span class="line">                            || msg.message == WM_GESTURENOTIFY</span></pre></td></tr><tr><td class="code"><pre><span class="line">#endif</span></pre></td></tr><tr><td class="code"><pre><span class="line">                            || msg.message == WM_CLOSE)) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                        <span class="comment">// queue user input events for later processing</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">                        d-&gt;queuedUserInputEvents.append(msg);</span></pre></td></tr><tr><td class="code"><pre><span class="line">                        <span class="keyword">continue</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                    <span class="comment">//...</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">                &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="comment">//...</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">if</span> (haveMessage) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                <span class="comment">//...</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">                <span class="keyword">if</span> (!filterNativeEvent(QByteArrayLiteral(<span class="string">"windows_generic_MSG"</span>), &amp;msg, <span class="number">0</span>)) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                    TranslateMessage(&amp;msg);</span></pre></td></tr><tr><td class="code"><pre><span class="line">                    DispatchMessage(&amp;msg);</span></pre></td></tr><tr><td class="code"><pre><span class="line">                &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            &#125; </span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="comment">//...</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">            retVal = <span class="literal">true</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="comment">// still nothing - wait for message or signalled objects</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">        canWait = (!retVal</span></pre></td></tr><tr><td class="code"><pre><span class="line">                   &amp;&amp; !d-&gt;interrupt</span></pre></td></tr><tr><td class="code"><pre><span class="line">                   &amp;&amp; (flags &amp; QEventLoop::WaitForMoreEvents));</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">if</span> (canWait) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            DWORD nCount = d-&gt;winEventNotifierList.count();</span></pre></td></tr><tr><td class="code"><pre><span class="line">            Q_ASSERT(nCount &lt; MAXIMUM_WAIT_OBJECTS - <span class="number">1</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> i=<span class="number">0</span>; i&lt;(<span class="keyword">int</span>)nCount; i++)</span></pre></td></tr><tr><td class="code"><pre><span class="line">                pHandles[i] = d-&gt;winEventNotifierList.at(i)-&gt;handle();</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="function">emit <span class="title">aboutToBlock</span><span class="params">()</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            waitRet = MsgWaitForMultipleObjectsEx(nCount, pHandles, INFINITE, QS_ALLINPUT, MWMO_ALERTABLE | MWMO_INPUTAVAILABLE);</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="function">emit <span class="title">awake</span><span class="params">()</span></span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            <span class="keyword">if</span> (waitRet - WAIT_OBJECT_0 &lt; nCount) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">                d-&gt;activateEventNotifier(d-&gt;winEventNotifierList.at(waitRet - WAIT_OBJECT_0));</span></pre></td></tr><tr><td class="code"><pre><span class="line">                retVal = <span class="literal">true</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">            &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125; <span class="keyword">while</span> (canWait);</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="comment">// ...</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> retVal;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>代码比较长，省略了一些暂时不关注的，在这里我们可以看到我们最熟悉的Win32的消息枚举和方法。现在问题来了<code>DispatchMessage()</code>以后，程序的调用会走到我们注册的回调函数，由我们自己来处理消息。所以要找到这个回调。</p>
<p>Qt的这个回调函数是<code>qt_internal_proc()</code>。那下一个问题就是在哪里注册的这个回调函数。<br>这个可以回顾一下Win32程序的一般套路：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">WNDCLASSEX wc;</span></pre></td></tr><tr><td class="code"><pre><span class="line">HWND hwnd;</span></pre></td></tr><tr><td class="code"><pre><span class="line">MSG Msg;</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="comment">//Step 1: Registering the Window Class</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">wc.cbSize        = <span class="keyword">sizeof</span>(WNDCLASSEX);</span></pre></td></tr><tr><td class="code"><pre><span class="line">wc.style         = <span class="number">0</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">wc.lpfnWndProc   = WndProc;</span></pre></td></tr><tr><td class="code"><pre><span class="line">wc.cbClsExtra    = <span class="number">0</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">wc.cbWndExtra    = <span class="number">0</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">wc.hInstance     = hInstance;</span></pre></td></tr><tr><td class="code"><pre><span class="line">wc.hIcon         = LoadIcon(<span class="literal">NULL</span>, IDI_APPLICATION);</span></pre></td></tr><tr><td class="code"><pre><span class="line">wc.hCursor       = LoadCursor(<span class="literal">NULL</span>, IDC_ARROW);</span></pre></td></tr><tr><td class="code"><pre><span class="line">wc.hbrBackground = (HBRUSH)(COLOR_WINDOW+<span class="number">1</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line">wc.lpszMenuName  = <span class="literal">NULL</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">wc.lpszClassName = g_szClassName;</span></pre></td></tr><tr><td class="code"><pre><span class="line">wc.hIconSm       = LoadIcon(<span class="literal">NULL</span>, IDI_APPLICATION);</span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="keyword">if</span>(!RegisterClassEx(&amp;wc))</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    MessageBox(<span class="literal">NULL</span>, <span class="string">"Window Registration Failed!"</span>, <span class="string">"Error!"</span>,</span></pre></td></tr><tr><td class="code"><pre><span class="line">        MB_ICONEXCLAMATION | MB_OK);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>
<p>如果要写一个Win32的程序，都要先注册一个<code>Windows Class</code>,就是在<code>lpfnWndProc</code>中指明我们的回调方法。再回过头去看<code>processEvents()</code>方法中<code>createInternalHwnd()</code>的调用:</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> QEventDispatcherWin32::createInternalHwnd()</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    Q_D(QEventDispatcherWin32);</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (d-&gt;internalHwnd)</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    d-&gt;internalHwnd = qt_create_internal_window(<span class="keyword">this</span>);</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    installMessageHook();</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="comment">// start all normal timers</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; d-&gt;timerVec.count(); ++i)</span></pre></td></tr><tr><td class="code"><pre><span class="line">        d-&gt;registerTimer(d-&gt;timerVec.at(i));</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>
<p>可以看到一个名叫<code>qt_create_internal_window()</code>的方法，顾名思义。在此方法中会获取一个<code>QWindowsMessageWindowClassContext</code>,看一下他的构造函数，一目了然:</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">QWindowsMessageWindowClassContext::QWindowsMessageWindowClassContext()</span></pre></td></tr><tr><td class="code"><pre><span class="line">    : atom(<span class="number">0</span>), className(<span class="number">0</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="comment">// make sure that multiple Qt's can coexist in the same process</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">const</span> QString qClassName = QStringLiteral(<span class="string">"QEventDispatcherWin32_Internal_Widget"</span>)</span></pre></td></tr><tr><td class="code"><pre><span class="line">        + QString::number(quintptr(qt_internal_proc));</span></pre></td></tr><tr><td class="code"><pre><span class="line">    className = <span class="keyword">new</span> <span class="keyword">wchar_t</span>[qClassName.<span class="built_in">size</span>() + <span class="number">1</span>];</span></pre></td></tr><tr><td class="code"><pre><span class="line">    qClassName.toWCharArray(className);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    className[qClassName.<span class="built_in">size</span>()] = <span class="number">0</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    WNDCLASS wc;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    wc.style = <span class="number">0</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    wc.lpfnWndProc = qt_internal_proc;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    wc.cbClsExtra = <span class="number">0</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    wc.cbWndExtra = <span class="number">0</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    wc.hInstance = qWinAppInst();</span></pre></td></tr><tr><td class="code"><pre><span class="line">    wc.hIcon = <span class="number">0</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    wc.hCursor = <span class="number">0</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    wc.hbrBackground = <span class="number">0</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    wc.lpszMenuName = <span class="literal">NULL</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    wc.lpszClassName = className;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    atom = RegisterClass(&amp;wc);</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (!atom) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        qErrnoWarning(<span class="string">"%s RegisterClass() failed"</span>, qPrintable(qClassName));</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">delete</span> [] className;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        className = <span class="number">0</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>不难发现回调函数<code>qt_internal_proc()</code>就是在这里注册的。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">LRESULT QT_WIN_CALLBACK <span class="title">qt_internal_proc</span><span class="params">(HWND hwnd, UINT message, WPARAM wp, LPARAM lp)</span></span></span></pre></td></tr><tr><td class="code"><pre><span class="line"><span class="function"></span>&#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (message == WM_NCCREATE)</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line"></span></pre></td></tr><tr><td class="code"><pre><span class="line">    MSG msg;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    msg.hwnd = hwnd;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    msg.message = message;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    msg.wParam = wp;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    msg.lParam = lp;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    QAbstractEventDispatcher* dispatcher = QAbstractEventDispatcher::instance();</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">long</span> result;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (!dispatcher) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">if</span> (message == WM_TIMER)</span></pre></td></tr><tr><td class="code"><pre><span class="line">            KillTimer(hwnd, wp);</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (dispatcher-&gt;filterNativeEvent(QByteArrayLiteral(<span class="string">"windows_dispatcher_MSG"</span>), &amp;msg, &amp;result)) &#123;</span></pre></td></tr><tr><td class="code"><pre><span class="line">        <span class="keyword">return</span> result;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    &#125;</span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="comment">//...</span></span></pre></td></tr><tr><td class="code"><pre><span class="line">    <span class="keyword">return</span> result;</span></pre></td></tr><tr><td class="code"><pre><span class="line">&#125;</span></pre></td></tr></table></figure>

<p>Qt就是这样将Win32的调用包装成了自己的调用。</p>
]]></content>
      <categories>
        <category>Programming</category>
      </categories>
      <tags>
        <tag>Qt</tag>
        <tag>源码分析</tag>
      </tags>
  </entry>
  <entry>
    <title>编译Qt</title>
    <url>/2016/01/03/build-qt/</url>
    <content><![CDATA[<p>参考:</p>
<ul>
<li><a href="http://wiki.qt.io/Building_Qt_5_from_Git" target="_blank" rel="noopener">Building-Qt5-from-Git</a></li>
</ul>
<p>Windows下自己准备自己想要的编译器。编译自己想要的Qt版本<br>最近5.6出来了，有好多新的变化，第一时间准备尝鲜。<br>我自己的是MSVC2015 Qt5.6</p>
<a id="more"></a>
<p>首先要准备：</p>
<ul>
<li>Perl</li>
<li>Git</li>
<li>python</li>
</ul>
<p>如果要是编译webkit的话，还需要icu套件，这次为了熟悉流程先不编这个，因为Qt5.6已经把webkit移除了，换成了webengine，官网的文档还没有更新，暂时还没研究</p>
<p><strong>一定要先装Perl再装Git，以免用了老的Git，会导致脚本失败</strong><br>而且安装Git的时候需要选择<strong>可以在cmd使用Git命令</strong>，不然就只能用Git自己带的那个命令行了<br>Qt的代码使用Git做版本管理，但是他的子库很多，每个模块都是单独的一个仓库，所以检出的时候为了避免麻烦则用Perl写成了脚本。</p>
<p>为了方便的话，可以直接用VS的命令行，这样免了设置MSVC的环境了。<br>设置一些环境变量：</p>
<blockquote>
<p>SET _ROOT=C:\qt5<br>SET PATH=%_ROOT%\qtbase\bin;%_ROOT%\gnuwin32\bin;%PATH%<br>SET QMAKESPEC=win32-msvc2015<br>SET _ROOT=</p>
</blockquote>
<ul>
<li>注：_ROOT设置成自己检出Qt代码的目录(你检出代码之后再去设置这些也是可以的)，QMAKESPEC设置成自己用的编译器</li>
</ul>
<h3 id="取顶层仓库"><a href="#取顶层仓库" class="headerlink" title="取顶层仓库"></a>取顶层仓库</h3><blockquote>
<p>git clone git://code.qt.io/qt/qt5.git</p>
</blockquote>
<p>或者是</p>
<blockquote>
<p>git clone <a href="https://code.qt.io/qt/qt5.git" target="_blank" rel="noopener">https://code.qt.io/qt/qt5.git</a></p>
</blockquote>
<p>这样会在你的目录下检出Qt顶层仓库。</p>
<p>然后执行Perl脚本检出子库</p>
<blockquote>
<p>perl init-repository</p>
</blockquote>
<ul>
<li>注：这里边官方文档说是可以使用–no-webkit来屏蔽webkit相关子库，但是我这里是不能用这个参数的，无法解析这个参数，看脚本内容也没有跟–no-webkit相关的。虽然把webkit的代码检出了，但是编译的时候，如果你没有安装上边说的icu套件，编译会自动跳过的。</li>
</ul>
<p>接下来是configure</p>
<blockquote>
<p>configure -prefix f:/qtsrc/qt-msvc2015-x64 -debug -static -platform win32-msvc2015 -c++11 -opengl desktop -qt-libpng -qt-libjpeg -nomake examples -nomake tests -mp</p>
</blockquote>
<p>这是我的配置，基本上都是一眼就能看出来的选项。(从我的观察来看Qt5以上应该已经不支持<em>demos</em>这个选项了，如果加了会报错)</p>
<p>然后编译</p>
<blockquote>
<p>nmake</p>
</blockquote>
<p>转移到其他目录(就是-prefix指定的目录)</p>
<blockquote>
<p>nmake install</p>
</blockquote>
<p>在目录下就能看到已经编好的工具还有lib了。想要dll什么的改下configure的参数就可以了</p>
]]></content>
      <categories>
        <category>Programming</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>Qt</tag>
      </tags>
  </entry>
  <entry>
    <title>总结与展望</title>
    <url>/2016/01/03/summary-and-preview/</url>
    <content><![CDATA[<p>2015注定是变化的一年，在大学呆久了总要出来看看，或许这就是大家常说的“走入社会”吧。</p>
<a id="more"></a>
<h3 id="这一年，是现实和理想有激烈冲突的一年："><a href="#这一年，是现实和理想有激烈冲突的一年：" class="headerlink" title="这一年，是现实和理想有激烈冲突的一年："></a>这一年，是现实和理想有激烈冲突的一年：</h3><p>理想是丰满的，现实总是骨感的，年轻人的骨子里总有那么一股子冲劲，但是现实会把这骨子冲劲硬生生的顶回去。但是我依旧会坚持自己的那种我行我素，因为程序猿不也应该有一点自己的职业操守吗？</p>
<h3 id="这一年，是与Qt结缘的一年："><a href="#这一年，是与Qt结缘的一年：" class="headerlink" title="这一年，是与Qt结缘的一年："></a>这一年，是与Qt结缘的一年：</h3><p>工作的原因，让我开始接触Qt，大学的时候也玩过Qt，那时的玩也不过是照着教程随便搞搞。这一年我开始真正的去认识Qt，我开始试着了解他的源码，开始体会它的设计哲学……也开始学着摆脱别人的描述，去体会他的强大；</p>
<h3 id="这一年，是与C-确立关系的一年："><a href="#这一年，是与C-确立关系的一年：" class="headerlink" title="这一年，是与C++确立关系的一年："></a>这一年，是与C++确立关系的一年：</h3><p>是的，迷茫了四年，最终还是选择了这个让人又爱又恨的语言。也许冥冥中有一种定数，让我厌恶java，找python的工作又找不到，最后还是因为C++得以被收留。我开始重新审视，开始变得立体，开始试着去玩转它；</p>
<h3 id="这一年，是烦躁的一年："><a href="#这一年，是烦躁的一年：" class="headerlink" title="这一年，是烦躁的一年："></a>这一年，是烦躁的一年：</h3><p>从大学走出了，我离开了一批人，又认识了更多的人……我从他们的身上看到了低调，可我却没有学会。这是烦躁的一年，烦躁的几乎要被冲昏头脑，变得飘飘然……</p>
<hr>
<p>2015年，是一个岔路口，我在纠结……<br>2016年，是下一条路，我会更加坚定！</p>
<hr>
<p>##2016年，少说多做，脚踏实地，从心出发……</p>
]]></content>
      <tags>
        <tag>随笔</tag>
      </tags>
  </entry>
  <entry>
    <title>一只狗</title>
    <url>/2015/12/01/dog-say/</url>
    <content><![CDATA[<p>一只狗是我微信的名字，这个名字确实蛮滑稽的。但确是我用过这么多的昵称名字里边最喜欢的，可以说除了QQ昵称之外最喜欢的。</p>
<p>经常有人问我，为什么会叫一只狗呢。我说：因为狗是人类最好的朋友啊……</p>
<a id="more"></a>

<p>在世上活了二十几载，却发现，单纯的朋友关系有多么的珍贵，保有一份朋友间的单纯有多么的难。如果重新选择，或许我更愿作他人身边的一只狗，这样在朋友高兴的时候，离开我我也不会觉得难过；朋友难过的时候找我倾诉，我依旧很高兴。</p>
<p>人就是这么复杂，七情六欲，高兴、生气、猜疑、嫉妒……或许狗就没有，到了狗那里就是摇尾巴啦~~</p>
<p>如果选择做一只狗，或许就不会因为其他的利益纠葛衍生出背叛，决裂。因为狗忠诚。这样看或许人不如狗……</p>
<p>我确实喜欢狗，但是和其他人的喜欢还是不同的。我不喜欢把动物放在对立面。我喜欢狗，是因为狗忠诚、单纯、阳光；我不喜欢猫，是因为猫高冷、忧郁、复杂……</p>
<p><em>那个人好奇怪，他好像一只狗……</em></p>
]]></content>
      <tags>
        <tag>随笔</tag>
      </tags>
  </entry>
  <entry>
    <title>写在深夜</title>
    <url>/2015/03/13/deep-night/</url>
    <content><![CDATA[<p>自己刚开始搞博客是去年12月份，弄得热火朝天，结果到头来写了两三篇日志就辍笔了。真是尴尬。寒假回家的时候就有心上来弄一下，但是玩的不亦乐乎也就忘了这茬了。在公司，晚上就有很多机会搞这个博客了。今天的收获不小，把之前一直搞不定的文件夹权限的问题搞定了，FTP连接也搞定了，现在的WordPress总算是稳定了。自己也能专心写写博客。但是还有phpmyadmin搞不定。没办法，自己nginx实在是不懂，搞了个反向代理也是现学现卖，其实原理还是傻傻的分不清，有时间再去详细弄弄。</p>
<a id="more"></a>

<p>这两天都是蛮开心的，毕业设计方面有比较大的进展，一直搞不定WP的TCP通信，昨天偶然想起我在图书馆借的一本《Windows Phone8开发技巧与案例精解》，最喜欢看这种书了，有例子一看就明白。果然，有一个现成的例子，照着敲了一下，还真的跑起来了。不过WP的TCP通信强行用异步的方式我也是醉了，没个例子只看文档的话还真是傻傻的弄不清。明天白天开始撸开题报告，感觉在公司就比较充实了，在学校玩心太重，真是罪过。港澳通行证也到了，还不错的样子。</p>
<p>《苏东坡传》回家之后看完了，感觉还是和我最初看的时候有一点点不一样，慢慢的接受了林对苏的赞美，也开始欣赏苏的为人。有时候想想自己对于很多事情太过武断，总是以为自己是见微知著，但是到最后却发现自己其实是坐井观天罢了。希望自己以后对事情能够更加认真些吧，这样也让自己看起来更稳重些吧。</p>
]]></content>
      <tags>
        <tag>随笔</tag>
      </tags>
  </entry>
  <entry>
    <title>【复音口琴】空气伴奏、爵士卡尔、小提琴奏法、曼陀铃技巧</title>
    <url>/2014/12/22/kong-jue-xiao-man/</url>
    <content><![CDATA[<p>空气伴奏是最简单了，就是直接吹七个孔。但是吹奏的时候要注意，如果平时是含五孔堵四孔，吹空气伴奏要注意口型的变化。空气伴奏也被叫做和弦伴奏，因为你吹七个孔123456[1]实际上吹出的只是135这是一个和弦。但是口琴的构造特殊，所以标准和弦一把琴就能吹一个两个的，如果想吹奏其他和弦，就要多只口琴，利用移调来实现。这个我觉得自娱自乐没必要纠结了，毕竟编谱还是和我们比较遥远的。</p>
<a id="more"></a>

<p>爵士卡尔奏法，也被叫做手哇音。吹奏主音的同时捂住的双手放开，声音便如呜哇一般。好像布鲁斯口琴上也是有这个技巧的。这个技巧也是说起来很容易说清，效果上比较难做到位。要自己反复体会。我在自己吹奏的时候有一种体会，可以稍稍拖一点舌伴奏，也就是在空气伴奏上把这个手哇音做出来，听起来会饱满一些。如果是伴奏的铿的声音出来的一瞬间，做出了手哇音，效果则没那么好。</p>
<p>小提琴奏法，口型缩到最小，吹琴格上排一个孔，同时用手轻微晃动口琴，使口琴发出小提琴般的声音。这里边我自己刚开始吹奏的时候发现，不像，一晃动口琴音就断了。这里边还是和杨敬民老师的讲座里学习了一下，下嘴唇放到琴格贴上下排孔，无论如何晃动，只要下嘴唇不离开琴格，音就不会断了，可以通过嘴的感觉来调整你自己晃动口琴的幅度，这方法简直是太机智了；另外就是如果自己觉得技巧游刃有余了，可以尝试在吹奏小提琴奏法时，根据乐曲需要加入滑音，有奇效。不过要看口琴，我自己的口琴感觉滑音控制不好，会发出啸音。</p>
<p>曼陀铃奏法，一种非常清脆悦耳的奏法，如曼陀铃一般。吹奏琴格上排单孔，口型缩到最小，舌头在里面前后或左右摆动。我自己习惯左右摆动，因为这样够快。但实际上我特意在网上听了曼陀铃这种音色，并没有特别快，所以前后也可以的。其实我听了好多视频，感觉很多人把曼陀铃都快演奏成风铃了。这里边的坑主要是：左右摆动频率要一致，不然自己就会听出来效果很差。而且在移动口琴时经常会因为不熟练，而导致乐曲演奏不连贯突然失声了，这也是在练习时要注意的。</p>
<p>以上的这四种技巧在练习的时候也是，每天练一段，时间一长自然感觉就到位了。</p>
]]></content>
      <categories>
        <category>口琴吹奏技巧</category>
      </categories>
      <tags>
        <tag>harmonica</tag>
        <tag>复音口琴</tag>
      </tags>
  </entry>
  <entry>
    <title>【复音口琴】前后伴奏技巧</title>
    <url>/2014/12/21/qian-hou-ban-zou/</url>
    <content><![CDATA[<p>单音之后紧接着技巧的学习大概有两种套路。</p>
<p>学习单音的相关技巧，如手震音、琶音、装饰音(倚音)、波音、咳音等<br>学习伴奏的相关技巧，如前后伴奏、大小伴奏等<br>值得一提的是，如果下定决心学下去，最后这些技巧肯定是都要学习的。那就无所谓先后了。</p>
<a id="more"></a>

<p>这里就先说伴奏技巧。简单的伴奏就是前伴奏、后伴奏。</p>
<p>硬要说区别就是：</p>
<ul>
<li>前伴奏：在吹奏单音的同时加上舌头伴奏</li>
<li>后伴奏：先吹奏单音，保持单音不断，加入舌头伴奏</li>
</ul>
<p>也就是说舌头伴奏加的时机就是前后伴奏的区别。</p>
<p>在学习新式单音之后，舌头会稍微灵活了一点。</p>
<ul>
<li>吹前伴奏：在吹奏单音的同时，将舌头迅速靠上去，既有主音，又可以发出清晰地：铿、铿、铿的音。</li>
<li>吹后伴奏：新式单音吹出主音之后，在谱子标记的位置收起舌头，再快速弹出，达到主音不断，又可以清晰的发出：铿、铿、铿的音。</li>
</ul>
<p>这里解释一下铿、铿、铿的音是怎么来的，主要就是极短的空气伴奏就会是铿、铿、铿的音了。</p>
<p>很多复音口琴的技巧其实一句两句便能说清楚，但是练习是要很长时间的。</p>
<p>在前后伴奏上可能遇到的坑就是：</p>
<ol>
<li>刚开始舌头灵活性较差，不能快速贴琴，听到的就是空气伴奏的声音；</li>
<li>另外就是前后伴奏分不开，本来应该吹前伴奏的，吹成了后伴奏，或者是本来应该吹成后伴奏的，吹成了前伴奏。</li>
</ol>
<p>对于第一个情况，就是一个原则，由慢到快。做专项练习。吹一个音，舌头先是慢慢贴琴，然后逐步加快，每天做做，舌头就练出来了；对于第二种情况，一定要严格按照谱子来吹，而且自己要注意分辨是否吹错。如果这个不强行纠正自己，就会走向歪路，这对以后吹奏变奏曲，有很大影响。</p>
<p>而且关于节奏实际上有很多种，但是常用的就那么几种，类似于什么前八后十六，前十六后八，等等。所以遇上一个新的节奏舌头反应不过来，只要自己停下来，专门练习这个节奏，大概十几分钟就掌握了，而且下次吹就很容易了。所以遇上新节奏不要强行求快，一定要分解下来练习，有奇效……</p>
]]></content>
      <categories>
        <category>口琴吹奏技巧</category>
      </categories>
      <tags>
        <tag>harmonica</tag>
        <tag>复音口琴</tag>
      </tags>
  </entry>
  <entry>
    <title>【复音口琴】手震音、波音、琶音、装饰音技巧</title>
    <url>/2014/12/21/shou-bo-pa-zhuang/</url>
    <content><![CDATA[<p>手震音、波音、琶音、装饰音放在一起说了，因为这几个技巧理解上大家会很好理解。所以每个技巧注意的点都不多。</p>
<p>首先要说的第一坑就是这些技巧练习是很好练习，所以很容易给人一个错觉，自己已经掌握了。我自己就曾经入过这个坑。</p>
<p>这些技巧的重点在于效果是否到位，而不在于你会不会，但是话说回来，所有技巧都是要求效果到位。</p>
<a id="more"></a>

<p>首先是各种技巧的吹奏要点：</p>
<ul>
<li>手震音：吹奏时，用手捂住口琴下部，根据曲子情感做一开一合，听起来有一种震颤的感觉。具体的图就不上了，很多教材都有</li>
<li>波音：实际上波音，我个人感觉就是简化了简谱记号，一个标有波音记号的，在这个音本身的节奏上要吹成和后一个音前八后十六的节奏，这个教材上也有写，不多说</li>
<li>琶音：以高音1为例，从倍低音1滑至高音1。就是从琶音标记的音第低两个八度的音滑至这个音。滑动的速度有快有慢，看曲子要求。</li>
<li>装饰音(倚音)：在标有装饰音记号的地方，将装饰音快速吹出，不占正常音的时值。这里其实也不是很准确，既然你有音吹，肯定就要占时值，但是这么说的原因是，体现了它的吹奏时极为快速的，要尽可能的快速而清晰的吹出来，以最少的时值吹出它。</li>
</ul>
<p>这四种技巧如果说会，自己看了就能吹，但是效果好不好，那就看个人技术了。下面说说四种技巧注意的要点：</p>
<ul>
<li>手震音：1、在演奏中，要时刻注意移动自己的手，如果吹低音6，但是你的手还在口琴中间，必然是没什么效果的；2、这个技巧实际上有个简单的分脑，就是有些曲子中手震音的技巧的部分还会有舌伴奏，这个时候很容易自己手的开合就跟着伴奏的节奏走了，效果显然是不好的；3、对曲子情绪的把握，有控制的手速开合。</li>
<li>波音：这个大坑没有，但是在比较快速的曲子上，要多加练习，不然节奏把握不好，很可能吹成前十六后八。</li>
<li>琶音：这个有个小坑，就是无论是快速滑动还是慢速滑动，中间经过的音，每个都是平均且清晰的，否则效果会很差。所以移动口琴的速度要做到有所控制，也不是随便乱拽一气的。</li>
<li>装饰音：这个参考了傅豪久老师的讲解，我才真正理解了什么是快，还是建议多听听网上老师们的视频。不然自己很难理解什么算是快。但是也要根据曲子来。比如龙的传人中有一段67[1][2]的装饰音，这个就没必要求快，因为曲子上面明显标记了缓慢。但是推荐大家在练习这个技巧的时候尽量吹快。毕竟快是难吹的，如果你能吹快，慢自然就会了，那个时候演奏乐曲就随心所欲了。</li>
</ul>
<p>这四种技巧，每一种练习的时候，可以不必拘泥于短时间大量的重复练习，而是需要一个长期积累的过程，每天都要把这几种技巧多多复习，这样练起来效果会比较好……</p>
]]></content>
      <categories>
        <category>口琴吹奏技巧</category>
      </categories>
      <tags>
        <tag>harmonica</tag>
        <tag>复音口琴</tag>
      </tags>
  </entry>
  <entry>
    <title>【复音口琴】单音技巧</title>
    <url>/2014/12/20/dan-yin-ji-qiao/</url>
    <content><![CDATA[<p>单音里边又分旧式单音和新式单音。</p>
<p>旧式单音就很容易学了，有两种方法</p>
<ol>
<li>口型缩到最小，然后吹一排孔，也很容易分辨自己是否吹干净，没杂音就算是吹对了；</li>
<li>或者是吹三个孔，拿3为例，吹234这三孔，因为旁边两孔均是吸音，所以只能吹出3这个音。吸音也是如此；</li>
</ol>
<p>第一种方法如果想验证是不是只吹了一个孔而不是吹成了第二种方法的三个孔，有一个小技巧，找一个吹音，例如3，保持口型不变，旁边移动一个格子，继续吹，如果是第二种方法会吹出三度和音的效果(吹到了3和5)，如果你的口型是第一种口型正确的话应该是吹不出声音，因为4是吸音，吹是不发声的。</p>
<p>旧式单音的两种方法均可以，而且也很好练习，就不做过多叙述了。</p>
<a id="more"></a>

<p>而新式单音则是新手的第一道门槛，也是舌堵单音。口含7孔堵住6个。</p>
<p>看到很多人会纠结是不是含七个，我自己的亲身体会是没必要纠结。因为我也是前几天在练习分解和音的时候偶然发现，我一直都是含5个堵住4个。但是只要掌握了这个技巧，改过来是很好改的。所以在新式单音的学习上，重点还是在舌堵，而不是多少个孔。</p>
<p>练习的时候，我比较推崇闲庭落木的方法，附上<a href="http://tieba.baidu.com/p/2741702530" target="_blank" rel="noopener">他的教学视频</a>。文字叙述的话，便是先吹空气伴奏，也就是七个孔一起吹，然后舌头堵上去，之后向左摆动调整位置，直到吹的单音干净。开始的时候，一个音一个音的练习，基本上把大部分的音都可以吹到干净，舌头在口中的相对位置便记熟了。</p>
<p>然后便练习新式单音在口琴上直接移动爬音阶。其实这个就很好练习了。放松自己的舌头，然后在琴上滑动吹奏各个音。需要注意的只是舌头只是搭在琴上，而不是黏在琴上，这一点尤其重要，所有的乐器在演奏的时候都是很放松的，如果舌头紧绷那必然也是不对的。</p>
<p>在可以熟练的吹奏音阶之后，找一些简单的曲子，我自己当时是吹奏《口琴考级曲集》里面的一二级曲子，当然事实上，什么简谱都可以拿来练习。但是刚开始一定不要求快，而是先吹每个音都吹准，然后才是快，这点很重要，不然养成了习惯，单音很容易吹不干净。</p>
<p>这些过程学习的时间因人而异，关键是看效果，效果吹到位了，单音吹干净了，便是学成了。</p>
<p>新式单音的练习可以锻炼自己的舌头的感觉，并且如果想学习复音口琴后面的技巧，干净的单音是前提。如果单音吹不干净，伴奏或者是后面的五度八度和音感觉都会很差。</p>
<p>关于练习时口水的问题，是很多刚开始学习的人都有的一个经历，如果特别讨厌这种感觉，那就不要学习了，勉强自己也不好。但是值得一提的是，随着自己的练习，口水就没有之前流的那么任性了，现在我吹琴，有时候吹着吹着反而觉得在琴上划不动了，口水流的太多只是刚开始的一个反应。还有就是，要注意保持口琴的清洁，吹奏结束要倒一倒口水，然后再用点酒精消消毒，擦干净，要不然时间长琴不卫生，而且还会有异味……</p>
]]></content>
      <categories>
        <category>口琴吹奏技巧</category>
      </categories>
      <tags>
        <tag>harmonica</tag>
        <tag>复音口琴</tag>
      </tags>
  </entry>
  <entry>
    <title>我与复音口琴</title>
    <url>/2014/12/19/harmonica-with-me/</url>
    <content><![CDATA[<p>自从尺神经卡压之后就再也没弹过吉他。但是我这个人又是闲不住的人。我爸年轻的时候是吹口琴，家里有几只口琴，复音，布鲁斯，半音阶倒是都有。有幸都吹过吧。于是就决定把口琴捡起来。我自己有个习惯，在入坑之前都要提前踩踩点，在逛复音口琴吧的时候偶然听到了盛叔的《龙的传人变奏曲》，当时觉得特别神奇，为什么一只口琴可以吹出这种感觉，再加上对三种口琴价格的比较，最终还是决定选择复音口琴。如今已是大四，两年过去了，复音口琴的技巧学的七七八八了。更准确的说《龙的传人变奏曲》需要的技巧已经学的差不多了，最近又入了一只ANm的复音口琴，就是专门为了练习这首曲子。</p>
<a id="more"></a>

<p>回头想想自己吹口琴的日子，其实也并不好过。而且当时觉得这首曲子似乎就是遥不可及。幸好开始的时候拿到了《口琴考级曲集》这本书，当时只知道，这本书学的差不多就应该能吹了。目的很单纯，学技巧。我也特别喜欢对技巧的掌握。</p>
<p>开始学习复音口琴，我是直接学习新式单音的，因为旧式单音初中我就可以吹了，只是捡了捡，便投入了新式单音的学习当中。口水流的不行，但是我知道这首曲子的基础就是这个东西，不会我肯定吹不了，而且我心里是有一个感觉的，就和我当初玩吉他弹唱一样，如果一直吹旧式单音，到最后就是找歌曲简谱，每天吹一吹，这种开始还好，后来就会非常无聊。所以自己开始时也很卖力的学习复音口琴。在这种驱动下，新式单音吹了两个礼拜便学会了，当时一个最大的体会就是前一个礼拜或者是一个多礼拜可能每天吹都是吹不干净，口水流的到处都是，但是很可能就在某一天自己突然吹准了一个音，就入门了，后面吹音阶就都“口到琴来”了。但我自己很清楚，没有自己前面一个多礼拜的碌碌无为，也不可能在某一天入门，所以在乐器上至少我相信量变是可以到质变的。</p>
<p>新式单音学会之后，下一个坎马上就来了，就是前后伴奏了。前后伴奏，刚开始的时候舌头很不灵活，在参考了很多资料以后，自己摸索出来一套练习方法，也就入了门，吹过一年多之后，发现这个方法和傅豪久老师的提倡的练习方法也是不谋而合，自己也是心里暗喜。前后伴奏大概吹了三四个礼拜，将近一个月的样子。每天都是打开《口琴考级曲集》从头开始，一首首的吹，然后再把自己正在练习的曲目着重吹一下，再和复音口琴吧拿到的一些录音对比一下，看看是不是吹的不正确。就这样前后伴奏拿下了。在拿下了前后伴奏，我庆幸自己在逛贴吧的时候看到了大家的心得：不要乱加伴奏，要时刻注意按照曲子编排的伴奏来吹，不然就会走向歪路。这也一直在警醒着我自己，到现在来看，是非常有用的，按照曲子编排的伴奏可以练习你对舌头的控制能力，以至于现在对大多数节奏的伴奏自己也是烂熟于心。</p>
<p>当时学习了前后伴奏以后还是很兴奋的，但是其实大家听来听去，我每天也就是吹那些老掉牙歌曲或者是一些儿歌，当然这些都是考级曲集里的，当时自己的想法是要让自己更加熟练，每天都要吹一吹。这种状态持续了一段时间，基本没学新的技巧，一直是在巩固。</p>
<p>之后自己开始考级曲集四级的内容，但是仔细回忆一下四、五级的内容我基本上并没有严格按照考级上的顺序来，而是自己先看看哪些技巧好学或是自己比较喜欢，就优先尝试一下，而且当时的自己觉得四、五级的内容其实很简单，类似手震音、琶音、波音一两句话实际上就说的很明白了，尝试一下还有点味道，所以当时的自己觉得很快就学会了。于是开始每天重复吹一到五级的曲子，但在当时实际上自己已经陷入了大坑，但并没有意识到。偶尔也会吹一下其他级别的曲子，但技巧先不加。这种状态持续了一段时间之后，那个时候就刚好找实习了，找到以后去实习，大概三四个月就没吹琴了。</p>
<p>三四个月过后，刚拿起琴的时候伴奏都有点打不利索，不过练一练马上就缓过来了。这个时候开始真正的用心琢磨我自己的技术水准，在反复比较标准的录音，还是自己找到了坑。四、五级的技巧是很简单，但是真正的难点是可以很从容的使用在曲子里，并且效果到位。重新审视自己之后尤其明显的发现自己的手震音效果很差，于是开始在贴吧开贴问，在Q群问。然后重新练习。就这样还是比较短的时间就把自己的一些毛病都纠正过来了。这个时候自己才算是准五级水平，因为后来发现自己的爵士卡尔实际上之前打的并不好。</p>
<p>在学习五级左右的时候就开始发现一些问题，吹到五级之后网上很多曲子找不到标准录音，自己也就没了参照，一些比较熟悉的歌曲，自己还能知道吹的是不是这个意思，不熟悉的就很麻烦了。</p>
<p>这个阶段就是一般看六级往上的技巧，自己摸索一下，然后看看是不是有大神吹的曲子带有这个技巧。基本上后面学的技巧都肯定是和一个视频有关。就像小鼓奏法，我在网上看到了王仁计老师的巡逻兵，第一次明白了考级曲集中说的舌头打滚是什么意思，真是书上说的就是不如视频形象，当时感动的要哭，很快这个技巧就学会了。再比如曼陀铃奏法，之前也是一直不知道是什么效果，当时是看了一个口琴比赛，一位盲人演奏家吹奏的《火车向着韶山跑》，当时也是看了震撼的不行，他的技术基本也是上乘。还有很多视频，很有启发，想起来我再整理整理。这个时候恰好自己一次逛贴吧，偶然得知了，傅豪久老师的教材，便果断买了，又加上配套视频，第一次知道除了考级上面的内容，还有其他技巧，真是开阔了眼界。</p>
<p>回学校一直到现在，我觉得自己的收获要比前半段收获大了不少，而且也深刻认识到学习乐器，如果能有一个老师指导，那肯定是要比自学好太多了。这里边除了自己真正开始琢磨复音口琴之外，也是太多偶然，看到了太多对我有启发的视频，如王敬民老师的相关演出和讲座。而且不知不觉我发现自己在学习新的技巧比较快了，一个是舌头的灵活程度和控制性上比刚开始好太多，在一个可能就是琴感吧，这两种东西帮我太大忙。</p>
<p>从公司回来，也是偶然拿到了《龙的传人变奏曲》的谱子。这个值得一说的是，在我刚开始吹口琴就想求这个谱子，但是贴吧的阳光老师说，这谱子难度大，一般人拿到也是吹不了，除非是十级左右的水平可以练习一下，而且可能不确定这个谱子是不是可以公开。所以当时一心练习技巧，以便有机会求谱子。结果这次偶然就拿到了。在分解和音会吹了之后，自己最近便开始研究这个谱子。我给自己三个月的时间，争取把它能吹的比较好一点。</p>
<p>最近便是在练习双琴和龙的传人，因为高音伴奏刚学会，也兼顾着复习高音伴奏。但是每次都要用四把琴练习，也导致酒精特别费，O(∩_∩)O哈哈~</p>
<p>洋洋洒洒写了很多，是总结，也是对我自己的警醒。回头看这些，感触太深，收获太多。自己终于有一个东西可以说比较了解。但是自己最近也比较动摇，之前其实是想着这首曲子吹会了便入布鲁斯的坑，现在可能又有了新的选择。不经意间逛了一下指弹吧，又想起了吉他，但是最主要的是一个东西触动我，那就是和弦。</p>
<p>自己最近在和弦上也是看了几篇视频和文章。主要是杨敬民老师在清华大学的讲座，他首要讲的就是和声合理化，在演奏西方古典音乐或是交响乐时，复音口琴采用多把琴移调完成和声合理化，当时听了觉得特别机智，真的好神奇。就在前两天刚好看了布鲁斯口琴的关于和声问题的一个帖子，感触很深。也让我彻底明白自己为什么一直都这么喜欢吉他，或者键盘类乐器。</p>
<p>最关键就在于口琴的一个点，因为他的构造所以导致和弦单一，即使是复音口琴有了分解和弦，但不得不承认自己的舌头再溜，也不可能分瓣，所以也只能在三五八度和弦上变化，这在乍听的人上会觉得很惊讶，就像我当初听到《龙的传人》，我的第一反应是很神奇，为什么一只口琴靠吹吸可以同时变化几个音。但是如果我们一直玩下去，我们自己就会觉得单调，甚至觉得不好听。而键盘类乐器和吉他这种弹奏乐器则截然不同。所以我们在长时间听这些曲子就不会觉得疲劳，真正觉得是好听。</p>
<p>话说回来，因为以上的原因自己很动摇，所以很可能在练习完《龙的传人》，就在大学毕业之后转而学习别的乐器了。不过在学别的之前，还是要认认真真学会这首我最想吹的曲子，并且把我自己练习各个技巧的心得体会整理出来，一是做个记录，二是如果将来有人可以用的上，那就是最好了。</p>
]]></content>
      <tags>
        <tag>随笔</tag>
        <tag>harmonica</tag>
      </tags>
  </entry>
  <entry>
    <title>深夜读《苏东坡传》</title>
    <url>/2014/12/14/su-dong-po-zhuan/</url>
    <content><![CDATA[<p>林语堂的《苏东坡传》，之前是没听说过的，也没人推荐过。买书的时候，为了凑优惠，选了一部作者名气比较大的。可以精神上接近下国学大师。翻开之后却觉得甚是难读，林语堂在书中说：为一个人写传，就要对这个人够了解，因为林特别喜爱苏东坡，故参考了关于苏的各种资料，写了这本传记。</p>
<p>可是他忽略了一点，越喜爱一个人，就越难对一个人作出客观的评价。只是翻看了两三章，都是溢美之词。失了客观的传记让我抵触情绪很大。这本书与其叫《苏东坡传》不如叫《苏东坡赞》，不知自己看过全书之后会不会对这种看法有所改变。</p>
<p>但是回头想想，容不下别人的溢美之词，也许就是自己的心里太满，做不到无欲无求，也许自己本身就不够客观，这也是自己应该修炼的地方。真不知道什么时候能改改这脾气。</p>
]]></content>
      <tags>
        <tag>随笔</tag>
      </tags>
  </entry>
</search>
